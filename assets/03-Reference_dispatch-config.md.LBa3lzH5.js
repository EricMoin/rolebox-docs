import{_ as e,o as d,c as a,a2 as s}from"./chunks/framework.CuUVYbLV.js";const k=JSON.parse('{"title":"调度配置","description":"调度（dispatch）配置参考——role.yaml 的 dispatch 块只接受两个字段，其余参数属于编程式 DispatchManagerConfig 与三个环境变量","frontmatter":{"title":"调度配置","description":"调度（dispatch）配置参考——role.yaml 的 dispatch 块只接受两个字段，其余参数属于编程式 DispatchManagerConfig 与三个环境变量"},"headers":[],"relativePath":"03-Reference/dispatch-config.md","filePath":"03-Reference/dispatch-config.md"}'),i={name:"03-Reference/dispatch-config.md"};function o(n,t,r,h,c,l){return d(),a("div",null,[...t[0]||(t[0]=[s(`<h1 id="调度配置-dispatch-configuration" tabindex="-1">调度配置（Dispatch Configuration） <a class="header-anchor" href="#调度配置-dispatch-configuration" aria-label="Permalink to &quot;调度配置（Dispatch Configuration）&quot;">​</a></h1><p>调度（dispatch）是把一个角色的任务交给子代理会话执行的机制。它的配置面比看上去窄：<code>role.yaml</code> 的 <code>dispatch:</code> 块只接受 2 个字段，环境变量只解析 3 个，其余参数（包括全部预算上限）都属于编程式的 <code>DispatchManagerConfig</code>。</p><blockquote><p>相关：<a href="/rolebox-docs/03-Reference/role-yaml.html">role.yaml 参考</a>｜<a href="/rolebox-docs/02-Guide/subagents.html">子代理</a>｜<a href="/rolebox-docs/02-Guide/graph-workflows.html">图工作流</a>｜<a href="/rolebox-docs/03-Reference/error-handling.html">错误处理</a></p></blockquote><h2 id="配置面一览" tabindex="-1">配置面一览 <a class="header-anchor" href="#配置面一览" aria-label="Permalink to &quot;配置面一览&quot;">​</a></h2><table tabindex="0"><thead><tr><th>配置面</th><th>载体</th><th>覆盖内容</th><th>谁来设置</th></tr></thead><tbody><tr><td>角色级</td><td><code>role.yaml</code> 的 <code>dispatch:</code> 块</td><td>2 个超时</td><td>角色作者</td></tr><tr><td>全局覆盖</td><td><code>ROLEBOX_DISPATCH_*</code> 环境变量</td><td>3 个超时 / 保留时间</td><td>运行环境</td></tr><tr><td>编程式</td><td><code>DispatchManagerConfig</code>（<code>configOverrides</code>）</td><td>全部字段，含预算上限</td><td>调用 rolebox 的宿主代码</td></tr></tbody></table><p>三者的合并优先级（低 → 高）是：内置默认值 → 角色 <code>dispatch:</code> 块 → 环境变量 → <code>configOverrides</code>。</p><h2 id="role-yaml-的-dispatch-块" tabindex="-1">role.yaml 的 dispatch 块 <a class="header-anchor" href="#role-yaml-的-dispatch-块" aria-label="Permalink to &quot;role.yaml 的 dispatch 块&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dispatch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  backgroundStaleTimeoutMs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">900000</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 后台任务默认过期超时（毫秒）</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  syncPromptTimeoutMs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600000</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # 同步调度中子代理提示词完成的超时（毫秒）</span></span></code></pre></div><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>必需</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>backgroundStaleTimeoutMs</code></td><td><code>number</code>（正数）</td><td>否</td><td><code>900000</code>（15 分钟）</td><td>后台任务的默认过期超时；任务自带的 <code>timeoutMs</code> 优先于它</td></tr><tr><td><code>syncPromptTimeoutMs</code></td><td><code>number</code>（正数）</td><td>否</td><td><code>600000</code>（10 分钟）</td><td>同步调度中子代理提示词完成的超时</td></tr></tbody></table><p>它是<strong>白名单解析</strong>，边界有三条：</p><ul><li><strong>只读上面这两个键。</strong> 块里写其它键既不会生效、也不会有报错提示；预算字段的归属见下文。</li><li><strong>值必须是正数。</strong> 非数字、0 与负数会被跳过，并在日志里留一条警告。</li><li><strong>每个角色各算一份。</strong> 同一进程里每个角色都有独立的合并结果；管理器级别的合并取主角色（<code>mode: primary</code>）的那一份。</li></ul><p>块内字符串同样支持 <code>{env:变量名}</code> 插值，见下文「环境变量插值」。</p><h2 id="环境变量" tabindex="-1">环境变量 <a class="header-anchor" href="#环境变量" aria-label="Permalink to &quot;环境变量&quot;">​</a></h2><p>只有下面 3 个 <code>ROLEBOX_DISPATCH_*</code> 变量会被解析，其余名字不产生任何效果：</p><table tabindex="0"><thead><tr><th>变量</th><th>映射字段</th><th>说明</th></tr></thead><tbody><tr><td><code>ROLEBOX_DISPATCH_BG_STALE_MS</code></td><td><code>backgroundStaleTimeoutMs</code></td><td>后台任务默认过期超时（毫秒）</td></tr><tr><td><code>ROLEBOX_DISPATCH_MATERIALIZE_TIMEOUT_MS</code></td><td><code>materializeTimeoutMs</code></td><td>结果物化抓取的超时（毫秒）</td></tr><tr><td><code>ROLEBOX_DISPATCH_RESULT_RETENTION_MS</code></td><td><code>resultRetentionMs</code></td><td>结果文件保留时间（毫秒）</td></tr></tbody></table><p>解析规则与 <code>dispatch:</code> 块一致：值必须是正数，NaN、≤0 与空字符串都会被忽略。预算字段<strong>没有</strong>环境变量入口。</p><p>另有三个与调度可观测性有关、但不改变调度行为的变量：<code>ROLEBOX_METRICS</code> 开启指标采集与持久化（默认关闭），<code>ROLEBOX_METRICS_EXPORT</code> 作为指标快照导出文件的默认路径，<code>ROLEBOX_NOTIFICATIONS_CONFIG</code> 指向全局通知配置文件。</p><h2 id="编程式-dispatchmanagerconfig" tabindex="-1">编程式：DispatchManagerConfig <a class="header-anchor" href="#编程式-dispatchmanagerconfig" aria-label="Permalink to &quot;编程式：DispatchManagerConfig&quot;">​</a></h2><p><code>dispatch:</code> 块能表达的只有两个超时；其余参数定义在 <code>DispatchManagerConfig</code>，由调用 rolebox 的代码通过 <code>configOverrides</code> 注入，这是配置预算上限的<strong>唯一</strong>途径。</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 调用 createDispatchManager() 时注入</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> configOverrides</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  maxInputTokensPerRequest: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 一次请求累计输入 token</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  maxOutputTokensPerRequest: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 一次请求累计输出 token</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  maxCostPerRequest: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 一次请求累计费用（USD）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  maxInputTokensPerSession: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 单个子代理会话输入 token</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  maxCostPerSession: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 单个子代理会话费用（USD）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  budgetSampleIntervalMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">15_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 采样间隔</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createDispatchManager</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // sessionClient / resolvedRoles / storeDirectory 等其余选项</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  configOverrides,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><table tabindex="0"><thead><tr><th>字段</th><th>默认值</th><th>能否在 <code>role.yaml</code> 写</th><th>说明</th></tr></thead><tbody><tr><td><code>maxInputTokensPerRequest</code></td><td>不限制</td><td>否</td><td>一次请求内所有子代理会话累计输入 token 上限</td></tr><tr><td><code>maxOutputTokensPerRequest</code></td><td>不限制</td><td>否</td><td>一次请求内累计输出 token 上限</td></tr><tr><td><code>maxCostPerRequest</code></td><td>不限制</td><td>否</td><td>一次请求内累计费用（USD）上限</td></tr><tr><td><code>maxInputTokensPerSession</code></td><td>不限制</td><td>否</td><td>单个子代理会话输入 token 上限</td></tr><tr><td><code>maxCostPerSession</code></td><td>不限制</td><td>否</td><td>单个子代理会话费用（USD）上限</td></tr><tr><td><code>budgetSampleIntervalMs</code></td><td><code>30000</code></td><td>否</td><td>预算采样间隔</td></tr><tr><td><code>taskTtlMs</code></td><td><code>1800000</code>（30 分钟）</td><td>否</td><td>终态任务记录的存活时间；<strong>必填</strong></td></tr><tr><td><code>minRuntimeMs</code></td><td><code>5000</code></td><td>否</td><td>任务被回收前的最小存活时间；<strong>必填</strong></td></tr><tr><td><code>backgroundStaleTimeoutMs</code></td><td><code>900000</code></td><td>是</td><td>后台任务默认过期超时</td></tr><tr><td><code>watchdogIntervalMs</code></td><td><code>15000</code></td><td>否</td><td>每任务 reconcile 看门狗间隔</td></tr><tr><td><code>globalSweepIntervalMs</code></td><td><code>30000</code></td><td>否</td><td>全局 sweep 间隔</td></tr><tr><td><code>idleDebounceMs</code></td><td><code>1500</code></td><td>否</td><td>完成确认前的空闲 debounce</td></tr><tr><td><code>syncTimeoutMs</code></td><td><code>600000</code></td><td>否</td><td><strong>已废弃</strong>，改用 <code>syncPromptTimeoutMs</code></td></tr><tr><td><code>syncPromptTimeoutMs</code></td><td><code>600000</code></td><td>是</td><td>同步调度提示词超时</td></tr><tr><td><code>createRetryAttempts</code></td><td><code>3</code></td><td>否</td><td>子代理会话创建失败的总尝试次数（设为 1 关闭重试）</td></tr><tr><td><code>createRetryBackoffMs</code></td><td><code>250</code></td><td>否</td><td>上述重试的退避间隔</td></tr><tr><td><code>materializeTimeoutMs</code></td><td><code>10000</code></td><td>否</td><td>结果物化抓取的超时</td></tr><tr><td><code>resultRetentionMs</code></td><td><code>3600000</code></td><td>否</td><td>任务清理后结果文件的保留时间</td></tr><tr><td><code>outboxFirstRetryMs</code></td><td><code>3000</code></td><td>否</td><td>结果投递 outbox 的初始重试延迟</td></tr><tr><td><code>outboxMaxRetryMs</code></td><td><code>60000</code></td><td>否</td><td>outbox 最大重试延迟</td></tr><tr><td><code>outboxSweepIntervalMs</code></td><td><code>5000</code></td><td>否</td><td>outbox 轮询间隔</td></tr></tbody></table><h2 id="预算" tabindex="-1">预算 <a class="header-anchor" href="#预算" aria-label="Permalink to &quot;预算&quot;">​</a></h2><p>预算上限有五个（请求级三项、会话级两项），全部只能编程式注入。它们的行为边界：</p><ul><li>只有至少配置了一项上限时，采样器才会启动；一项都不配时不会产生任何采样开销。</li><li>超限判定分请求级与会话级两种口径，调用方据此取消对应任务。</li><li>预算用量延迟落盘，进程重启后可以接着累计。</li></ul><h2 id="看门狗与清理" tabindex="-1">看门狗与清理 <a class="header-anchor" href="#看门狗与清理" aria-label="Permalink to &quot;看门狗与清理&quot;">​</a></h2><p>调度内置三级定时器，间隔都可以在 <code>DispatchManagerConfig</code> 中覆盖：</p><table tabindex="0"><thead><tr><th>层级</th><th>机制</th><th>默认间隔</th><th>职责</th></tr></thead><tbody><tr><td>Tier 1</td><td>每任务 reconcile</td><td><code>watchdogIntervalMs</code>（15000）</td><td>事件静默超过阈值后触发一次对账</td></tr><tr><td>Tier 2</td><td>全局 sweep</td><td><code>globalSweepIntervalMs</code>（30000）</td><td>兜底扫描所有运行中任务，覆盖漏事件与崩溃恢复</td></tr><tr><td>Tier 3</td><td>空闲 debounce</td><td><code>idleDebounceMs</code>（1500）</td><td>确认空闲前等待一段时间，吸收分步之间的假空闲</td></tr></tbody></table><p>回调逐个隔离：单个任务的回调抛错不会影响其它任务的定时器。</p><p>清理相关的三个时间：</p><table tabindex="0"><thead><tr><th>参数</th><th>默认值</th><th>含义</th></tr></thead><tbody><tr><td><code>taskTtlMs</code></td><td>30 分钟</td><td>终态任务记录在被清出任务表前的存活时间</td></tr><tr><td><code>minRuntimeMs</code></td><td>5 秒</td><td>任务至少要存活这么久才允许被回收</td></tr><tr><td><code>resultRetentionMs</code></td><td>1 小时</td><td>任务清理后结果文件的保留时间；关联任务已不存在的结果文件按 24 小时阈值清理</td></tr></tbody></table><h2 id="任务状态与结果" tabindex="-1">任务状态与结果 <a class="header-anchor" href="#任务状态与结果" aria-label="Permalink to &quot;任务状态与结果&quot;">​</a></h2><p>后台任务在任一时刻处于 7 个状态之一：</p><table tabindex="0"><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td><code>pending</code></td><td>已创建、尚未启动</td></tr><tr><td><code>running</code></td><td>子代理会话正在执行</td></tr><tr><td><code>awaiting_approval</code></td><td>等待人工审批</td></tr><tr><td><code>completed</code></td><td>结果已物化并通知父会话</td></tr><tr><td><code>error</code></td><td>启动或执行失败，或被父会话拒绝</td></tr><tr><td><code>cancelled</code></td><td>被取消</td></tr><tr><td><code>timeout</code></td><td>超过过期超时，或被判定卡死</td></tr></tbody></table><p>超过内联上限（默认 16000 字符）的结果会溢出到工作区下的 <code>.rolebox/state/results/{taskId}.txt</code>（先写临时文件再原子替换），读取时支持偏移量与尾部两种分页方式。任务失败的原因保留在任务记录上，便于事后排查；状态与结果的具体读取方式见<a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a>与<a href="/rolebox-docs/03-Reference/error-handling.html">错误处理</a>。</p><p>模型侧的编排入口是图工具集与 <code>task_*</code> 兼容层；裸 <code>dispatch_*</code> 工具在 v1.9.0 不再注册给模型（原因与替代关系见<a href="/rolebox-docs/06-Appendix/migration.html">迁移对照</a>）。</p><h2 id="环境变量插值" tabindex="-1">环境变量插值 <a class="header-anchor" href="#环境变量插值" aria-label="Permalink to &quot;环境变量插值&quot;">​</a></h2><p><code>role.yaml</code>（含 <code>dispatch:</code> 块）中任意字符串位置都可以写 <code>{env:变量名}</code>，在加载时解析；对象与数组会深度遍历。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dispatch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  syncPromptTimeoutMs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{env:ROLEBOX_SYNC_PROMPT_MS}&quot;</span></span></code></pre></div><p>变量未设置时，占位符<strong>原样保留</strong>并记录一条日志，不会被替换成空字符串。</p><h2 id="常见错误" tabindex="-1">常见错误 <a class="header-anchor" href="#常见错误" aria-label="Permalink to &quot;常见错误&quot;">​</a></h2><h3 id="把预算字段写进-dispatch-块" tabindex="-1">把预算字段写进 <code>dispatch:</code> 块 <a class="header-anchor" href="#把预算字段写进-dispatch-块" aria-label="Permalink to &quot;把预算字段写进 \`dispatch:\` 块&quot;">​</a></h3><p>❌ 解析器只遍历 <code>backgroundStaleTimeoutMs</code> 与 <code>syncPromptTimeoutMs</code> 两个键，下面的字段不会被读取（也不会有报错提示）：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dispatch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  maxInputTokensPerRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  maxCostPerSession</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span></span></code></pre></div><p>正确做法是通过 <code>configOverrides</code> 注入，见上文「编程式：DispatchManagerConfig」。</p><h3 id="期望环境变量能配预算" tabindex="-1">期望环境变量能配预算 <a class="header-anchor" href="#期望环境变量能配预算" aria-label="Permalink to &quot;期望环境变量能配预算&quot;">​</a></h3><p>三个 <code>ROLEBOX_DISPATCH_*</code> 变量里没有预算相关项：预算只能编程式配置。</p><h3 id="依赖-synctimeoutms" tabindex="-1">依赖 <code>syncTimeoutMs</code> <a class="header-anchor" href="#依赖-synctimeoutms" aria-label="Permalink to &quot;依赖 \`syncTimeoutMs\`&quot;">​</a></h3><p>它是 <code>syncPromptTimeoutMs</code> 的已废弃别名，新配置一律用后者。</p><h2 id="备注" tabindex="-1">备注 <a class="header-anchor" href="#备注" aria-label="Permalink to &quot;备注&quot;">​</a></h2><blockquote><p>自 v1.5.0 起，旧的并发槽位子系统、每请求的会话预算跟踪与相关扩展点已删除；图节点的并发度改由图引擎自管（就绪前沿、循环组遍历上限、节点级预算），因此这里不再有任何并发 / 队列 / 背压参数。</p></blockquote><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li><a href="/rolebox-docs/03-Reference/role-yaml.html">role.yaml 参考</a> — 完整的角色字段参考</li><li><a href="/rolebox-docs/02-Guide/subagents.html">子代理</a> — 子代理的声明与调度方式</li><li><a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a> — 图工具与任务工具的参数</li><li><a href="/rolebox-docs/03-Reference/error-handling.html">错误处理</a> — 调度失败时的错误面与恢复策略</li></ul>`,52)])])}const g=e(i,[["render",o]]);export{k as __pageData,g as default};
