import{_ as o,C as i,o as t,c as n,a2 as d,b as c,w as s,a as l,E as r,a3 as p}from"./chunks/framework.CuUVYbLV.js";const _=JSON.parse('{"title":"信号系统实现","description":"rolebox 带外信号子系统的内部实现 — 8 种信号的四个类别、两级台账、FSM 集成、制品捕获与可观测性","frontmatter":{"title":"信号系统实现","description":"rolebox 带外信号子系统的内部实现 — 8 种信号的四个类别、两级台账、FSM 集成、制品捕获与可观测性"},"headers":[],"relativePath":"04-Advanced/signal-system.md","filePath":"04-Advanced/signal-system.md"}'),h={name:"04-Advanced/signal-system.md"};function g(k,e,E,u,y,A){const a=i("Mermaid");return t(),n("div",null,[e[1]||(e[1]=d('<h1 id="信号系统-signal-system" tabindex="-1">信号系统（Signal System） <a class="header-anchor" href="#信号系统-signal-system" aria-label="Permalink to &quot;信号系统（Signal System）&quot;">​</a></h1><p>信号（signal）是 rolebox 的带外控制通道：代理用一次 <code>signal</code> 调用表达状态转换意图，而不必把意图写进回复正文。 本页讲它的实现——词表与分类、两级台账、与函数状态机的对接、payload 的去向，以及发出一次信号究竟发生了什么。</p><blockquote><p><strong>本页边界</strong>：<code>signal</code> 工具的<strong>参数与返回文案</strong>见<a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a>；<code>signal_observed</code> 在函数定义里的写法与全部条件表达式见<a href="/rolebox-docs/02-Guide/writing-functions.html">函数规范</a>；图引擎如何消费这些分类见<a href="/rolebox-docs/04-Advanced/graph-engine.html">图执行引擎</a>；信号在节点生命周期里的运行时行为见<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>。</p></blockquote><h2 id="词表与分类" tabindex="-1">词表与分类 <a class="header-anchor" href="#词表与分类" aria-label="Permalink to &quot;词表与分类&quot;">​</a></h2><p>信号类型只有 8 个，定义在 <code>src/signal/signal-constants.ts</code>，是整条链路的唯一真相来源：工具的 Zod 枚举直接从同一份数组派生，所以校验边界不会与词表漂移。</p><p>按对状态机的影响，8 个类型分成四个类别，影响从「终止执行」递减到「仅记录」：</p><table tabindex="0"><thead><tr><th>类别</th><th>类型</th><th>台账记录</th><th>状态效果</th></tr></thead><tbody><tr><td>TERMINATING</td><td><code>answer</code>、<code>revise_needed</code>、<code>escalate</code></td><td>是</td><td>满足 <code>continue_until</code>，下次空闲周期可让节点完成</td></tr><tr><td>PAUSING</td><td><code>need_approval</code>、<code>blocked</code>、<code>need_clarification</code></td><td>是</td><td><code>phase = &quot;gated&quot;</code>，并打上 <code>paused</code> 证据标签</td></tr><tr><td>HANDOFF</td><td><code>handoff</code></td><td>是</td><td>不终止也不暂停，payload 里记录交接目标</td></tr><tr><td>INFO</td><td><code>progress</code></td><td>是</td><td>无状态变更，仅用于日志与监控</td></tr></tbody></table><p>四个类别是四个 <code>Set</code> 常量（<code>TERMINATING_SIGNALS</code> / <code>PAUSING_SIGNALS</code> / <code>HANDOFF_SIGNALS</code> / <code>INFO_SIGNALS</code>），<code>ALL_SIGNAL_TYPES</code> 是它们的并集——运行时 guard 用它兜住绕过 Zod 的直接调用。图引擎侧（<code>src/graph/engine/signal-bridge.ts</code>）不重复定义词表，而是从同一文件导入并再导出。</p><h3 id="终止严重度与合成-answer" tabindex="-1">终止严重度与合成 answer <a class="header-anchor" href="#终止严重度与合成-answer" aria-label="Permalink to &quot;终止严重度与合成 answer&quot;">​</a></h3><p>多个终止信号同时存在时需要定序。<code>TERMINATING_SIGNALS_BY_SEVERITY</code> 固定为 <code>escalate</code> &gt; <code>revise_needed</code> &gt; <code>answer</code>，会话级台账的「最高严重度终止信号」查询按这个顺序取第一个命中的。</p><p>还有一个不出现在词表使用面的特殊值：<code>SYNTHETIC_ANSWER_SIGNAL</code>（<code>answer</code> 加 <code>payload: { __inferred: true }</code>）。当子代理正常完成任务却没有显式调用过 <code>signal</code> 时，完成评估器（<code>src/dispatch/completion/completion-evaluator.ts</code>）替它补发这个信号，<code>__inferred</code> 标记让下游能分辨「框架推断」与「代理声明」。若台账读取失败，兜底值同样是这个合成信号。</p><h2 id="一次信号发出发生什么" tabindex="-1">一次信号发出发生什么 <a class="header-anchor" href="#一次信号发出发生什么" aria-label="Permalink to &quot;一次信号发出发生什么&quot;">​</a></h2><p><code>signal</code> 工具由 <code>src/signal/signal-tool.ts</code> 的 <code>createSignalTool()</code> 创建，在共享装配层无条件注册（三个平台都有）。它的执行分成五步：</p>',13)),(t(),c(p,null,{default:s(()=>[r(a,{id:"mermaid-114",class:"mermaid",graph:"flowchart%20LR%0A%20%20A%5B%22signal(type%2C%20payload%3F)%22%5D%20--%3E%20B%5B%22%E5%86%99%E5%85%A5%E4%BC%9A%E8%AF%9D%E7%BA%A7%E5%8F%B0%E8%B4%A6%22%5D%0A%20%20B%20--%3E%20C%7B%22%E7%B1%BB%E5%9E%8B%E5%9C%A8%E8%AF%8D%E8%A1%A8%E5%86%85%3F%22%7D%0A%20%20C%20--%3E%7C%E5%90%A6%7C%20D%5B%22%E6%8A%9B%E9%94%99%22%5D%0A%20%20C%20--%3E%7C%E6%98%AF%7C%20E%5B%22%E9%81%8D%E5%8E%86%E4%BC%9A%E8%AF%9D%E5%86%85%E6%AF%8F%E4%B8%AA%E6%B4%BB%E8%B7%83%E5%87%BD%E6%95%B0%22%5D%0A%20%20E%20--%3E%20F%5B%22recordSignal(fnState%2C%20type%2C%20payload)%22%5D%0A%20%20F%20--%3E%20G%5B%22st.kv.__signal_type%20%3D%20type%22%5D%0A%20%20G%20--%3E%20H%7B%22%E7%B1%BB%E5%88%AB%3F%22%7D%0A%20%20H%20--%3E%7CPAUSING%7C%20I%5B%22paused%20%E6%A0%87%E7%AD%BE%20%2B%20phase%20%3D%20gated%3Cbr%2F%3Eblocked%20%E5%8F%A6%E8%AE%B0%20blockedAt%20%E4%B8%8E%20120s%20%E8%B6%85%E6%97%B6%22%5D%0A%20%20H%20--%3E%7C%E5%85%B6%E5%AE%83%7C%20J%5B%22%E4%BB%85%E8%AE%B0%E5%BD%95%22%5D%0A%20%20I%20--%3E%20K%5B%22%E5%88%B6%E5%93%81%E6%8D%95%E8%8E%B7%EF%BC%88%E8%8B%A5%E5%A3%B0%E6%98%8E%E4%BA%86%20capture_payload_as%EF%BC%89%22%5D%0A%20%20J%20--%3E%20K%0A%20%20K%20--%3E%20L%5B%22%E6%8B%BC%E6%8E%A5%E8%BF%94%E5%9B%9E%E6%96%87%E6%A1%88%22%5D%0A"})]),fallback:s(()=>[...e[0]||(e[0]=[l(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=d(`<ol><li><strong>会话级台账先行</strong>：<code>sessionSignalLedger.record(sessionID, type, payload)</code> 在任何函数状态遍历之前执行，这样即便当前没有任何活跃函数，信号也不会丢；</li><li><strong>类型 guard</strong>：Zod 枚举在解析阶段已经拦截非法类型，这一步是给绕过工具协议的直接调用兜底，命中就抛出「Unrecognized signal type」并列出合法取值；</li><li><strong>函数级台账</strong>：遍历 <code>functionSessionState.getActive(sessionID)</code>，对每个有运行时状态的函数调用 <code>recordSignal(st, type, payload)</code>，并写入 <code>st.kv[&quot;__signal_type&quot;] = type</code>，最后 <code>markDirty()</code> 让状态持久化；</li><li><strong>暂停处理</strong>：PAUSING 类别额外设置 <code>st.evidenceObserved[&quot;paused&quot;] = true</code> 与 <code>st.phase = &quot;gated&quot;</code>；<code>blocked</code> 还会记录 <code>blockedAt</code> 时间戳，并在未配置时把 <code>blockedTimeoutMs</code> 设为 120000（2 分钟）；</li><li><strong>制品捕获与返回</strong>：payload 在满足条件时写进 ArtifactStore（下一节），随后按类别拼出返回文案。</li></ol><p>返回文案是分层的：没有活跃函数时是 <code>signal: &lt;type&gt; acknowledged (no active functions)</code>；否则以 <code>signal: &lt;type&gt; acknowledged | recorded on N function(s)</code> 开头，再按类别追加一个箭头片段——PAUSING 是 <code>→ function paused</code>，TERMINATING 是 <code>→ satisfies continue_until condition</code>，HANDOFF 是 <code>→ handoff to &lt;目标&gt;</code>（依次取 payload 的 <code>target</code>、<code>subagent</code>，都没有则写 <code>(unspecified)</code>），INFO 是 <code>→ informational (no state transition)</code>。</p><h2 id="函数级台账-api" tabindex="-1">函数级台账 API <a class="header-anchor" href="#函数级台账-api" aria-label="Permalink to &quot;函数级台账 API&quot;">​</a></h2><p>函数级台账存放在 <code>FnState.kv[&quot;__signals_observed&quot;]</code>，形状是 <code>Record&lt;string, unknown&gt;</code>——信号类型是键，payload（没有则 <code>null</code>）是值。<code>src/signal/signal-ledger.ts</code> 导出 6 个辅助函数，外部不直接读写这个键：</p><table tabindex="0"><thead><tr><th>函数</th><th>行为</th></tr></thead><tbody><tr><td><code>recordSignal(fnState, type, payload?)</code></td><td>写入或<strong>覆盖</strong>同类型记录</td></tr><tr><td><code>hasSignal(fnState, type)</code></td><td>键是否存在</td></tr><tr><td><code>getSignalPayload(fnState, type)</code></td><td>取 payload，未记录时返回 <code>undefined</code></td></tr><tr><td><code>readLedgerRecord(fnState)</code></td><td>返回台账的浅拷贝，供序列化或调试</td></tr><tr><td><code>clearSignal(fnState, type)</code></td><td>删除单条</td></tr><tr><td><code>clearAllSignals(fnState)</code></td><td>清空整个台账</td></tr></tbody></table><p>覆盖语义是设计选择而不是缺陷：同一个函数在一次运行中多次发出同类型信号时，后写入的 payload 覆盖先前的，台账因此始终是「每种类型一条最新记录」，而不是事件流。</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> recordSignal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">fnState</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FnState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ledger</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> readLedger</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fnState);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ledger[type] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> payload </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> payload </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  writeLedger</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fnState, ledger);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>readLedger</code> 还做了一层兼容：如果 <code>kv[&quot;__signals_observed&quot;]</code> 是数组（台账迁移前的旧格式），它会返回空对象，而条件求值那一侧会按旧格式再判一次（见下节）。</p><h2 id="会话级台账" tabindex="-1">会话级台账 <a class="header-anchor" href="#会话级台账" aria-label="Permalink to &quot;会话级台账&quot;">​</a></h2><p><code>sessionSignalLedger</code> 是 <code>SessionSignalLedger</code> 的单例（<code>src/signal/session-signal-ledger.ts</code>），内部是 <code>Map&lt;sessionID, Map&lt;type, SignalRecord&gt;&gt;</code>，<strong>按会话键控、独立于函数激活</strong>。它回答的是「这次会话里到底发过哪些信号」，而不是「某个函数看到了什么」。</p><table tabindex="0"><thead><tr><th>方法</th><th>返回</th></tr></thead><tbody><tr><td><code>record(sessionID, type, payload?)</code></td><td>写入；同类型覆盖</td></tr><tr><td><code>getTerminating(sessionID)</code></td><td>最高严重度的终止信号，或 <code>null</code></td></tr><tr><td><code>getHitlSignal(sessionID)</code></td><td>最高优先级的人工在环信号（<code>need_approval</code> &gt; <code>blocked</code> &gt; <code>need_clarification</code>），或 <code>null</code></td></tr><tr><td><code>hasSignal(sessionID, type)</code></td><td>该会话是否记录过某类型</td></tr><tr><td><code>clearSession(sessionID)</code></td><td>清除该会话的全部信号并触发持久化</td></tr></tbody></table><p>持久化落到 <code>.rolebox/state/signalledger-&lt;workspaceHash&gt;.json</code>（文件形状是 <code>{ version: 1, sessions: [...] }</code>）。写入是<strong>延迟合并</strong>的：每次变更只把内部标记置脏，500ms 内的多次变更合并成一次原子写；<code>flushSync()</code> 用于在进程退出前同步落盘。<code>setStoreDirectory()</code> 指定工作区目录，<code>recover()</code> 在重启时从磁盘恢复，<code>resetAll()</code> 只清内存（不写盘），供状态重置路径避免出现多个实例。</p><h2 id="与函数状态机的集成" tabindex="-1">与函数状态机的集成 <a class="header-anchor" href="#与函数状态机的集成" aria-label="Permalink to &quot;与函数状态机的集成&quot;">​</a></h2><p>信号进入函数状态机只有一个入口：内建命名条件 <code>signal_observed(type)</code>（<code>src/function/conditions.ts</code>）。它和 <code>user_approval</code>、<code>artifact_exists</code>、<code>plan_todos_complete</code>、<code>evidence_met</code>、<code>tool_observed</code>、<code>turn_count</code>、<code>state_eq</code> 并列。</p><p>求值按三步短路：</p><ol><li><strong>台账格式</strong>（当前）：读取 <code>FnState.kv[&quot;__signals_observed&quot;]</code>，若是对象就用 <code>arg in record</code> 判断键存在；</li><li><strong>旧数组格式</strong>：若是 <code>string[]</code>，用 <code>Array.includes(arg)</code> 判断；</li><li><strong>会话级兜底</strong>：函数级台账没有该信号时，回退到 <code>sessionSignalLedger.hasSignal(env.sessionID, arg)</code>。</li></ol><p>第三步是关键设计：它让「在没有活跃函数时发出的信号」仍然能满足 <code>continue_until: signal_observed(...)</code>，从而把人工审批这类带外事件接进函数的继续条件。</p><p>条件可以出现在函数定义的三个位置：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">continue_until</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">signal_observed(answer)</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 收到 answer 才认为这次运行结束</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">gate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">signal_observed(answer)</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # 门控：未满足前停在 gated</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">transitions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">when</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">signal_observed(escalate)</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # 收到 escalate 时激活紧急处理函数</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    activate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">emergency-handler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>配合规则按类别划分：TERMINATING 适合放进 <code>continue_until</code> 作为正常退出条件；PAUSING 适合放进 <code>gate</code> 等待恢复——它们把函数推进 <code>gated</code> 而不是让它完成；HANDOFF 与 INFO 不改变状态，适合只想留下记录、不打断流程的场景。</p><h3 id="blocked-的有界兜底" tabindex="-1">blocked 的有界兜底 <a class="header-anchor" href="#blocked-的有界兜底" aria-label="Permalink to &quot;blocked 的有界兜底&quot;">​</a></h3><p>暂停态不能无限期停留。发过 <code>blocked</code> 的函数带上了 <code>blockedAt</code> 与 <code>blockedTimeoutMs</code>（默认 120 秒），空闲续跑路径（<code>src/copilot/sources/builtin.ts</code>）在检查 gated 函数时会判断 <code>now - blockedAt &gt; blockedTimeoutMs</code>：超时就强制解封——<code>phase</code> 回到 <code>active</code>、清除 <code>paused</code> 标签与 <code>blockedAt</code>——让下一个空闲周期重新评估 <code>continue_until</code>，而不是让编排器永久挂起。</p><h2 id="制品捕获" tabindex="-1">制品捕获 <a class="header-anchor" href="#制品捕获" aria-label="Permalink to &quot;制品捕获&quot;">​</a></h2><p>当某个活跃函数的 observe 规范声明了 <code>capture_payload_as</code> 且观察目标是 <code>signal</code> 时，payload 会以 JSON 字符串写进 ArtifactStore。触发条件有两个额外约束：payload 必须存在，且当前至少有一个活跃函数；查找方式是用 <code>src/resolver/registry.ts</code> 的角色函数表反查活跃函数的名字与 observe 声明。</p><p>这条路径与 observe 系统本身的处理是重复的，而且是有意为之：observe 系统在标准链路里已经会捕获 payload，但直接工具调用（observe 未触发）的场景需要一个兜底，于是信号工具自己承担了写入职责。反查失败（拿不到角色函数表）不会让信号失败——只记一条 warn 日志。</p><h2 id="可观测性" tabindex="-1">可观测性 <a class="header-anchor" href="#可观测性" aria-label="Permalink to &quot;可观测性&quot;">​</a></h2><p>信号子系统使用 <code>createSubLogger(&quot;signal-tool&quot;)</code> 与 <code>createSubLogger(&quot;session-signal-ledger&quot;)</code> 两个命名空间输出结构化日志：</p><ul><li><code>signal-tool</code>：每发出一次信号记一条 session 级 debug；对每个函数再记一条含 <code>fnName</code>、<code>type</code>、<code>phase</code> 的 debug；制品反查失败记 warn；</li><li><code>session-signal-ledger</code>：延迟持久化失败记 warn。</li></ul><p>排查时的两个抓手：一是返回文案已经区分了四种状态效果，可以直接从工具结果判断信号落在哪一类；二是没有活跃函数时返回的 <code>(no active functions)</code> 明确说明这次信号只进了会话级台账——如果此时期望的是函数状态变化，问题在「函数没有被激活」，而不在信号。</p><h2 id="实现模块" tabindex="-1">实现模块 <a class="header-anchor" href="#实现模块" aria-label="Permalink to &quot;实现模块&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th>职责</th></tr></thead><tbody><tr><td><code>src/signal/signal-constants.ts</code></td><td>8 个类型的词表、四个类别集合与严重度顺序</td></tr><tr><td><code>src/signal/signal-tool.ts</code></td><td><code>signal</code> 工具：会话/函数两级记录、暂停处理、制品捕获、返回文案</td></tr><tr><td><code>src/signal/signal-ledger.ts</code></td><td>函数级台账的 6 个读写辅助函数</td></tr><tr><td><code>src/signal/session-signal-ledger.ts</code></td><td><code>SessionSignalLedger</code> 单例：按会话键控、查询与延迟持久化</td></tr><tr><td><code>src/function/conditions.ts</code></td><td><code>signal_observed</code> 条件的三步求值</td></tr><tr><td><code>src/function/runtime-state.ts</code></td><td><code>FnState.kv</code>、<code>phase</code>、<code>blockedAt</code> 与 <code>blockedTimeoutMs</code> 的字段定义</td></tr><tr><td><code>src/copilot/sources/builtin.ts</code></td><td>gated 函数的有界兜底（超时强制解封）</td></tr><tr><td><code>src/function/artifact-store.ts</code></td><td><code>capture_payload_as</code> 的写入目标</td></tr><tr><td><code>src/graph/engine/signal-bridge.ts</code></td><td>图引擎侧的读取接缝：把信号记进节点台账并分类</td></tr><tr><td><code>src/dispatch/completion/completion-evaluator.ts</code></td><td>无显式信号时补发合成 <code>answer</code></td></tr></tbody></table><h2 id="备注" tabindex="-1">备注 <a class="header-anchor" href="#备注" aria-label="Permalink to &quot;备注&quot;">​</a></h2><blockquote><p>自 v0.22.0 起，rolebox 提供这套带外信号词表与 <code>signal</code> 工具；自 v1.9.0 起，dsh 适配器在任务落定时读取完成台账并写入终止信号，台账损坏时回退到合成 <code>answer</code>，不再中断落定路径。</p></blockquote>`,33))])}const F=o(h,[["render",g]]);export{_ as __pageData,F as default};
