import{_ as t,o,c as d,a2 as s}from"./chunks/framework.CuUVYbLV.js";const g=JSON.parse('{"title":"贡献指南","description":"rolebox 的贡献流程、提交消息与代码风格、测试政策、CHANGELOG 约定、PR 清单、审查标准与发布流程","frontmatter":{"title":"贡献指南","description":"rolebox 的贡献流程、提交消息与代码风格、测试政策、CHANGELOG 约定、PR 清单、审查标准与发布流程"},"headers":[],"relativePath":"05-Contributing/contributing.md","filePath":"05-Contributing/contributing.md"}'),i={name:"05-Contributing/contributing.md"};function a(c,e,n,l,r,p){return o(),d("div",null,[...e[0]||(e[0]=[s(`<h1 id="贡献指南-contributing" tabindex="-1">贡献指南（Contributing） <a class="header-anchor" href="#贡献指南-contributing" aria-label="Permalink to &quot;贡献指南（Contributing）&quot;">​</a></h1><p>本页回答「给 rolebox 提一个改动，要遵守什么」：从分支、提交消息、代码风格、测试政策，到 PR 清单与发布流程。仓库根的 <code>CONTRIBUTING.md</code> 给出对外的最小流程，<code>AGENTS.md</code> 给出面向编码代理与贡献者的详细约定——本页把两者的规则整理成可执行清单；构建与测试的<strong>具体命令</strong>在<a href="/rolebox-docs/05-Contributing/development-setup.html">开发环境搭建</a>。</p><blockquote><p>相关：<a href="/rolebox-docs/05-Contributing/development-setup.html">开发环境搭建</a>｜<a href="/rolebox-docs/01-Overview/architecture-overview.html">架构概览</a>｜<a href="/rolebox-docs/03-Reference/cli.html">CLI 参考</a></p></blockquote><h2 id="贡献流程" tabindex="-1">贡献流程 <a class="header-anchor" href="#贡献流程" aria-label="Permalink to &quot;贡献流程&quot;">​</a></h2><ol><li><strong>Fork 并从 <code>main</code> 建分支</strong> —— 不要直接在 <code>main</code> 上工作。</li><li><strong>保持改动聚焦</strong> —— 一个 PR 只做一件事，不做无关的重命名、重排版或清理。</li><li><strong>按需补测试</strong> —— 新功能或修复带上对应的单元 / 集成测试。</li><li><strong>跑模块切片</strong> —— 只跑改动模块对应的测试目录，不要本地跑全量（原因见下方<a href="#测试规范">测试规范</a>）。</li><li><strong>向 <code>main</code> 提 Pull Request</strong> —— 说明它做了什么、为什么这么做。</li></ol><p>仓库根还有两份必读的贡献者文档：</p><table tabindex="0"><thead><tr><th>文件</th><th>内容</th></tr></thead><tbody><tr><td><code>CONTRIBUTING.md</code></td><td>对外的最小流程：issue、PR、角色创建与 YAML 风格</td></tr><tr><td><code>AGENTS.md</code></td><td>详细约定：关键脚本表、隐私与泄漏卫生、测试政策、模块 → 测试路径映射、测试编写约定</td></tr></tbody></table><h2 id="提交消息规范" tabindex="-1">提交消息规范 <a class="header-anchor" href="#提交消息规范" aria-label="Permalink to &quot;提交消息规范&quot;">​</a></h2><p>提交消息采用 <code>&lt;type&gt;(&lt;scope&gt;): &lt;描述&gt;</code> 形式。仓库历史的真实样例：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fix(ci): point order-independence guard at existing test files</span></span>
<span class="line"><span>feat(dsh-web-ui): redesign role dock and monitor</span></span>
<span class="line"><span>docs(readme): refocus and move detail to docs</span></span>
<span class="line"><span>chore(release): v1.9.0</span></span></code></pre></div><table tabindex="0"><thead><tr><th>类型</th><th>用途</th></tr></thead><tbody><tr><td><code>feat</code></td><td>新功能</td></tr><tr><td><code>fix</code></td><td>错误修复</td></tr><tr><td><code>docs</code></td><td>文档更新</td></tr><tr><td><code>chore</code></td><td>构建、CI、依赖、发布等杂项</td></tr></tbody></table><p><code>scope</code> 取改动所在的模块或关注点（<code>ci</code>、<code>dsh</code>、<code>dsh-web-ui</code>、<code>readme</code>、<code>agents</code> 等都是实际出现过的取值）。仓库没有 commitlint 之类的强制配置，格式靠约定与审查维持。</p><h2 id="代码风格" tabindex="-1">代码风格 <a class="header-anchor" href="#代码风格" aria-label="Permalink to &quot;代码风格&quot;">​</a></h2><ul><li><strong>语言</strong>：TypeScript（严格模式），编译配置见下表</li><li><strong>缩进</strong>：2 空格</li><li><strong>命名</strong>：camelCase（变量 / 函数）、PascalCase（类型 / 类）、kebab-case（文件名）</li><li><strong>避免</strong>：新增 <code>any</code> / <code>as any</code>、<code>@ts-ignore</code>、没有理由的抽象</li></ul><h3 id="typescript-配置" tabindex="-1">TypeScript 配置 <a class="header-anchor" href="#typescript-配置" aria-label="Permalink to &quot;TypeScript 配置&quot;">​</a></h3><p><code>tsconfig.json</code> 为整个仓库定下严格的编译环境（TUI 由 <code>tsconfig.tui.json</code> 单独处理）：</p><table tabindex="0"><thead><tr><th>选项</th><th>值</th><th>说明</th></tr></thead><tbody><tr><td><code>target</code></td><td><code>ES2022</code></td><td>编译目标</td></tr><tr><td><code>module</code> / <code>moduleResolution</code></td><td><code>NodeNext</code></td><td>ESM（ES 模块 / ECMAScript Modules）解析</td></tr><tr><td><code>rewriteRelativeImportExtensions</code></td><td><code>true</code></td><td>编译时把相对导入里的 <code>.ts</code> 改写成 <code>.js</code></td></tr><tr><td><code>outDir</code></td><td><code>dist</code></td><td>编译输出目录</td></tr><tr><td><code>declaration</code> / <code>declarationMap</code></td><td><code>true</code></td><td>生成 <code>.d.ts</code> 与声明源映射</td></tr><tr><td><code>sourceMap</code></td><td><code>true</code></td><td>源码映射（配合 Bun 运行时调试）</td></tr><tr><td><code>strict</code></td><td><code>true</code></td><td>全部严格检查（<code>strictNullChecks</code>、<code>noImplicitAny</code> 等）</td></tr><tr><td><code>esModuleInterop</code></td><td><code>true</code></td><td>CommonJS 互操作</td></tr><tr><td><code>skipLibCheck</code></td><td><code>true</code></td><td>跳过 <code>node_modules</code> 的类型检查</td></tr><tr><td><code>forceConsistentCasingInFileNames</code></td><td><code>true</code></td><td>文件名大小写必须一致</td></tr><tr><td><code>types</code></td><td><code>[&quot;bun-types&quot;]</code></td><td>使用 Bun 的类型定义</td></tr><tr><td><code>jsx</code></td><td><code>preserve</code></td><td>交由 TUI 构建脚本处理 JSX</td></tr><tr><td><code>include</code> / <code>exclude</code></td><td><code>[&quot;src&quot;]</code> / <code>[&quot;src/tui&quot;]</code></td><td>根编译排除 TUI，TUI 单独编译</td></tr></tbody></table><h3 id="命名规范" tabindex="-1">命名规范 <a class="header-anchor" href="#命名规范" aria-label="Permalink to &quot;命名规范&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类别</th><th>规范</th><th>现有例子</th></tr></thead><tbody><tr><td>变量 / 函数</td><td>camelCase</td><td><code>createPluginHooks</code>、<code>resolveRoleboxDirectories</code></td></tr><tr><td>类型 / 接口 / 类</td><td>PascalCase</td><td><code>PluginCore</code>、<code>ServiceSupervisor</code></td></tr><tr><td>文件名 / 目录名</td><td>kebab-case</td><td><code>chat-message.ts</code>、<code>service-supervisor.ts</code>、<code>tool-registry.ts</code></td></tr><tr><td>常量</td><td>UPPER_SNAKE_CASE</td><td><code>SUPERVISOR_DEFAULTS</code>、<code>DEFAULT_MAX_FILE_BYTES</code></td></tr><tr><td>错误类</td><td>PascalCase + <code>Error</code> 后缀</td><td><code>DescriptiveCycleError</code></td></tr></tbody></table><h3 id="导入规范" tabindex="-1">导入规范 <a class="header-anchor" href="#导入规范" aria-label="Permalink to &quot;导入规范&quot;">​</a></h3><ul><li>使用 ESM <code>import</code> / <code>export</code>，遵循 <code>NodeNext</code> 解析。</li><li>相对导入写 <strong><code>.ts</code> 扩展名</strong>（如 <code>from &quot;./platform/factory.ts&quot;</code>）；<code>rewriteRelativeImportExtensions</code> 在编译时把它们改写成 <code>.js</code>。仓库里仍有少量历史文件直接写 <code>.js</code>，新代码统一写 <code>.ts</code>。</li><li>优先具名导入，避免 <code>import *</code> 通配导入。</li><li>类型导入使用 <code>import type { ... }</code>。</li></ul><h3 id="类型注释" tabindex="-1">类型注释 <a class="header-anchor" href="#类型注释" aria-label="Permalink to &quot;类型注释&quot;">​</a></h3><p>公共 API（应用程序接口，Application Programming Interface）需要显式类型注释，内部实现可以依赖类型推断。工具定义用 <code>zod</code> schema 同时承担运行时校验与类型推导——见<a href="/rolebox-docs/03-Reference/plugin-interface.html">插件接口</a>里的 <code>defineTool()</code>，不需要再手写一份参数类型。</p><h3 id="错误处理" tabindex="-1">错误处理 <a class="header-anchor" href="#错误处理" aria-label="Permalink to &quot;错误处理&quot;">​</a></h3><ul><li>使用具名错误类型而不是裸 <code>new Error(...)</code>，便于调用方区分失败原因——服务依赖成环时抛出的 <code>DescriptiveCycleError</code> 就带着 <code>cycleMembers</code> 字段。</li><li>不要在 <code>catch</code> 块里静默吞掉错误：可选服务初始化失败时，<code>PluginCore</code> 会记日志并把该服务标记为<strong>永久降级</strong>，而不是假装成功。</li><li>工具函数应返回有意义的错误消息。</li></ul><h2 id="测试规范" tabindex="-1">测试规范 <a class="header-anchor" href="#测试规范" aria-label="Permalink to &quot;测试规范&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title">不要本地运行全量测试套件</p><p><code>AGENTS.md</code> 明确禁止在本地运行完整套件（<code>bun test</code>）：它很慢，会用无关输出淹没上下文，那是 CI 的职责。本地只运行与改动模块对应的<strong>切片</strong>。</p></div><div class="warning custom-block"><p class="custom-block-title">两份文档的测试指令不一致</p><p><code>CONTRIBUTING.md</code> 的流程示例让贡献者运行 <code>bun test</code>（全量），而 <code>AGENTS.md</code> 禁止本地跑全量。<strong>以 <code>AGENTS.md</code> 为准</strong>：全量套件留给 CI 与维护者明确要求的预发布验证。</p></div><table tabindex="0"><thead><tr><th>命令</th><th>用途</th></tr></thead><tbody><tr><td><code>bun run typecheck</code></td><td>只做类型检查，不跑测试（最快的门禁）</td></tr><tr><td><code>bun test --isolate tests/&lt;module&gt;/</code></td><td>模块切片</td></tr><tr><td><code>bun test --isolate tests/&lt;module&gt;/&lt;file&gt;.test.ts</code></td><td>单文件（最快的反馈回路）</td></tr><tr><td><code>bun test tests/tui/</code></td><td>TUI 切片，<strong>不加</strong> <code>--isolate</code></td></tr><tr><td><code>bun run test</code></td><td>全量（<code>test:core</code> + <code>test:tui</code>），仅 CI / 预发布</td></tr></tbody></table><p>core 与 TUI 的 <code>--isolate</code> 取舍：<code>@opentui/core</code> 在导入期执行顶层 <code>await</code>，在 <code>--isolate</code> 下会失败；core 切片则必须启用它——每个测试文件拿到独立模块注册表后，跨文件的 <code>mock.module</code> 污染在结构上不可能发生。</p><h3 id="测试编写约定" tabindex="-1">测试编写约定 <a class="header-anchor" href="#测试编写约定" aria-label="Permalink to &quot;测试编写约定&quot;">​</a></h3><ul><li><strong>镜像 <code>src/</code> 路径</strong> —— <code>src/&lt;模块&gt;/&lt;文件&gt;.ts</code> 的测试放在 <code>tests/&lt;模块&gt;/&lt;文件&gt;.test.ts</code>。</li><li><strong>文件命名</strong> <code>&lt;subject&gt;.test.ts</code>。</li><li><strong>保持工作树干净</strong> —— CI 在最后一步断言 <code>git status --porcelain --untracked-files=all</code> 为空，测试不得留下临时文件或 <code>cache/</code> 之类的残留目录。</li><li><strong>Unix-only 测试加守卫</strong> —— 用来自 <code>tests/helpers/tar.ts</code> 的 <code>it.skipIf(!hasTar())</code>。</li><li><strong>模块 mock 用 <code>mock.module()</code></strong> 在文件顶部设置；跨测试共享的夹具放 <code>tests/</code> 对应子目录，轻量夹具优先内联创建，而不是落成大型 JSON fixture。</li></ul><p>测试目录与 <code>src/</code> 的完整对应表、每条命令的预期输出，见<a href="/rolebox-docs/05-Contributing/development-setup.html">开发环境搭建</a>。</p><h2 id="changelog-格式" tabindex="-1">CHANGELOG 格式 <a class="header-anchor" href="#changelog-格式" aria-label="Permalink to &quot;CHANGELOG 格式&quot;">​</a></h2><p>每次发布前更新 <code>CHANGELOG.md</code>。结构是<strong>按版本分组、按类型分类</strong>：</p><table tabindex="0"><thead><tr><th>层级</th><th>形式</th><th>说明</th></tr></thead><tbody><tr><td>版本标题</td><td><code>## &lt;version&gt;</code></td><td>例如 <code>## 1.9.0</code>；发布流水线要求它存在</td></tr><tr><td>分类标题</td><td><code>### &lt;Category&gt;</code></td><td>见下</td></tr><tr><td>条目</td><td><code>- **模块名** — 描述</code></td><td>模块名加粗，描述用自然语言</td></tr></tbody></table><p><code>CHANGELOG.md</code> 实际用过的分类：<code>Features</code>、<code>Bug Fixes</code>、<code>Refactors</code>、<code>Documentation</code>、<code>Tests</code>、<code>Breaking Changes</code>、<code>Performance</code>。一个版本通常只用到其中若干类。</p><p>关键原则：</p><ul><li><strong>模块名粗体</strong> —— 条目以 <code>**模块名**</code> 开头，帮助读者快速定位变更范围；</li><li><strong>描述以动词开头</strong> —— 如 “Add”“Fix”“Remove”“Replace”；</li><li><strong>一行一条</strong> —— 每个变更独立成行，不合并为段落；</li><li><strong>面向使用者</strong> —— 描述库消费者能看到的行为，而不是内部实现细节。</li></ul><h2 id="代码组织" tabindex="-1">代码组织 <a class="header-anchor" href="#代码组织" aria-label="Permalink to &quot;代码组织&quot;">​</a></h2><p>源码按功能域组织在 <code>src/</code> 下：26 个一级目录，加上 <code>index.ts</code>（opencode 入口）、<code>pi-extension.ts</code>、<code>dsh-plugin.ts</code> 三个入口与若干顶层模块文件。按分组看：</p><table tabindex="0"><thead><tr><th>分组</th><th>目录</th><th>内容</th></tr></thead><tbody><tr><td>核心</td><td><code>src/core/</code></td><td>插件内核 <code>PluginCore</code>、服务注册与拓扑初始化、事件总线、工具注册</td></tr><tr><td>编排</td><td><code>src/graph/</code>、<code>src/dispatch/</code>、<code>src/loop/</code>、<code>src/recovery/</code></td><td>图执行引擎、调度管理器、循环协调器、恢复策略</td></tr><tr><td>角色资产</td><td><code>src/loader/</code>、<code>src/resolver/</code>、<code>src/prompt/</code>、<code>src/function/</code>、<code>src/hooks/</code>、<code>src/extensions/</code>、<code>src/asset/</code></td><td>角色发现与解析、提示词组装、函数状态机、Hook、扩展点、资产查询</td></tr><tr><td>平台</td><td><code>src/platform/</code>、<code>src/cli/</code>、<code>src/tui/</code>、<code>src/sync/</code>、<code>src/terminal/</code></td><td>三套 harness 适配、命令行、终端 UI、代理同步、交互式终端</td></tr><tr><td>能力</td><td><code>src/memory/</code>、<code>src/session/</code>、<code>src/signal/</code>、<code>src/notifications/</code>、<code>src/hashline/</code>、<code>src/web/</code>、<code>src/lsp/</code>、<code>src/copilot/</code>、<code>src/utils/</code></td><td>记忆、会话工具、信号、通知、内容哈希编辑、网页工具、LSP、copilot、通用工具</td></tr></tbody></table><p>新增功能先定位所属域目录，再到 <code>tests/</code> 的镜像位置补测试。逐模块的职责与运行时数据流见<a href="/rolebox-docs/01-Overview/architecture-overview.html">架构概览</a>；具体服务之间的依赖拓扑见<a href="/rolebox-docs/01-Overview/service-architecture.html">服务架构</a>。</p><h2 id="pr-清单" tabindex="-1">PR 清单 <a class="header-anchor" href="#pr-清单" aria-label="Permalink to &quot;PR 清单&quot;">​</a></h2><p>提交 Pull Request 前逐项检查：</p><ul><li>[ ] <strong>提交消息</strong>符合 <code>&lt;type&gt;(&lt;scope&gt;): &lt;描述&gt;</code> 形式（<code>feat</code> / <code>fix</code> / <code>docs</code> / <code>chore</code>）</li><li>[ ] <strong>类型检查通过</strong>：<code>bun run typecheck</code>（<code>tsc --noEmit</code>）零错误</li><li>[ ] <strong>模块切片测试通过</strong>：<code>bun test --isolate tests/&lt;改动的模块&gt;/</code>（不要本地跑全量套件）</li><li>[ ] <strong>新增测试覆盖</strong>：新功能或修复包含对应的单元或集成测试</li><li>[ ] <strong>无新增类型逃逸</strong>：未引入新的 <code>any</code> / <code>as any</code> / <code>@ts-ignore</code></li><li>[ ] <strong>命名一致</strong>：遵循 camelCase / PascalCase / kebab-case</li><li>[ ] <strong>错误路径已处理</strong>：异常场景有明确的错误返回，不会被静默吞掉</li><li>[ ] <strong>公共 API 已记录</strong>：新增工具或接口有对应的 JSDoc 注释</li><li>[ ] <strong>CHANGELOG 已更新</strong>：若变更影响用户可见行为，在 <code>CHANGELOG.md</code> 对应版本下添加条目</li><li>[ ] <strong>工作树干净</strong>：未遗留临时文件、测试日志或 mock 残留（CI 会断言）</li><li>[ ] <strong>隐私检查</strong>：改动不包含私有基础设施标识、密钥或个人信息</li></ul><div class="tip custom-block"><p class="custom-block-title">提交前至少跑一次 <code>bun run typecheck</code></p><p>它比测试更快，适合在开发循环里频繁执行。</p></div><h2 id="隐私与泄漏卫生" tabindex="-1">隐私与泄漏卫生 <a class="header-anchor" href="#隐私与泄漏卫生" aria-label="Permalink to &quot;隐私与泄漏卫生&quot;">​</a></h2><p>任何离开会话的输出都可能公开：git 历史、提交消息、CHANGELOG、发布说明、workflow 文件、发布包内容、CI 日志。写出这类内容前先确认它不暴露：</p><ul><li><strong>私有厂商与基础设施</strong> —— 内部主机名、服务名、账号或 provider key、模型代号，以及任何尚未公开的标识；</li><li><strong>密钥与凭据</strong> —— token、API key、密码、私钥、<code>.npmrc</code> 与环境变量值、CI secret；</li><li><strong>可定位到个人的信息</strong> —— 用户名、家目录路径、邮箱、机器专属路径。</li></ul><p>规则：示例一律用中性占位符（<code>example-provider/model-name</code>、<code>/path/to/project</code>、<code>user@example.com</code>）；不在提交消息或发布说明里宣布「清理了某某数据」，保持措辞通用；发布前扫描真正离开的产物（例如 <code>npm pack</code> 的输出），并让扫描规则本身不进版本控制。</p><h2 id="pr-模板" tabindex="-1">PR 模板 <a class="header-anchor" href="#pr-模板" aria-label="Permalink to &quot;PR 模板&quot;">​</a></h2><p>提交 PR 时建议包含以下信息：</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 变更类型</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] feat: 新功能</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] fix: 错误修复</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] docs: 文档更新</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] chore: 构建 / CI / 依赖</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 描述</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;简要描述变更内容和动机&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 测试</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] 新增单元测试覆盖</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] 受影响模块的测试切片通过</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ ] bun run typecheck 通过</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 关联 Issue</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Fixes #&lt;issue-number&gt;</span></span></code></pre></div><h2 id="pr-审查标准" tabindex="-1">PR 审查标准 <a class="header-anchor" href="#pr-审查标准" aria-label="Permalink to &quot;PR 审查标准&quot;">​</a></h2><p>PR 会被审查以下方面：</p><ol><li><strong>正确性</strong>：代码是否按预期工作？</li><li><strong>测试覆盖</strong>：是否包含合适的测试，并且只跑必要的切片？</li><li><strong>类型安全</strong>：是否避免了新增的 <code>any</code> 与类型断言？</li><li><strong>错误处理</strong>：错误路径是否被妥善处理、有可观测的失败信号？</li><li><strong>跨 harness 一致性</strong>：改动是否同时适用于 opencode、pi 与 dsh，或明确说明了平台差异？</li><li><strong>文档</strong>：公共 API 与用户可见行为是否已记录？</li></ol><h2 id="发布流程" tabindex="-1">发布流程 <a class="header-anchor" href="#发布流程" aria-label="Permalink to &quot;发布流程&quot;">​</a></h2><p>发布由 tag 驱动，不要手动执行发布命令：</p><ol><li><p><strong>更新 <code>package.json</code> 的 <code>version</code></strong>。</p></li><li><p><strong>写入 <code>CHANGELOG.md</code> 段</strong> —— 必须存在 <code>## &lt;version&gt;</code> 标题，否则流水线在元数据校验一步就失败。</p></li><li><p><strong>打 tag 并推送</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tag</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vX.Y.Z</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;vX.Y.Z&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">prepare-commi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vX.Y.Z</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>应看到：</span></span>
<span class="line"><span>To github.com:EricMoin/rolebox.git</span></span>
<span class="line"><span> * [new tag]         vX.Y.Z -&gt; vX.Y.Z</span></span>
<span class="line"><span>（示例输出，随远端配置与是否已存在同名 tag 略有差异）</span></span></code></pre></div></li><li><p><strong><code>publish.yml</code> 自动执行</strong>：</p><ul><li>校验 tag 与 <code>package.json</code>、<code>CHANGELOG.md</code> 三者版本一致；</li><li><code>bun run build</code> —— 必须是完整构建，因为 <code>dist/tui.js</code> 与 <code>dist/dsh-web-client.js</code> 是 <code>tsc</code> 不产出的 bundle；</li><li><code>bun run test</code> —— 全量套件，CI 正是为它准备的运行位置；</li><li><code>npm pack --dry-run</code> 断言关键文件都在包里；</li><li><code>npm publish</code>；</li><li>从 <code>CHANGELOG.md</code> 抽取该版本段作为 GitHub Release 说明。</li></ul></li></ol><p><code>package.json</code> 的 <code>prepublishOnly</code> 钩子是 <code>tsc</code>，作为额外的类型门禁。</p><h2 id="文档约定" tabindex="-1">文档约定 <a class="header-anchor" href="#文档约定" aria-label="Permalink to &quot;文档约定&quot;">​</a></h2><p>站点在<strong>独立的 rolebox-docs 仓库</strong>维护，与源码仓库分开。改文档前先读该仓库根的 <code>HOUSE-STYLE.md</code>——它是全站写作契约（不进站点，VitePress 只发布 <code>docs/</code>）：</p><ul><li><strong>出处只有一个去处</strong>：<a href="/rolebox-docs/06-Appendix/source-index.html">/06-Appendix/source-index</a> 的「概念 → 模块 → 该模块负责什么」对照表。用户可见页面（教程 / 指南 / 参考 / 首页 / 术语表）不写 <code>src/</code> 路径，也不写「路径 + 行号」这类行号锚点。</li><li><strong>模块路径只出现在三类页面</strong>：内部实现、贡献、源码索引；且只在模块本身是句子主语时出现，不作为证据附件。</li><li><strong>版本信息只有两种落点</strong>：站点级一处（页脚版本说明），页面级最多一条 <code>&gt; 自 vX.Y.Z 起，…</code>，且该版本必须真实存在于 <code>CHANGELOG.md</code>。</li><li><strong>术语在首次出现处就地定义</strong>：<code>中文（English，一句话解释）</code>，不写前置术语表。</li><li><strong>结构变更要同步导航</strong>：新增或移动页面时同步改 <code>docs/.vitepress/config.ts</code> 的 sidebar 与 nav，保证 <code>docs/</code> 下每个页面都能从侧边栏到达。</li></ul><p>常用命令（在 rolebox-docs 仓库根执行）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # 本地预览</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 生产构建；部署流水线跑的就是它</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>应看到：</span></span>
<span class="line"><span>  vitepress v1.6.x  ready in … ms</span></span>
<span class="line"><span>  ➜  Local:   http://localhost:5173/</span></span>
<span class="line"><span>docs:build 无报错退出，产物写入 docs/.vitepress/dist</span></span>
<span class="line"><span>（示例输出，随环境略有差异）</span></span></code></pre></div><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li><a href="/rolebox-docs/05-Contributing/development-setup.html">开发环境搭建</a> — 前提条件、克隆、构建、测试与调试</li><li><a href="/rolebox-docs/03-Reference/plugin-interface.html">插件接口</a> — 服务与平台适配器的接口契约</li><li><a href="/rolebox-docs/01-Overview/architecture-overview.html">架构概览</a> — 模块地图与运行时数据流</li></ul>`,69)])])}const u=t(i,[["render",a]]);export{g as __pageData,u as default};
