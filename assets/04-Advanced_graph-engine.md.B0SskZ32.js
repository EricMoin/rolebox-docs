import{_ as t,o as d,c as s,a2 as i}from"./chunks/framework.CuUVYbLV.js";const k=JSON.parse('{"title":"图执行引擎","description":"图引擎的模型层——图由哪些声明构成、引擎由哪些对象承载、各子系统分别负责什么。","frontmatter":{"title":"图执行引擎","description":"图引擎的模型层——图由哪些声明构成、引擎由哪些对象承载、各子系统分别负责什么。"},"headers":[],"relativePath":"04-Advanced/graph-engine.md","filePath":"04-Advanced/graph-engine.md"}'),a={name:"04-Advanced/graph-engine.md"};function o(n,e,c,r,h,l){return d(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="图执行引擎-graph-engine" tabindex="-1">图执行引擎（Graph Engine） <a class="header-anchor" href="#图执行引擎-graph-engine" aria-label="Permalink to &quot;图执行引擎（Graph Engine）&quot;">​</a></h1><p>rolebox 把「谁把工作交给谁」这个问题建模成一张有向图来执行：**节点（node，一次派发给某个代理的工作，内容是一个 <code>{agent, prompt}</code> 元组）**是工作单元，**边（edge，声明 <code>from → to</code> 流转关系的连接）**是工作之间的流向。本页讲这套模型的静态结构——图由哪些声明组成、引擎由哪些对象承载、每个子系统负责什么。</p><p>同一次执行按时间展开的时序（节点状态机、推进临界区、崩溃恢复的每一步）在<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>；把图搭起来并跑起来的操作步骤在<a href="/rolebox-docs/02-Guide/graph-workflows.html">图工作流</a>；<code>graph_*</code> 每个工具的参数表在<a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a>。这三处的内容本页都不重复。</p><blockquote><p>前置：<a href="/rolebox-docs/04-Advanced/graph-declaration.html">图声明</a>｜相关：<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>、<a href="/rolebox-docs/04-Advanced/signal-system.html">信号系统</a></p></blockquote><h2 id="_1-图的模型" tabindex="-1">1. 图的模型 <a class="header-anchor" href="#_1-图的模型" aria-label="Permalink to &quot;1. 图的模型&quot;">​</a></h2><p>引擎执行的对象是一份<strong>图声明（graph declaration，schema 版本固定为 <code>2</code>）</strong>。它由四类结构组成；先看一个最小形状，再看每类结构各自承载什么（完整字段规范见<a href="/rolebox-docs/04-Advanced/graph-declaration.html">图声明</a>）：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;review-pipeline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;nodes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;coder&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;coder&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;prompt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;实现需求&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reviewer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reviewer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;prompt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;评审实现&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;needs_approval&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;join&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;strategy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;all&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;budget&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;max_retries&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;writer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;doc-writer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;prompt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;定稿&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;edges&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;from&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;coder&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;to&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reviewer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;always&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;from&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reviewer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;to&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;writer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;on_signal&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;signal_filter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;answer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;from&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;writer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;to&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reviewer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;on_signal&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;signal_filter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;revise_needed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;retry&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;max&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;backoff_ms&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;loop_groups&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;revise&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;nodes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reviewer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;writer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;max_traversals&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;budget&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;max_total_cost_usd&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>结构</th><th>必需字段</th><th>可选字段</th><th>在模型里的角色</th></tr></thead><tbody><tr><td>节点 node</td><td><code>id</code> / <code>agent</code> / <code>prompt</code></td><td><code>completion_condition</code> / <code>needs_approval</code> / <code>join</code> / <code>budget</code></td><td>工作单元，角色无关</td></tr><tr><td>边 edge</td><td><code>from</code> / <code>to</code> / <code>type</code></td><td><code>signal_filter</code> / <code>condition</code> / <code>data_passthrough</code> / <code>retry</code></td><td>数据流与信号路由</td></tr><tr><td>循环组 loop group</td><td><code>id</code> / <code>nodes</code> / <code>max_traversals</code></td><td><code>mode</code></td><td>有界循环的边界</td></tr><tr><td>预算 budget</td><td>——（整块可选）</td><td>图级 <code>max_total_*</code>、节点级 <code>max_*</code></td><td>资源上限</td></tr></tbody></table><h3 id="_1-1-节点-角色无关的-agent-prompt-元组" tabindex="-1">1.1 节点：角色无关的 <code>{agent, prompt}</code> 元组 <a class="header-anchor" href="#_1-1-节点-角色无关的-agent-prompt-元组" aria-label="Permalink to &quot;1.1 节点：角色无关的 \`{agent, prompt}\` 元组&quot;">​</a></h3><p>节点类型 <code>NodeConfig</code> 只有三个必需字段：<code>id</code>（图内唯一标识）、<code>agent</code>（承接该节点的代理名）、<code>prompt</code>（交给它的任务指令）。节点<strong>没有</strong>类型字段，也没有角色分类——语义完全来自编排者给它分配的 <code>agent</code> 与 <code>prompt</code>，因此同一个节点可以换成任何可派发的角色。</p><p><code>needs_approval</code> 是一个<strong>暂停标志</strong>而不是节点类别：置 <code>true</code> 时引擎在该节点执行完成后停下来等人工批准，而不是把它当成另一种节点。</p><h3 id="_1-2-边-数据流与信号路由" tabindex="-1">1.2 边：数据流与信号路由 <a class="header-anchor" href="#_1-2-边-数据流与信号路由" aria-label="Permalink to &quot;1.2 边：数据流与信号路由&quot;">​</a></h3><p>边类型 <code>EdgeDeclaration</code> 声明 <code>from</code> 与 <code>to</code>，以及激活语义 <code>type</code>：</p><ul><li><code>always</code> —— 源节点一有结果就激活。</li><li><code>on_signal</code> —— 只有源节点发出 <code>signal_filter</code> 列出的信号时才激活。</li><li><code>on_condition</code> —— 命名条件 <code>condition</code> 求值为真时激活（条件词表与解析器见<a href="/rolebox-docs/04-Advanced/graph-declaration.html">图声明</a>）。</li></ul><p>边还承载两项行为：<code>data_passthrough</code>（规定上游结果的哪些字段传给下游）与 <code>retry</code>。<code>retry</code> 是<strong>边属性</strong>而非节点属性：它同时覆盖该边两端的节点，节点升级时的有效重试额度取「节点自身 <code>budget.max_retries</code>」与「关联边上 <code>retry.max</code>」的<strong>最大值</strong>，<code>backoff_ms</code> 则决定两次尝试之间的退避窗口（重试闸门的运行时细节见<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>）。</p><h3 id="_1-3-循环组-有界循环" tabindex="-1">1.3 循环组：有界循环 <a class="header-anchor" href="#_1-3-循环组-有界循环" aria-label="Permalink to &quot;1.3 循环组：有界循环&quot;">​</a></h3><p>循环组 <code>LoopGroupDecl</code> 用 <code>id</code>、<code>nodes</code>（成员节点）、<code>max_traversals</code>（遍历次数<strong>硬上限</strong>）把一组节点圈成一个有界循环。<code>mode</code> 记录轮次之间的会话隔离方式：<code>inherit</code>（默认且唯一真正支持的取值）表示各轮在同一个引擎状态内重新派发；<code>fresh</code> 明确不支持，请求它会返回错误并指向替代做法（每轮开一张新图）。</p><h3 id="_1-4-预算-图级与节点级上限" tabindex="-1">1.4 预算：图级与节点级上限 <a class="header-anchor" href="#_1-4-预算-图级与节点级上限" aria-label="Permalink to &quot;1.4 预算：图级与节点级上限&quot;">​</a></h3><ul><li>图级 <code>GraphBudgetSpec</code>：<code>max_total_input_tokens</code> / <code>max_total_output_tokens</code> / <code>max_total_cost_usd</code>，对全图所有节点<strong>累计</strong>生效。</li><li>节点级 <code>NodeBudgetSpec</code>：<code>max_input_tokens</code> / <code>max_output_tokens</code> / <code>max_cost_usd</code> / <code>timeout_ms</code> / <code>max_retries</code>，映射到调度层对单个会话的限制。</li></ul><p>编排者可以把节点预算<strong>超订</strong>（各节点之和大于图预算），但实际消耗始终以图预算为上限。检查时机与越界后的行为属于运行时，见<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>。</p><h3 id="_1-5-拓扑模板-声明元数据-不是运行时结构" tabindex="-1">1.5 拓扑模板：声明元数据，不是运行时结构 <a class="header-anchor" href="#_1-5-拓扑模板-声明元数据-不是运行时结构" aria-label="Permalink to &quot;1.5 拓扑模板：声明元数据，不是运行时结构&quot;">​</a></h3><p>图声明可以带一个 <code>template</code> 字段（<code>pipeline</code> / <code>review-loop</code> / <code>star</code>），它是<strong>声明元数据</strong>：<code>src/graph/templates.ts</code> 负责把模板展开成一组边，也可以注册自定义拓扑；引擎本身只认边集，不读这个字段。展开时代表编排者（父角色）的节点使用保留名 <code>parent</code>（<code>PARENT_NODE</code>，定义在 <code>src/constants.ts</code>）。三种内建拓扑各自适合什么场景，见<a href="/rolebox-docs/04-Advanced/workflow-patterns.html">工作流模式</a>。</p><h2 id="_2-引擎的对象模型" tabindex="-1">2. 引擎的对象模型 <a class="header-anchor" href="#_2-引擎的对象模型" aria-label="Permalink to &quot;2. 引擎的对象模型&quot;">​</a></h2><p>一次图执行由「一个运行时对象 + 一个状态对象」承载：运行时是执行者，状态是它持有的全部数据。<code>src/graph/engine/index.ts</code> 定义运行时接口与构造入口，<code>src/types.engine-v2.ts</code> 定义状态的形状，<code>src/graph/engine/engine-state.ts</code> 提供读写状态的原子原语。</p><h3 id="_2-1-engineruntime-一次执行的载体" tabindex="-1">2.1 EngineRuntime：一次执行的载体 <a class="header-anchor" href="#_2-1-engineruntime-一次执行的载体" aria-label="Permalink to &quot;2.1 EngineRuntime：一次执行的载体&quot;">​</a></h3><p>每个图实例对应一个 <code>EngineRuntime</code>，由 <code>createEngine(graphDeclaration, options)</code> 构造；它拥有一个 <code>EngineState</code> 和一个信号驱动的推进器。构造本身不派发任何东西，典型调用顺序是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>const engine = createEngine(declaration, { manager });  // 构造：持有声明与状态</span></span>
<span class="line"><span>engine.provision();                                     // 注册节点、引导拓扑，根节点就绪</span></span>
<span class="line"><span>await engine.run();                                     // idle → executing，派发根节点</span></span>
<span class="line"><span>engine.status();                                        // 只读快照（集合为克隆副本）</span></span>
<span class="line"><span>engine.dispose();                                       // 释放；幂等</span></span></code></pre></div><p>构造选项里最关键的是<strong>派发缝</strong>：<code>options.dispatch</code>（直接注入派发端口）或 <code>options.manager</code>（由调度管理器生成默认的派发与预算缝）。没有派发缝时引擎仍可构造、<code>status()</code> 与 <code>provision()</code> 仍可用，但 <code>run()</code> 会以明确的错误拒绝。</p><p>公开方法：</p><table tabindex="0"><thead><tr><th>方法</th><th>职责</th></tr></thead><tbody><tr><td><code>provision()</code></td><td>注册全部声明节点、引导拓扑（无入边的根节点进入 <code>ready</code> 并加入前沿）；幂等</td></tr><tr><td><code>run()</code></td><td>从 <code>idle</code> 过渡到 <code>executing</code> 并派发就绪根节点；未 <code>provision()</code> 时先自动补齐</td></tr><tr><td><code>recover()</code></td><td>崩溃恢复：加载持久化状态、把 <code>running</code> 节点与调度系统对账、重建前沿、排空积压的完成事件</td></tr><tr><td><code>adoptPrior(prior, opts)</code></td><td>把先前一次运行的逐节点进度采纳进本次新建的运行时，绝不重复派发已推进的节点</td></tr><tr><td><code>status()</code></td><td>返回 <code>EngineState</code> 的只读快照（集合为克隆副本）</td></tr><tr><td><code>cancel()</code></td><td>整图取消：活跃节点 → <code>cancelled</code>，阶段推进到 <code>complete</code>；<code>blocked</code> 节点留给人工</td></tr><tr><td><code>approveNode(nodeId, payload?)</code></td><td>批准阻塞节点：<code>blocked → completed</code>，记录 <code>answer</code> 信号并激活前向 <code>answer</code> 边</td></tr><tr><td><code>rejectNode(nodeId, reason?)</code></td><td>拒绝阻塞节点：<code>blocked → ready</code>（携带反馈重入），或在无循环组可重开时 <code>blocked → escalate</code></td></tr><tr><td><code>partialApprove(nodeId, approved, rejected, reason?)</code></td><td>部分批准：接受 <code>approved</code> 分支、取消 <code>rejected</code> 分支中无法存活的传递依赖，被拒上游重入 <code>ready</code></td></tr><tr><td><code>retryNode(nodeId, opts?)</code></td><td>重试终态节点：把目标及其下游子图重置为干净的 <code>pending</code>，可前置 <code>modifyPrompt</code>，再重新派发</td></tr><tr><td><code>cancelNodes(nodeIds, options?)</code></td><td>局部取消（<code>cascade</code> 时级联到传递下游），循环组目标展开为其全部成员；不触碰整图阶段</td></tr><tr><td><code>dispose()</code></td><td>释放运行时：取消防抖写盘，使陈旧状态永不覆盖后继运行时；幂等</td></tr></tbody></table><p>这些方法的<strong>调用时机</strong>与副作用顺序是运行时行为，见<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>。</p><h3 id="_2-2-enginestate-一次执行的全部数据" tabindex="-1">2.2 EngineState：一次执行的全部数据 <a class="header-anchor" href="#_2-2-enginestate-一次执行的全部数据" aria-label="Permalink to &quot;2.2 EngineState：一次执行的全部数据&quot;">​</a></h3><p>状态对象 <code>EngineState</code> 是引擎唯一的真相来源：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>phase</code></td><td><code>EnginePhase</code></td><td>引擎生命周期阶段</td></tr><tr><td><code>graphId</code></td><td><code>string</code></td><td>该图实例的唯一标识</td></tr><tr><td><code>graphDeclaration</code></td><td><code>GraphDeclaration</code></td><td>执行所依据的图声明</td></tr><tr><td><code>nodes</code></td><td><code>Map&lt;string, NodeRuntimeState&gt;</code></td><td>逐节点运行状态，按节点 ID 索引</td></tr><tr><td><code>loopGroups</code></td><td><code>Map&lt;string, LoopGroupRuntimeState&gt;</code></td><td>逐循环组运行状态（遍历计数等）</td></tr><tr><td><code>frontier</code></td><td><code>string[]</code></td><td>当前可派发的就绪节点队列（<strong>前沿</strong>）</td></tr><tr><td><code>budget</code></td><td><code>GraphBudgetState</code></td><td>图级累计预算消耗</td></tr><tr><td><code>signalLedger</code></td><td><code>Map&lt;string, SignalLedgerEntry&gt;</code></td><td>逐节点信号历史</td></tr><tr><td><code>advancingLock</code></td><td><code>boolean</code></td><td>重入守卫：同一时刻只允许一个推进临界区</td></tr><tr><td><code>isDirty</code> / <code>isNonCriticalDirty</code></td><td><code>boolean</code></td><td>仅运行时的脏标记（关键 / 非关键），决定写盘策略</td></tr><tr><td><code>pendingCompletions</code></td><td><code>string[]</code></td><td>临界区内被推迟、解锁后排空的完成事件</td></tr><tr><td><code>checkpoints</code> / <code>checkpointHistory</code></td><td>可选</td><td>逐节点生命周期检查点（最新一份 / 追加式全部历史）</td></tr><tr><td><code>terminalNotified</code></td><td>可选</td><td>跨重启的终止通知去重标记</td></tr><tr><td><code>startedAt</code> / <code>updatedAt</code></td><td><code>number</code></td><td>图开始时间与最后一次状态更新的时间戳</td></tr></tbody></table><p><code>isDirty</code> 与 <code>isNonCriticalDirty</code> 是<strong>纯运行时</strong>字段：它们不会被序列化，新建或恢复得到的状态一律从干净值开始。（<code>advancingLock</code> 与 <code>pendingCompletions</code> 会随状态一起持久化；崩溃恢复时由陈旧临界区清理负责释放卡住的锁、丢弃孤儿的完成队列。）</p><h3 id="_2-3-enginephase-三个生命周期阶段" tabindex="-1">2.3 EnginePhase：三个生命周期阶段 <a class="header-anchor" href="#_2-3-enginephase-三个生命周期阶段" aria-label="Permalink to &quot;2.3 EnginePhase：三个生命周期阶段&quot;">​</a></h3><table tabindex="0"><thead><tr><th>阶段</th><th>值</th><th>说明</th></tr></thead><tbody><tr><td><code>Idle</code></td><td><code>idle</code></td><td>已构造、尚未运行</td></tr><tr><td><code>Executing</code></td><td><code>executing</code></td><td>已开始推进：就绪节点已派发，等待信号</td></tr><tr><td><code>Complete</code></td><td><code>complete</code></td><td>图执行结束（正常完成或整体取消）</td></tr></tbody></table><p>阶段的合法转换由 <code>canTransitionPhase()</code> 校验、<code>transitionPhase()</code> 执行（<code>src/graph/engine/engine-state.ts</code>）：顺序是硬约束 <code>idle → executing → complete</code>，其余转换一律被拒绝。转换发生在什么时刻，见<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>。</p><h3 id="_2-4-nodestatus-九个状态词汇" tabindex="-1">2.4 NodeStatus：九个状态词汇 <a class="header-anchor" href="#_2-4-nodestatus-九个状态词汇" aria-label="Permalink to &quot;2.4 NodeStatus：九个状态词汇&quot;">​</a></h3><table tabindex="0"><thead><tr><th>状态</th><th>值</th><th>说明</th></tr></thead><tbody><tr><td><code>Pending</code></td><td><code>pending</code></td><td>已注册、等待上游</td></tr><tr><td><code>Ready</code></td><td><code>ready</code></td><td>可派发（在前沿中）</td></tr><tr><td><code>Running</code></td><td><code>running</code></td><td>已派发，等待终止信号</td></tr><tr><td><code>Completed</code></td><td><code>completed</code></td><td>已产出结果</td></tr><tr><td><code>Blocked</code></td><td><code>blocked</code></td><td>审批暂停（<code>needs_approval</code>），等待人工</td></tr><tr><td><code>Timeout</code></td><td><code>timeout</code></td><td>超时（可由重试回到 <code>ready</code>）</td></tr><tr><td><code>Escalate</code></td><td><code>escalate</code></td><td>出错升级（最坏信号）</td></tr><tr><td><code>Cancelled</code></td><td><code>cancelled</code></td><td>已取消</td></tr><tr><td><code>Done</code></td><td><code>done</code></td><td>终态，无后继转换</td></tr></tbody></table><p>所有节点共用同一张转换表，合法性是 <code>(from, to)</code> 的纯函数，不读 <code>agent</code> / <code>prompt</code> / <code>needs_approval</code>；表本身与状态机图在<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>，实现在 <code>src/graph/engine/node-lifecycle.ts</code>。</p><h2 id="_3-join-扇入的模型" tabindex="-1">3. join：扇入的模型 <a class="header-anchor" href="#_3-join-扇入的模型" aria-label="Permalink to &quot;3. join：扇入的模型&quot;">​</a></h2><p>**扇入（fan-in，多个上游汇合到一个节点）**由节点上的 <code>join</code> 声明决定何时放行。<code>JoinConfig</code> 只有两个字段：<code>strategy</code>（<code>all</code> / <code>any</code> / <code>quorum</code>）与 <code>quorum</code>（<code>quorum</code> 策略下需要的应答数 N）；<code>resolveJoinStrategy()</code>（<code>src/graph/engine/join-evaluator.ts</code>）把它归一化为 <code>all</code> / <code>any</code> / <code>{ quorum: N }</code> 三种形态。</p><p>三种策略的模型含义：</p><ul><li><code>all</code> —— 等所有上游给出应答。</li><li><code>any</code> —— 第一个上游应答即可放行。</li><li><code>quorum:N</code> —— 累计 N 个上游应答即可放行。</li></ul><p>一次评估的结论是<strong>三态</strong> <code>JoinVerdict</code>：<code>satisfied</code>（已满足）、<code>failed</code>（已不可能满足）、<code>waiting</code>（仍未决，继续累积上游结果）。评估算法、各策略的满足 / 失败判据与循环组首轮的例外规则都在运行时页，见<a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a>。</p><h2 id="_4-驱动引擎的信号词汇" tabindex="-1">4. 驱动引擎的信号词汇 <a class="header-anchor" href="#_4-驱动引擎的信号词汇" aria-label="Permalink to &quot;4. 驱动引擎的信号词汇&quot;">​</a></h2><p>**信号（signal，节点发出的带外控制信令：不嵌入文本、独立传递）**共 8 种，由单一来源 <code>src/signal/signal-constants.ts</code> 定义，分四类：</p><table tabindex="0"><thead><tr><th>类别</th><th>信号</th><th>语义</th></tr></thead><tbody><tr><td>终止信号</td><td><code>answer</code> / <code>revise_needed</code> / <code>escalate</code></td><td>结束本节点本次运行</td></tr><tr><td>暂停信号</td><td><code>need_approval</code> / <code>blocked</code> / <code>need_clarification</code></td><td>触发暂停转换（审批门 / 阻塞 / 澄清）</td></tr><tr><td>移交信号</td><td><code>handoff</code></td><td>把工作转交他处，不终止</td></tr><tr><td>信息信号</td><td><code>progress</code></td><td>仅记录，不产生状态转换</td></tr></tbody></table><p>同一次运行记录了多个终止信号时，按严重度取最坏：<code>escalate</code> &gt; <code>revise_needed</code> &gt; <code>answer</code>。信号经 <code>src/graph/engine/signal-bridge.ts</code> 进入引擎并写入 <code>EngineState.signalLedger</code>；信号的产生方、台账 API 与 <code>signal</code> 工具见<a href="/rolebox-docs/04-Advanced/signal-system.html">信号系统</a>。</p><h2 id="_5-子系统职责地图" tabindex="-1">5. 子系统职责地图 <a class="header-anchor" href="#_5-子系统职责地图" aria-label="Permalink to &quot;5. 子系统职责地图&quot;">​</a></h2><p>引擎不是一个单体：每个关注点都有独立模块，运行时页只讲它们<strong>按什么顺序</strong>工作，这里讲它们<strong>各自拥有什么</strong>。</p><p><strong>推进。</strong> <code>src/graph/engine/engine-advance.ts</code> 是推进核心：它在临界区内转换节点生命周期、评估出边、检查下游 join 并派发就绪节点；<code>src/graph/engine/signal-bridge.ts</code> 负责把节点发出的信号接入引擎；<code>src/graph/engine/condition-resolver.ts</code> 解析 <code>on_condition</code> 边的命名条件；<code>src/graph/engine/dispatch-bridge.ts</code> 是引擎与调度系统之间的派发 / 完成 / 用量桥。</p><p><strong>收敛与循环。</strong> <code>src/graph/engine/loop-group-executor.ts</code> 是循环组成员终止信号的合并收敛决策，协调遍历计数、revise 重派发、升级级联与上游取消；<code>src/graph/engine/engine-termination.ts</code> 做引擎级终止评估；<code>src/graph/engine/node-retry.ts</code> 负责终态节点的重开与重派发。</p><p><strong>审批门。</strong> <code>src/graph/engine/approval-handler.ts</code> 提供批准、拒绝、下游裁剪与上游重入原语；<code>src/graph/engine/approval-payload.ts</code> 构造传给下游的批准载荷。</p><p><strong>预算。</strong> <code>src/graph/engine/budget-bridge.ts</code> 是 <code>BudgetTracker</code>（<code>src/dispatch/budget/budget-tracker.ts</code>）之上的只读包装，对外只暴露图级与节点级两个检查；引擎从不通过它写预算状态。</p><p><strong>取消。</strong> <code>src/graph/engine/cancellation.ts</code> 提供取消原语（整图与局部）；<code>src/graph/engine/cascade-canceller.ts</code> 提供扇入的自动回收半边——join 判定完成后回收不再可能影响结果的上游。</p><p><strong>持久化与事件日志。</strong> <code>src/graph/engine/engine-persistence.ts</code> 负责状态的序列化、两级写盘与容错加载；<code>src/graph/engine/graph-events.ts</code> 写追加式 NDJSON 事件日志；<code>src/graph/engine/engine-recovery.ts</code> 负责崩溃恢复与调度对账；<code>src/graph/engine/engine-startup.ts</code> 在插件启动时扫掠未完成的图。</p><p><strong>状态渲染与通知。</strong> <code>src/graph/engine/graph-state-block.ts</code> 渲染每轮注入系统提示的 <code>&lt;graph_state&gt;</code> 定向块；<code>src/graph/engine/graph-notify.ts</code> 构造终止通知文本，<code>[GRAPH COMPLETE]</code> / <code>[GRAPH BLOCKED]</code> 标记常量在 <code>src/dispatch/notification.ts</code>。</p><h2 id="_6-工具层边界" tabindex="-1">6. 工具层边界 <a class="header-anchor" href="#_6-工具层边界" aria-label="Permalink to &quot;6. 工具层边界&quot;">​</a></h2><p><code>src/graph/tools/index.ts</code> 的 <code>createGraphTools()</code> 注册全部 8 个图工具：<code>graph_create</code> / <code>graph_add_node</code> / <code>graph_add_edge</code> / <code>graph_add_loop</code> / <code>graph_run</code> / <code>graph_status</code> / <code>graph_cancel</code> / <code>graph_approve</code>。实现分布在同目录的 <code>graph-tools.ts</code>（核心工具集）、<code>approve-tools.ts</code>（<code>graph_approve</code>）、<code>status-queries.ts</code>（<code>graph_status</code> 查询）、<code>persisted-state.ts</code>（持久化状态查询）与 <code>live-state.ts</code>（进程内注册表，供内存态监视器读取）。</p><p>工具层与引擎的边界只有一条：<strong>工具是引擎的唯一构建入口</strong>。每次构建步骤之后以及每次 <code>graph_run</code> 时，工具层都依据当前声明<strong>重建一个全新的引擎</strong>，再通过 <code>adoptPrior()</code> 把先前一次运行的逐节点进度采纳进来——否则重建会把已完成的节点重置回 <code>ready</code> / <code>pending</code> 并重复派发。每个工具的参数与返回格式见<a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a>与<a href="/rolebox-docs/03-Reference/tool-catalog.html">工具目录</a>，本页不复制。</p><h2 id="备注" tabindex="-1">备注 <a class="header-anchor" href="#备注" aria-label="Permalink to &quot;备注&quot;">​</a></h2><blockquote><p>自 v1.0.0 起图引擎可用；v1.8.0 移除声明式多代理块后，<code>graph_*</code> 成为唯一的编排入口，且没有迁移命令——迁移是配置改写，对照表见<a href="/rolebox-docs/06-Appendix/migration.html">迁移</a>。</p></blockquote><h2 id="相关页面" tabindex="-1">相关页面 <a class="header-anchor" href="#相关页面" aria-label="Permalink to &quot;相关页面&quot;">​</a></h2><ul><li><a href="/rolebox-docs/04-Advanced/runtime-behavior.html">运行时行为</a> —— 状态机、推进时序与崩溃恢复</li><li><a href="/rolebox-docs/04-Advanced/graph-declaration.html">图声明</a> —— 图文档的字段、校验与序列化</li><li><a href="/rolebox-docs/02-Guide/graph-workflows.html">图工作流</a> —— 用 <code>graph_*</code> 编排团队的任务视角</li><li><a href="/rolebox-docs/04-Advanced/signal-system.html">信号系统</a> —— 信号台账、FSM 与 <code>signal</code> 工具</li><li><a href="/rolebox-docs/04-Advanced/workflow-patterns.html">工作流模式</a> —— 内建拓扑与选型</li><li><a href="/rolebox-docs/06-Appendix/source-index.html">源码索引</a> —— 概念到模块的总索引</li></ul>`,66)])])}const g=t(a,[["render",o]]);export{k as __pageData,g as default};
