import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.ChW7XBuO.js";const k=JSON.parse('{"title":"错误处理","description":"错误处理策略 — rolebox 的错误容忍机制、降级行为与安全边界","frontmatter":{"title":"错误处理","description":"错误处理策略 — rolebox 的错误容忍机制、降级行为与安全边界"},"headers":[],"relativePath":"03-Reference/error-handling.md","filePath":"03-Reference/error-handling.md"}'),d={name:"03-Reference/error-handling.md"};function n(r,e,o,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="错误处理" tabindex="-1">错误处理 <a class="header-anchor" href="#错误处理" aria-label="Permalink to &quot;错误处理&quot;">​</a></h1><blockquote><p><strong>相关文档：</strong> <a href="/rolebox-docs/03-Reference/recovery-system.html">恢复系统</a> — 恢复系统详细架构 | <a href="/rolebox-docs/03-Reference/dispatch-config.html">调度配置</a> — 并发与预算配置 | <a href="/rolebox-docs/03-Reference/limitations.html">已知限制</a> — 当前版本限制</p></blockquote><p>rolebox 采用**错误容忍（fault-tolerant）**设计原则：单个组件的故障不会导致整个系统崩溃。以下是各场景下的降级行为汇总。</p><h2 id="降级行为一览" tabindex="-1">降级行为一览 <a class="header-anchor" href="#降级行为一览" aria-label="Permalink to &quot;降级行为一览&quot;">​</a></h2><table tabindex="0"><thead><tr><th>场景</th><th>行为</th></tr></thead><tbody><tr><td>YAML 格式无效或文件缺失</td><td>跳过该角色，opencode 不会崩溃</td></tr><tr><td>技能文件缺失</td><td>输出警告，不阻塞角色加载</td></tr><tr><td>函数文件缺失</td><td>静默跳过</td></tr><tr><td>无效的函数激活语法（大写、句中管道符）</td><td>保持消息原样，不做修改</td></tr><tr><td>Hook 模块加载失败（缺失、语法错误）</td><td>记录警告，Hook 被跳过，继续运行</td></tr><tr><td>Hook 处理器异常</td><td>try/catch 包裹，记录警告，继续运行</td></tr><tr><td>扩展模块加载失败</td><td>按模块捕获，记录警告，跳过该扩展</td></tr><tr><td>子代理调度失败</td><td>支持重试机制和背压控制</td></tr><tr><td>无效的环境变量插值</td><td>保留原始 <code>{env:...}</code> 文本</td></tr></tbody></table><h2 id="快速参考-我应该配置哪个恢复策略" tabindex="-1">快速参考：我应该配置哪个恢复策略 <a class="header-anchor" href="#快速参考-我应该配置哪个恢复策略" aria-label="Permalink to &quot;快速参考：我应该配置哪个恢复策略&quot;">​</a></h2><p>根据你遇到的错误类型，以下表格推荐默认策略链。所有策略名称均来自内置注册表（<code>src/recovery/config.ts:88-96</code>），可直接用于 <code>role.yaml</code> 的 <code>hooks.recovery</code> 配置块。</p><table tabindex="0"><thead><tr><th>症状</th><th>建议策略链</th><th>说明</th></tr></thead><tbody><tr><td><strong>API 500 / 网络超时 / 服务端错误</strong></td><td><code>retry(2次, 指数退避)</code> → <code>compact</code> → <code>abort</code></td><td>使用 <code>session_error</code> 默认链。前两次重试指数退避，然后压缩会话上下文，最后中止</td></tr><tr><td><strong>Token 超限（context_length_exceeded）</strong></td><td><code>truncate(最多8次, 50%压缩)</code> → <code>summarize</code> → <code>abort</code></td><td>使用 <code>context_window</code> 默认链。先截断输出，再尝试总结压缩，均失败则中止</td></tr><tr><td><strong>Edit 错误（oldString not found / multiple matches / same content）</strong></td><td><code>remind_and_retry(2次)</code></td><td>使用 <code>edit_error</code> 默认链。注入提示让模型重新读取文件再重试</td></tr><tr><td><strong>JSON 解析错误（unexpected token / invalid json）</strong></td><td><code>remind_and_retry(2次)</code></td><td>使用 <code>json_error</code> 默认链。注入修复提示后重试</td></tr><tr><td><strong>空响应（工具返回空或过短）</strong></td><td><code>remind_and_retry(1次)</code></td><td>使用 <code>empty_response</code> 默认链。重试一次后推进到下一策略或中止</td></tr><tr><td><strong>模型能力不足需要回退</strong></td><td><code>retry</code> → <code>fallback_model</code> → <code>abort</code></td><td>在 <code>session_error</code> 链中插入 <code>fallback_model</code>，使用备选模型重新提示。需要额外配置备选模型参数</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">策略链是可按角色定制</p><p>每个角色可以在 <code>role.yaml</code> 中独立配置策略链。详细配置语法参见下方「按角色配置恢复策略」章节。</p></div><h2 id="设计原则" tabindex="-1">设计原则 <a class="header-anchor" href="#设计原则" aria-label="Permalink to &quot;设计原则&quot;">​</a></h2><ul><li><strong>隔离故障</strong>：每个角色、Hook、扩展都是独立的故障域。一个角色的 YAML 错误不会影响其他角色。</li><li><strong>优雅降级</strong>：缺失的技能或函数不会阻塞启动——系统以警告的方式继续运行，尽最大努力提供可用功能。</li><li><strong>用户可见性</strong>：所有降级行为都会通过日志/警告的方式通知用户，便于诊断和修复。</li><li><strong>不做猜测</strong>：遇到无法解析的内容时，保持原样传递，不尝试自动修正。</li></ul><h2 id="恢复系统-recovery-system" tabindex="-1">恢复系统 (Recovery System) <a class="header-anchor" href="#恢复系统-recovery-system" aria-label="Permalink to &quot;恢复系统 (Recovery System)&quot;">​</a></h2><p>恢复系统是一个可插拔的错误恢复框架，用于捕获常见故障模式并自动执行补偿策略。它由 <code>RecoveryEngine</code> 统一管理（<code>src/recovery/engine.ts:21-56</code>），通过可配置的策略链（chain-of-strategies）将错误恢复与业务逻辑解耦。</p><h3 id="架构概览" tabindex="-1">架构概览 <a class="header-anchor" href="#架构概览" aria-label="Permalink to &quot;架构概览&quot;">​</a></h3><p>恢复系统由四个核心组件构成：</p><table tabindex="0"><thead><tr><th>组件</th><th>文件</th><th>职责</th></tr></thead><tbody><tr><td><code>RecoveryEngine</code></td><td><code>src/recovery/engine.ts</code></td><td>恢复系统的入口，管理配置、状态、指标和策略链的执行入口</td></tr><tr><td><code>PatternRegistry</code></td><td><code>src/recovery/error-detection.ts</code></td><td>注册和匹配错误模式，从原始错误中提取结构化 <code>RecoveryError</code></td></tr><tr><td><code>StrategyRegistry</code></td><td><code>src/recovery/strategies/registry.ts</code></td><td>注册和查找恢复策略，运行时按名称获取策略实例</td></tr><tr><td><code>RecoveryChainExecutor</code></td><td><code>src/recovery/chain-executor.ts</code></td><td>执行策略链，按顺序尝试策略，直到恢复成功或链耗尽</td></tr></tbody></table><h3 id="错误检测" tabindex="-1">错误检测 <a class="header-anchor" href="#错误检测" aria-label="Permalink to &quot;错误检测&quot;">​</a></h3><p><code>PatternRegistry</code>（<code>src/recovery/error-detection.ts:6-57</code>）维护一组 <code>ErrorPattern</code>（<code>src/recovery/types.ts:192-203</code>），每个模式通过 <code>match(error)</code> 方法判断是否匹配。系统内置 7 个默认错误模式（<code>src/recovery/error-detection.ts:90-297</code>）：</p><table tabindex="0"><thead><tr><th>模式名称</th><th>所属类别</th><th>匹配条件</th></tr></thead><tbody><tr><td><code>api-error</code></td><td><code>session_error</code></td><td>错误对象包含 <code>error.type</code>、<code>code</code> 或 <code>status</code> 字段</td></tr><tr><td><code>timeout</code></td><td><code>session_error</code></td><td>消息包含 timeout / timed out / deadline exceeded / ETIMEDOUT 关键字</td></tr><tr><td><code>tool-unavailable</code></td><td><code>session_error</code></td><td>消息匹配 tool not found / unknown tool / unavailable tool</td></tr><tr><td><code>token-limit</code></td><td><code>context_window</code></td><td>消息包含 context_length_exceeded / maximum context length / token limit 等</td></tr><tr><td><code>edit-not-found</code> / <code>edit-multiple-matches</code> / <code>edit-same-content</code></td><td><code>edit_error</code></td><td>Edit 工具返回 oldString not found / multiple matches / same content 错误</td></tr><tr><td><code>json-parse-error</code></td><td><code>json_error</code></td><td>消息匹配 JSON 语法错误正则（unexpected token / invalid json 等）</td></tr><tr><td><code>empty-response</code></td><td><code>empty_response</code></td><td>工具输出或模型响应为空或过短（&lt; 5 字符）</td></tr></tbody></table><h3 id="_7-种内置恢复策略" tabindex="-1">7 种内置恢复策略 <a class="header-anchor" href="#_7-种内置恢复策略" aria-label="Permalink to &quot;7 种内置恢复策略&quot;">​</a></h3><p>所有策略实现 <code>RecoveryStrategy</code> 接口（<code>src/recovery/types.ts:259-264</code>），通过 <code>registerBuiltinStrategies()</code> 注册（<code>src/recovery/strategies/index.ts:20-28</code>）：</p><table tabindex="0"><thead><tr><th>策略</th><th>文件</th><th>行为</th></tr></thead><tbody><tr><td><code>retry</code></td><td><code>src/recovery/strategies/retry-strategy.ts</code></td><td>指数退避重试（<code>backoff_ms * backoff_factor^attempt</code>），直到达到 <code>max_retries</code></td></tr><tr><td><code>compact</code></td><td><code>src/recovery/strategies/compact-strategy.ts</code></td><td>调用 client 的 session.compact() 压缩会话上下文</td></tr><tr><td><code>fallback_model</code></td><td><code>src/recovery/strategies/fallback-model-strategy.ts</code></td><td>使用备选模型重新提示（通过 <code>promptAsync</code> 注入恢复指令）</td></tr><tr><td><code>remind_and_retry</code></td><td><code>src/recovery/strategies/remind-and-retry-strategy.ts</code></td><td>注入提示文本到系统 prompt，然后重试——适用于编辑错误和 JSON 解析错误</td></tr><tr><td><code>truncate</code></td><td><code>src/recovery/strategies/truncate-strategy.ts</code></td><td>注入截断指令，要求模型将输出减少到指定的 <code>target_ratio</code></td></tr><tr><td><code>summarize</code></td><td><code>src/recovery/strategies/summarize-strategy.ts</code></td><td>注入总结指令或调用 API 进行上下文压缩</td></tr><tr><td><code>abort</code></td><td><code>src/recovery/strategies/abort-strategy.ts</code></td><td>最终策略：注入中止消息并终止恢复链</td></tr></tbody></table><h3 id="策略链-strategy-chain" tabindex="-1">策略链 (Strategy Chain) <a class="header-anchor" href="#策略链-strategy-chain" aria-label="Permalink to &quot;策略链 (Strategy Chain)&quot;">​</a></h3><p>策略链是恢复系统的核心设计模式（<code>src/recovery/chain-executor.ts:26-123</code>）。每个错误类别可以配置一个有序的策略序列。当错误被捕获时，<code>RecoveryChainExecutor</code> 按顺序执行链中的策略，根据策略返回的状态决定流程：</p><ul><li><strong><code>success</code></strong> → 恢复成功，清除状态</li><li><strong><code>retry</code></strong> → 再次执行当前策略（支持延迟等待）</li><li><strong><code>next_strategy</code></strong> → 前进到链中的下一个策略</li><li><strong><code>abort</code></strong> → 中止整个恢复链，向模型注入终止消息</li></ul><p>默认配置（<code>src/recovery/config.ts:48-78</code>）定义了以下策略链：</p><table tabindex="0"><thead><tr><th>错误类别</th><th>默认链</th></tr></thead><tbody><tr><td><code>session_error</code></td><td>retry(2次, 指数退避) → compact → abort</td></tr><tr><td><code>context_window</code></td><td>truncate(最多8次, 50%压缩比) → summarize → abort</td></tr><tr><td><code>edit_error</code></td><td>remind_and_retry(2次)</td></tr><tr><td><code>json_error</code></td><td>remind_and_retry(2次)</td></tr><tr><td><code>empty_response</code></td><td>remind_and_retry(1次)</td></tr></tbody></table><h3 id="恢复策略组合最佳实践" tabindex="-1">恢复策略组合最佳实践 <a class="header-anchor" href="#恢复策略组合最佳实践" aria-label="Permalink to &quot;恢复策略组合最佳实践&quot;">​</a></h3><p>合理编排策略链可以显著提升恢复成功率。以下三条原则来自实际部署经验（策略名称均源于 <code>src/recovery/config.ts:88-96</code> 的内置注册表）。</p><h4 id="_1-始终以-abort-结尾" tabindex="-1">1. 始终以 <code>abort</code> 结尾 <a class="header-anchor" href="#_1-始终以-abort-结尾" aria-label="Permalink to &quot;1. 始终以 \`abort\` 结尾&quot;">​</a></h4><p>每个策略链的最后一个策略应为 <code>abort</code>，确保恢复失败时能向模型注入明确的中止消息，而非静默失败。默认链已遵循此原则（<code>session_error</code> 和 <code>context_window</code> 均以 <code>abort</code> 结尾），自定义链中也应保持。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ✅ 推荐：以 abort 结尾</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">session_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">retry</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">compact</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">abort</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # ← 最终报告失败</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ❌ 不推荐：链耗尽后无任何反馈</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">session_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">retry</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">compact</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 如果失败，模型得不到任何提示</span></span></code></pre></div><h4 id="_2-策略链不超过-4-个环节" tabindex="-1">2. 策略链不超过 4 个环节 <a class="header-anchor" href="#_2-策略链不超过-4-个环节" aria-label="Permalink to &quot;2. 策略链不超过 4 个环节&quot;">​</a></h4><p>每增加一个策略，恢复链的延迟和复杂性随之增长。超过 4 个环节的链往往因累积延迟而抵消恢复收益。建议将非核心策略放在并行链中，或提高前置策略的 <code>max_retries</code> 以减少不必要的链跳转。</p><table tabindex="0"><thead><tr><th>推荐配置</th><th>说明</th></tr></thead><tbody><tr><td><code>retry(2-3次)</code> → <code>compact</code> → <code>fallback_model</code> → <code>abort</code></td><td>4 环节以内，覆盖最常见的恢复路径</td></tr><tr><td><code>truncate(多次)</code> → <code>summarize</code> → <code>abort</code></td><td>3 环节，足以应对上下文窗口溢出</td></tr><tr><td><code>remind_and_retry(2次)</code> → <code>abort</code></td><td>2 环节，适用于编辑和 JSON 类错误</td></tr></tbody></table><h4 id="_3-remind-and-retry-单独用于编辑-json-错误" tabindex="-1">3. <code>remind_and_retry</code> 单独用于编辑/JSON 错误 <a class="header-anchor" href="#_3-remind-and-retry-单独用于编辑-json-错误" aria-label="Permalink to &quot;3. \`remind_and_retry\` 单独用于编辑/JSON 错误&quot;">​</a></h4><p><code>remind_and_retry</code> 的行为是注入提示文本后请求模型原地重试，与 <code>retry</code>（指数退避重试）的机制不同。对于 <code>edit_error</code> 和 <code>json_error</code> 类别，<code>remind_and_retry</code> 比重试更有效，因为这两类错误的根因是模型输出格式问题而非服务端暂时故障——重试一段时间后再次调用并不会自行修复。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">edit_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">remind_and_retry</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        max_retries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        reminder_text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[EDIT ERROR] 请重新读取文件后重试&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">json_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">remind_and_retry</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        max_retries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        reminder_text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[JSON 错误] 修正 JSON 语法后重试&quot;</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">调试新策略链</p><p>部署新策略链前，建议先在开发环境中使用 <code>rolebox info --check</code> 验证配置是否生效。然后通过观察日志前缀 <code>recovery:chain</code> 确认策略按预期顺序执行。详见下方「调试恢复系统」章节。</p></div><h3 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h3><p>恢复系统通过 <code>RecoveryConfig</code> 类型配置（<code>src/recovery/config.ts:31-37</code>）。关键配置项：</p><ul><li><strong><code>enabled</code></strong>：全局开关（默认 <code>true</code>），设为 <code>false</code> 可完全禁用恢复</li><li><strong><code>maxTotalAttempts</code></strong>：单次会话的总尝试次数硬限制（默认 10）</li><li><strong><code>persistState</code></strong>：是否将恢复状态持久化到磁盘</li><li><strong><code>collectMetrics</code></strong>：是否收集恢复指标（计数、成功率等）</li><li><strong><code>chains</code></strong>：按错误类别配置的策略链</li></ul><p>自定义策略可通过 <code>RecoveryEngine.registerStrategy()</code> 注册（<code>src/recovery/engine.ts:163-166</code>），同时使用 <code>addKnownStrategy()</code> 登记到已知策略列表（<code>src/recovery/config.ts:101-103</code>），确保 YAML 配置验证通过。</p><blockquote><p>完整的恢复系统详细架构说明见 <a href="./recovery-system.html">恢复系统参考</a>。</p></blockquote><h2 id="用户可见的错误消息" tabindex="-1">用户可见的错误消息 <a class="header-anchor" href="#用户可见的错误消息" aria-label="Permalink to &quot;用户可见的错误消息&quot;">​</a></h2><p>以下错误是用户在使用过程中可能直接遇到的，以 <code>QueueFullError</code> 和 <code>WaiterTimeoutError</code> 两个核心类为代表（均在 <code>src/dispatch/concurrency/concurrency.ts</code> 中定义）。</p><h3 id="queuefullerror" tabindex="-1"><code>QueueFullError</code> <a class="header-anchor" href="#queuefullerror" aria-label="Permalink to &quot;\`QueueFullError\`&quot;">​</a></h3><p>当并发等待队列满时抛出。用户会看到类似以下消息：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Queue is full: 12 queued tasks (limit: 10)</span></span></code></pre></div><p>包含的字段（<code>src/dispatch/concurrency/concurrency.ts:49-61</code>）：</p><ul><li><code>depth</code>：当前排队任务数</li><li><code>limit</code>：队列最大深度</li><li><code>retryAfter</code>：建议重试延迟（默认 30000ms）</li></ul><p>此错误通常在 <code>maxQueueDepth</code> 和 <code>maxConcurrent</code> 配置过小时出现。建议增加 <code>maxConcurrent</code> 或 <code>maxQueueDepth</code> 的值，或在 <code>role.yaml</code> 的 <code>dispatch:</code> 块中调整。</p><h3 id="waitertimeouterror" tabindex="-1"><code>WaiterTimeoutError</code> <a class="header-anchor" href="#waitertimeouterror" aria-label="Permalink to &quot;\`WaiterTimeoutError\`&quot;">​</a></h3><p>当任务在队列中等待超过 TTL 后超时抛出。用户会看到：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Waiter timed out after 300000ms for key &quot;anthropic/claude-sonnet&quot;</span></span></code></pre></div><p>默认 TTL 为 300 秒（5 分钟）（<code>src/dispatch/concurrency/concurrency.ts:46</code>）。如果任务在排队阶段持续等待超过此时间，该任务被自动取消。建议检查模型响应速度或增加模型的并发槽位数。</p><h3 id="预算超限" tabindex="-1">预算超限 <a class="header-anchor" href="#预算超限" aria-label="Permalink to &quot;预算超限&quot;">​</a></h3><p>预算限制由 <code>BudgetTracker</code> 管理（<code>src/dispatch/budget/budget-tracker.ts:148-203</code>），超限时用户会看到类似以下消息：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Request cost budget exhausted: 0.52 &gt;= 0.5</span></span>
<span class="line"><span>Session input token budget exhausted: 52410 &gt;= 50000</span></span></code></pre></div><p>预算超限的任务会被自动取消，并在下次轮询时记录到指标中。这些限制通过 <code>role.yaml</code> 的 <code>dispatch:</code> 块中以下字段配置：</p><ul><li><code>maxInputTokensPerRequest</code> / <code>maxOutputTokensPerRequest</code> / <code>maxCostPerRequest</code></li><li><code>maxInputTokensPerSession</code> / <code>maxCostPerSession</code></li></ul><h2 id="按角色配置恢复策略" tabindex="-1">按角色配置恢复策略 <a class="header-anchor" href="#按角色配置恢复策略" aria-label="Permalink to &quot;按角色配置恢复策略&quot;">​</a></h2><p>恢复策略可以在每个角色的 <code>role.yaml</code> 的 <code>hooks.recovery</code> 块中按类别配置。配置结构遵循 <code>RecoveryConfig</code> 类型（<code>src/recovery/config.ts:31-37</code>）：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  recovery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                   # 全局开关</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    max_total_attempts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # 单次会话总尝试次数硬限制</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    persist_state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             # 持久化恢复状态到磁盘</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    collect_metrics</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # 收集恢复指标</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 按错误类别配置策略链</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    session_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">retry</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            max_retries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            backoff_ms</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            backoff_factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">compact</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">fallback_model</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">abort</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;会话失败，已尝试所有恢复手段&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    edit_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # 禁用编辑错误的自动恢复</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    json_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">remind_and_retry</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            max_retries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            reminder_text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;请检查 JSON 语法后重试&quot;</span></span></code></pre></div><h3 id="完全禁用恢复" tabindex="-1">完全禁用恢复 <a class="header-anchor" href="#完全禁用恢复" aria-label="Permalink to &quot;完全禁用恢复&quot;">​</a></h3><p>将 <code>enabled: false</code> 可以完全禁用整个恢复系统（<code>src/recovery/engine.ts:70-72</code>）：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  recovery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><p>禁用后，所有错误检测和策略链都不会执行，错误直接抛出到上层调用方。</p><div class="tip custom-block"><p class="custom-block-title">自定义恢复策略注册</p><p>除了内置的 7 种策略外，你可以通过两种方式添加自定义策略：</p><ol><li><strong>扩展机制</strong>：在 <code>extensions.recovery_strategies</code> 中声明模块（见<a href="./extensions.html">扩展机制</a>）。</li><li><strong>API 注册</strong>：通过 <code>RecoveryEngine.registerStrategy()</code>（<code>src/recovery/engine.ts:163-166</code>）注册策略实例，并调用 <code>addKnownStrategy()</code>（<code>src/recovery/config.ts:101-103</code>）登记到已知策略列表，确保 YAML 配置验证通过。</li></ol><p>两种方式都需要在自定义策略模块中实现 <code>RecoveryStrategy</code> 接口（<code>src/recovery/types.ts:259-264</code>）。</p></div><h2 id="调试恢复系统" tabindex="-1">调试恢复系统 <a class="header-anchor" href="#调试恢复系统" aria-label="Permalink to &quot;调试恢复系统&quot;">​</a></h2><h3 id="检查恢复配置是否生效" tabindex="-1">检查恢复配置是否生效 <a class="header-anchor" href="#检查恢复配置是否生效" aria-label="Permalink to &quot;检查恢复配置是否生效&quot;">​</a></h3><p>使用 <code>rolebox info &lt;role&gt; --check</code> 可验证角色的恢复配置：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rolebox</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> info</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-role</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span></span></code></pre></div><p>命令输出包含恢复系统状态信息：</p><ul><li>恢复系统是否启用（<code>hooks.recovery.enabled</code>）</li><li>已注册的策略列表（通过 <code>RecoveryEngine.getStrategyRegistry().names()</code>）</li><li>各错误类别的策略链配置</li></ul><h3 id="恢复指标快照" tabindex="-1">恢复指标快照 <a class="header-anchor" href="#恢复指标快照" aria-label="Permalink to &quot;恢复指标快照&quot;">​</a></h3><p>通过 <code>RecoveryEngine.getMetrics()</code>（<code>src/recovery/engine.ts:172-174</code>）获取运行时指标快照，可用于诊断恢复行为的健康状况：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>totalAttempts: 15          # 总恢复尝试次数</span></span>
<span class="line"><span>successfulRecoveries: 12   # 成功恢复次数</span></span>
<span class="line"><span>abortedChains: 2           # 被中止的链数</span></span>
<span class="line"><span>exhaustedChains: 1         # 耗尽的链数</span></span>
<span class="line"><span>byCategory:</span></span>
<span class="line"><span>  session_error:</span></span>
<span class="line"><span>    attempts: 8</span></span>
<span class="line"><span>    successes: 6</span></span>
<span class="line"><span>  edit_error:</span></span>
<span class="line"><span>    attempts: 7</span></span>
<span class="line"><span>    successes: 6</span></span></code></pre></div><h3 id="日志前缀" tabindex="-1">日志前缀 <a class="header-anchor" href="#日志前缀" aria-label="Permalink to &quot;日志前缀&quot;">​</a></h3><p>恢复系统各组件使用独立的结构化日志器（<code>src/recovery/engine.ts:12</code>）：</p><table tabindex="0"><thead><tr><th>日志前缀</th><th>来源</th><th>典型消息</th></tr></thead><tbody><tr><td><code>recovery:engine</code></td><td><code>src/recovery/engine.ts</code></td><td><code>RecoveryEngine initialized</code> / <code>Starting recovery chain</code> / <code>Recovery successful</code></td></tr><tr><td><code>recovery:state</code></td><td><code>src/recovery/state.ts</code></td><td>状态持久化相关</td></tr><tr><td><code>recovery:metrics</code></td><td><code>src/recovery/metrics.ts</code></td><td>指标收集相关</td></tr><tr><td><code>recovery:chain</code></td><td><code>src/recovery/chain-executor.ts</code></td><td>策略链执行过程</td></tr><tr><td><code>ext:loader</code></td><td><code>src/extensions/loader.ts</code></td><td>自定义恢复策略模块加载</td></tr></tbody></table><h3 id="常见问题排查" tabindex="-1">常见问题排查 <a class="header-anchor" href="#常见问题排查" aria-label="Permalink to &quot;常见问题排查&quot;">​</a></h3><table tabindex="0"><thead><tr><th>症状</th><th>可能原因</th><th>检查点</th></tr></thead><tbody><tr><td>错误未被恢复</td><td>类别无对应策略链</td><td>检查 <code>hooks.recovery.{category}.enabled</code> 是否为 <code>false</code></td></tr><tr><td>自定义策略未生效</td><td>未调用 <code>addKnownStrategy()</code></td><td>验证 <code>src/recovery/config.ts:101-103</code> 是否已调用</td></tr><tr><td>恢复状态持续增长</td><td><code>persistState: true</code> 但未清除</td><td>检查恢复成功后 <code>stateStore.delete()</code> 是否调用（<code>src/recovery/engine.ts:149-150</code>）</td></tr><tr><td>恢复链始终失败</td><td><code>maxTotalAttempts</code> 过小</td><td>默认 10 次，可在 <code>role.yaml</code> 中增加</td></tr><tr><td>重试后任务状态丢失</td><td>未使用 <code>dispatch_checkpoint</code> 持久化中间状态</td><td>在任务执行过程中调用 <code>dispatch_checkpoint()</code> 保存阶段状态，失败重试时可自动注入上下文避免重复工作。详见<a href="./tool-catalog.html#dispatch_checkpoint">工具目录</a></td></tr></tbody></table><blockquote><p>恢复系统详细架构说明见<a href="./recovery-system.html">恢复系统参考</a>。</p></blockquote><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li><a href="./limitations.html">已知限制</a> — 当前版本的已知功能限制</li></ul>`,86)])])}const y=s(d,[["render",n]]);export{k as __pageData,y as default};
