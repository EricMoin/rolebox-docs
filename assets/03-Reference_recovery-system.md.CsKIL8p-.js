import{_ as i,C as o,o as t,c as r,a2 as s,b as c,w as d,a as n,E as h,a3 as l}from"./chunks/framework.CuUVYbLV.js";const _=JSON.parse('{"title":"恢复系统（Recovery System）","description":"RecoveryEngine 与 PatternRegistry 的内部实现 — 9 个错误模式、7 个内置策略、策略链执行、状态持久化与平台差异","frontmatter":{"title":"恢复系统（Recovery System）","description":"RecoveryEngine 与 PatternRegistry 的内部实现 — 9 个错误模式、7 个内置策略、策略链执行、状态持久化与平台差异"},"headers":[],"relativePath":"03-Reference/recovery-system.md","filePath":"03-Reference/recovery-system.md"}'),p={name:"03-Reference/recovery-system.md"};function k(y,e,g,E,m,u){const a=o("Mermaid");return t(),r("div",null,[e[1]||(e[1]=s('<h1 id="恢复系统-recovery-system" tabindex="-1">恢复系统（Recovery System） <a class="header-anchor" href="#恢复系统-recovery-system" aria-label="Permalink to &quot;恢复系统（Recovery System）&quot;">​</a></h1><p>恢复系统拦截 Agent 运行时的常见故障模式，按错误类别选择一条策略链并依次执行补偿动作。实现位于 <code>src/recovery/</code>，由 <code>RecoveryEngine</code> 统一管理错误检测、链执行、状态持久化与指标收集。</p><blockquote><p><strong>本页的边界</strong>：本页是引擎内部实现。角色作者要写的 <code>hooks.recovery</code> 配置字段、默认值与示例见 <a href="/rolebox-docs/03-Reference/role-yaml.html">role.yaml 参考</a>；用户面的降级行为总表与选型决策表见<a href="/rolebox-docs/03-Reference/error-handling.html">错误处理</a>；恢复策略与错误模式的扩展注册协议见<a href="/rolebox-docs/03-Reference/extensions.html">扩展机制</a>。</p></blockquote><blockquote><p>自 v0.19.0 起，恢复框架提供可配置策略链与恢复状态持久化。</p></blockquote><h2 id="_1-组件构成" tabindex="-1">1. 组件构成 <a class="header-anchor" href="#_1-组件构成" aria-label="Permalink to &quot;1. 组件构成&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>模块</th><th>职责</th></tr></thead><tbody><tr><td><code>RecoveryEngine</code></td><td><code>src/recovery/engine.ts</code></td><td>入口：初始化默认模式与策略、编排 <code>recover()</code>、暴露注册 API</td></tr><tr><td><code>PatternRegistry</code></td><td><code>src/recovery/error-detection.ts</code></td><td>注册与匹配错误模式，把原始错误转成结构化 <code>RecoveryError</code></td></tr><tr><td><code>StrategyRegistry</code></td><td><code>src/recovery/strategies/registry.ts</code></td><td>按名称注册与查找策略实例</td></tr><tr><td><code>RecoveryChainExecutor</code></td><td><code>src/recovery/chain-executor.ts</code></td><td>顺序执行策略链，按策略返回状态决定走向</td></tr><tr><td><code>RecoveryStateStore</code></td><td><code>src/recovery/state.ts</code></td><td>每会话状态持久化（写代次保护、原子写入、去重）</td></tr><tr><td><code>RecoveryMetricsCollector</code></td><td><code>src/recovery/metrics.ts</code></td><td>尝试次数、成功率、链结局统计</td></tr><tr><td>内置策略</td><td><code>src/recovery/strategies/</code></td><td>7 个策略实现与注册入口</td></tr><tr><td>恢复型内置 Hook</td><td><code>src/recovery/builtin/</code></td><td>5 个错误恢复 Hook 与 4 个护栏 Hook</td></tr></tbody></table>',6)),(t(),c(l,null,{default:d(()=>[h(a,{id:"mermaid-124",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20A%5BRecoveryEngine%5D%20--%3E%20B%5BPatternRegistry%5D%0A%20%20%20%20A%20--%3E%20C%5BRecoveryChainExecutor%5D%0A%20%20%20%20A%20--%3E%20D%5BStrategyRegistry%5D%0A%20%20%20%20A%20--%3E%20E%5BRecoveryStateStore%5D%0A%20%20%20%20A%20--%3E%20F%5BRecoveryMetricsCollector%5D%0A%20%20%20%20C%20--%3E%20D%0A%20%20%20%20C%20--%3E%20G%5Babort%20%2F%20retry%20%2F%20compact%20%2F%20fallback_model%5D%0A%20%20%20%20C%20--%3E%20H%5Btruncate%20%2F%20remind_and_retry%20%2F%20summarize%5D%0A"})]),fallback:d(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=s(`<p><code>RecoveryEngine</code> 在构造时完成四件事：创建 <code>RecoveryMetricsCollector</code>；用 <code>createDefaultPatterns()</code> 注册 9 个默认错误模式；用 <code>registerBuiltinStrategies()</code> 注册 7 个内置策略；创建 <code>RecoveryChainExecutor</code>。</p><h2 id="_2-recover-的主流程" tabindex="-1">2. <code>recover()</code> 的主流程 <a class="header-anchor" href="#_2-recover-的主流程" aria-label="Permalink to &quot;2. \`recover()\` 的主流程&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>recover(sessionID, error, category?)</span></span>
<span class="line"><span>  ├─ 配置未启用 → { recovered: false }</span></span>
<span class="line"><span>  ├─ 给了 category → PatternRegistry.detectCategory()</span></span>
<span class="line"><span>  │      未匹配 → 退回构造一个基础 RecoveryError</span></span>
<span class="line"><span>  ├─ 未给 category → PatternRegistry.detectFirst()</span></span>
<span class="line"><span>  │      未匹配 → { recovered: false }</span></span>
<span class="line"><span>  ├─ 记录 errorType 指标</span></span>
<span class="line"><span>  ├─ 取该 category 的链配置</span></span>
<span class="line"><span>  │      无配置或链被禁用 → { recovered: false }</span></span>
<span class="line"><span>  ├─ 构造 inject() 闭包与 onAttempt() 回调</span></span>
<span class="line"><span>  └─ chainExecutor.executeChain()</span></span>
<span class="line"><span>        ├─ recovered → 清除持久化状态，{ recovered: true }</span></span>
<span class="line"><span>        ├─ aborted   → { recovered: false }（带中止原因）</span></span>
<span class="line"><span>        └─ exhausted → { recovered: false }（带耗尽原因）</span></span></code></pre></div><p>生命周期方法（<code>registerStrategy()</code>、<code>registerErrorPattern()</code>、<code>getMetrics()</code>、<code>getPatternRegistry()</code>、<code>getStrategyRegistry()</code>、<code>dispose()</code>）都是薄封装；<code>dispose()</code> 只在 <code>persistState</code> 与 <code>collectMetrics</code> 同时为真时调用一次 <code>flushSync()</code>，把待写状态落盘。</p><h2 id="_3-错误检测" tabindex="-1">3. 错误检测 <a class="header-anchor" href="#_3-错误检测" aria-label="Permalink to &quot;3. 错误检测&quot;">​</a></h2><p><code>PatternRegistry</code> 持有一组 <code>ErrorPattern</code>，每个模式自行判断是否匹配：</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ErrorPattern</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 模式名，用于日志与指标</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  category</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RecoveryErrorCategory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 匹配后产生的错误类别</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  match</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RecoveryError</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 匹配函数</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>方法</th><th>行为</th></tr></thead><tbody><tr><td><code>detect(error)</code></td><td>返回全部匹配的 <code>RecoveryError</code></td></tr><tr><td><code>detectFirst(error)</code></td><td>返回第一个匹配项</td></tr><tr><td><code>detectCategory(error, category)</code></td><td>只在指定类别内匹配</td></tr></tbody></table><h3 id="_9-个默认错误模式" tabindex="-1">9 个默认错误模式 <a class="header-anchor" href="#_9-个默认错误模式" aria-label="Permalink to &quot;9 个默认错误模式&quot;">​</a></h3><table tabindex="0"><thead><tr><th>模式</th><th>类别</th><th>触发条件</th></tr></thead><tbody><tr><td><code>api-error</code></td><td><code>session_error</code></td><td>错误对象带有 <code>error.type</code>、<code>code</code> 或 <code>status</code> 字段</td></tr><tr><td><code>timeout</code></td><td><code>session_error</code></td><td>消息包含 timeout / timed out / deadline exceeded / ETIMEDOUT</td></tr><tr><td><code>tool-unavailable</code></td><td><code>session_error</code></td><td>消息匹配 tool not found / unknown tool / unavailable tool</td></tr><tr><td><code>token-limit</code></td><td><code>context_window</code></td><td>消息包含 context_length_exceeded / maximum context length / token limit</td></tr><tr><td><code>edit-not-found</code></td><td><code>edit_error</code></td><td>Edit 工具返回 oldString not found</td></tr><tr><td><code>edit-multiple-matches</code></td><td><code>edit_error</code></td><td>Edit 工具返回 oldString found multiple times</td></tr><tr><td><code>edit-same-content</code></td><td><code>edit_error</code></td><td>Edit 工具返回 oldString and newString must be different</td></tr><tr><td><code>json-parse-error</code></td><td><code>json_error</code></td><td>消息匹配 JSON 语法错误正则（unexpected token / invalid json）</td></tr><tr><td><code>empty-response</code></td><td><code>empty_response</code></td><td>工具输出或模型响应为空，或短于 5 个字符</td></tr></tbody></table><p>错误消息的提取由 <code>extractMessage()</code> 统一处理，兼容字符串、<code>Error</code> 对象，以及带 <code>message</code> / <code>error</code> / <code>output</code> / <code>title</code> 字段的对象。</p><h2 id="_4-恢复策略" tabindex="-1">4. 恢复策略 <a class="header-anchor" href="#_4-恢复策略" aria-label="Permalink to &quot;4. 恢复策略&quot;">​</a></h2><p>所有策略实现同一个接口：</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RecoveryStrategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RecoveryStrategyContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RecoveryStrategyResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>上下文 <code>RecoveryStrategyContext</code> 提供 <code>sessionID</code>、<code>error</code>、<code>attempt</code>、<code>stepConfig</code>、<code>inject(text)</code> 与可选的 <code>sessionClient</code>。其中 <code>attempt</code> 是当前步骤内自 0 起计的尝试序号：执行器在每次 <code>retry</code> 之后自增，因此第一次执行时它是 0（源码类型注释写作 1-based，与执行器的实际传值不一致）。</p><p>返回值是判别联合，决定链执行器的走向：</p><table tabindex="0"><thead><tr><th>状态</th><th>含义</th><th>链执行器行为</th></tr></thead><tbody><tr><td><code>success</code></td><td>恢复成功</td><td>终止链，返回 recovered</td></tr><tr><td><code>retry</code></td><td>需要重试（可带 <code>delayMs</code>）</td><td>停留在同一步骤，等待后重试</td></tr><tr><td><code>next_strategy</code></td><td>当前策略无效</td><td>前进到链中的下一步骤</td></tr><tr><td><code>abort</code></td><td>放弃恢复</td><td>终止链，返回 aborted</td></tr></tbody></table><h3 id="_7-个内置策略" tabindex="-1">7 个内置策略 <a class="header-anchor" href="#_7-个内置策略" aria-label="Permalink to &quot;7 个内置策略&quot;">​</a></h3><table tabindex="0"><thead><tr><th>策略</th><th>配置参数</th><th>行为要点</th></tr></thead><tbody><tr><td><code>abort</code></td><td><code>message</code></td><td>终端策略：注入 <code>[RECOVERY ABORTED]</code> 标记后返回 <code>abort</code></td></tr><tr><td><code>retry</code></td><td><code>max_retries</code>（2）、<code>backoff_ms</code>（2000）、<code>backoff_factor</code>（2）</td><td>第 n 次重试延迟 <code>backoff_ms × backoff_factor^attempt</code>；<code>attempt &gt;= max_retries</code> 时转 <code>next_strategy</code></td></tr><tr><td><code>compact</code></td><td>无</td><td>调用 <code>sessionClient.compact(sessionID)</code>；无该方法或返回 false 时转 <code>next_strategy</code></td></tr><tr><td><code>fallback_model</code></td><td><code>model</code></td><td>用指定模型重发恢复提示；未配置 <code>model</code> 或客户端无 <code>prompt</code> 时转 <code>next_strategy</code></td></tr><tr><td><code>truncate</code></td><td><code>max_truncations</code>（8）、<code>target_ratio</code>（0.5）、<code>min_output_size</code>（500）</td><td>注入「把输出再压缩约 1 − target_ratio」的指令；超次数后转 <code>next_strategy</code></td></tr><tr><td><code>remind_and_retry</code></td><td><code>max_retries</code>（2）、<code>reminder_text</code></td><td>注入提醒文本后固定间隔 1 秒重试</td></tr><tr><td><code>summarize</code></td><td><code>model</code>（默认 <code>default</code>）</td><td>优先让模型总结；客户端无 <code>prompt</code> 时回退为注入总结指令并返回 <code>retry</code></td></tr></tbody></table><p>依赖 <code>sessionClient</code> 的三个策略（<code>compact</code>、<code>fallback_model</code>、<code>summarize</code>）在客户端能力缺失时都不抛错，而是降级为 <code>next_strategy</code> 或指令注入。</p><h2 id="_5-策略链" tabindex="-1">5. 策略链 <a class="header-anchor" href="#_5-策略链" aria-label="Permalink to &quot;5. 策略链&quot;">​</a></h2><p><code>RecoveryChainExecutor.executeChain()</code> 按顺序遍历链。每个步骤把 <code>stepConfig</code> 交给策略；策略返回 <code>retry</code> 时保持步骤号不变、<code>attempt</code> 自增，返回 <code>next_strategy</code> 时步骤号加一并把 <code>attempt</code> 归零。链被禁用（<code>enabled: false</code>）或为空时立即返回 <code>exhausted</code>。</p><p>链的结局有三种：</p><table tabindex="0"><thead><tr><th>结果</th><th>含义</th><th>引擎后续处理</th></tr></thead><tbody><tr><td><code>recovered</code></td><td>某个策略成功</td><td>清除持久化状态，返回 <code>{ recovered: true }</code></td></tr><tr><td><code>aborted</code></td><td>某个策略要求中止</td><td>返回 <code>{ recovered: false }</code> 与中止原因</td></tr><tr><td><code>exhausted</code></td><td>链走完仍未成功</td><td>返回 <code>{ recovered: false }</code> 与耗尽原因</td></tr></tbody></table><p>两条工程性保证：</p><ul><li><strong>全局尝试上限</strong>：<code>maxTotalAttempts</code>（默认 10）对所有步骤的总尝试数生效，超出立即返回 <code>exhausted</code>。</li><li><strong>策略异常不外溢</strong>：每个策略的 <code>execute()</code> 都包在 try/catch 中，抛出的异常被折算成 <code>next_strategy</code>，链继续走下一步骤。</li></ul><h3 id="_5-条默认链" tabindex="-1">5 条默认链 <a class="header-anchor" href="#_5-条默认链" aria-label="Permalink to &quot;5 条默认链&quot;">​</a></h3><table tabindex="0"><thead><tr><th>错误类别</th><th>默认链</th></tr></thead><tbody><tr><td><code>session_error</code></td><td><code>retry</code>（2 次指数退避）→ <code>compact</code> → <code>abort</code></td></tr><tr><td><code>context_window</code></td><td><code>truncate</code>（最多 8 次，目标 50%）→ <code>summarize</code> → <code>abort</code></td></tr><tr><td><code>edit_error</code></td><td><code>remind_and_retry</code>（2 次）</td></tr><tr><td><code>json_error</code></td><td><code>remind_and_retry</code>（2 次）</td></tr><tr><td><code>empty_response</code></td><td><code>remind_and_retry</code>（1 次）</td></tr></tbody></table><p><code>tool_pair</code> 与 <code>guard_violation</code> 两个类别没有默认链，必须显式配置才会执行。</p><h2 id="_6-配置解析" tabindex="-1">6. 配置解析 <a class="header-anchor" href="#_6-配置解析" aria-label="Permalink to &quot;6. 配置解析&quot;">​</a></h2><p><code>parseRecoveryConfig()</code> 接收已经解析过的对象（来自 <code>hooks.recovery</code>），输出 <code>RecoveryConfig</code>：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  recovery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    max_total_attempts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">15</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    session_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">retry</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">max_retries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">backoff_ms</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">backoff_factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">abort</span></span></code></pre></div><table tabindex="0"><thead><tr><th>输入键</th><th>目标字段</th><th>默认值</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>enabled</code></td><td><code>true</code></td></tr><tr><td><code>max_total_attempts</code></td><td><code>maxTotalAttempts</code></td><td><code>10</code></td></tr><tr><td><code>persist_state</code></td><td><code>persistState</code></td><td><code>true</code></td></tr><tr><td><code>collect_metrics</code></td><td><code>collectMetrics</code></td><td><code>true</code></td></tr><tr><td>以错误类别为键的块（<code>chain</code> + <code>enabled</code>）</td><td><code>chains[category]</code></td><td>无（回落到默认链）</td></tr></tbody></table><p>解析规则有三条值得注意：</p><ol><li>某个类别解析出<strong>非空</strong> <code>chain</code> 时，它完全覆盖该类别，不再与默认链合并；<code>chain</code> 缺失或过滤后为空（例如所有策略名都不认识）时回落到默认链。</li><li>链中出现的策略名必须已在 <code>KNOWN_STRATEGIES</code> 中；未知策略名会记警告并跳过该步骤。</li><li>第二个参数 <code>roleDefaults</code> 会用整块对象覆盖解析结果，但当前生产调用点只传一个参数，因此角色覆盖实际走的是「声明即替换」。</li></ol><p>完整的角色面字段说明见 <a href="/rolebox-docs/03-Reference/role-yaml.html">role.yaml 参考</a>。</p><h2 id="_7-状态持久化" tabindex="-1">7. 状态持久化 <a class="header-anchor" href="#_7-状态持久化" aria-label="Permalink to &quot;7. 状态持久化&quot;">​</a></h2><p><code>RecoveryStateStore</code> 把每会话状态写到 <code>.rolebox/state/recovery-{sessionID}.json</code>，目录由 <code>stateDirFor(workspaceDir)</code> 拼出（<code>src/utils/state-paths.ts</code>）。</p><table tabindex="0"><thead><tr><th>机制</th><th>说明</th></tr></thead><tbody><tr><td>原子写入</td><td>异步路径先写临时文件再 <code>renameSync()</code>；同步路径走 <code>atomicWriteSync()</code></td></tr><tr><td>写代次保护</td><td>每次写入意图递增 <code>writeGen</code>，被取代的在途异步写在 rename 前中止，旧内容不会覆盖新内容</td></tr><tr><td>脏映射</td><td>跟踪待写状态，<code>flushSync()</code> 一次性落盘</td></tr><tr><td>尝试去重</td><td><code>recordAttempt()</code> 按 <code>timestamp + strategy</code> 去重，防止重启后重复计数</td></tr><tr><td>清理</td><td>恢复成功后 <code>delete()</code> 移除状态文件，并递增写代次以阻止在途异步写把它重建出来</td></tr></tbody></table><p>状态结构包含 <code>sessionID</code>、全部恢复尝试记录 <code>attempts</code>、当前活跃链 <code>activeChains</code>（<code>currentStep</code> / <code>startTime</code> / <code>totalAttempts</code>）与累计指标快照 <code>metrics</code>。</p><h2 id="_8-指标" tabindex="-1">8. 指标 <a class="header-anchor" href="#_8-指标" aria-label="Permalink to &quot;8. 指标&quot;">​</a></h2><p><code>RecoveryMetricsCollector</code> 记录的快照字段为：<code>totalAttempts</code>、<code>successfulRecoveries</code>、<code>abortedChains</code>、<code>exhaustedChains</code>、<code>byCategory</code>（每类别的 attempts / successes）、<code>byStrategy</code>（每策略的 attempts / successes）、<code>errorTypeFrequency</code>（错误类型出现次数）。</p><p>记录点有三个：检测到错误类型时记 <code>errorTypeFrequency</code>，每次策略执行后记一次尝试，链结束时记一次结局。</p><h2 id="_9-错误类别" tabindex="-1">9. 错误类别 <a class="header-anchor" href="#_9-错误类别" aria-label="Permalink to &quot;9. 错误类别&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类别</th><th>含义</th><th>默认链</th></tr></thead><tbody><tr><td><code>session_error</code></td><td>会话遇到不可恢复的失败（超时、崩溃、意外终止）</td><td>有</td></tr><tr><td><code>context_window</code></td><td>上下文窗口已满或接近已满</td><td>有</td></tr><tr><td><code>edit_error</code></td><td>文件编辑失败（写入冲突、内容过期、权限拒绝）</td><td>有</td></tr><tr><td><code>json_error</code></td><td>JSON 解析错误</td><td>有</td></tr><tr><td><code>empty_response</code></td><td>工具调用或模型响应为空</td><td>有</td></tr><tr><td><code>tool_pair</code></td><td>配对的工具调用失败（如 read 成功但 write 失败）</td><td>无</td></tr><tr><td><code>guard_violation</code></td><td>安全或验证护栏被触发</td><td>无</td></tr></tbody></table><h2 id="_10-调度集成" tabindex="-1">10. 调度集成 <a class="header-anchor" href="#_10-调度集成" aria-label="Permalink to &quot;10. 调度集成&quot;">​</a></h2><p>恢复系统与调度系统共享 <code>.rolebox/state/</code> 目录，并在两点上衔接：子代理调度失败时可以利用 <code>session_error</code> 链的 <code>retry</code> 先手重试；<code>maxTotalAttempts</code> 是跨策略、跨步骤的全局闸门，因此一次故障无论命中多少模式都不会无限重试。</p><h2 id="_11-扩展-api" tabindex="-1">11. 扩展 API <a class="header-anchor" href="#_11-扩展-api" aria-label="Permalink to &quot;11. 扩展 API&quot;">​</a></h2><h3 id="声明式注册" tabindex="-1">声明式注册 <a class="header-anchor" href="#声明式注册" aria-label="Permalink to &quot;声明式注册&quot;">​</a></h3><p>恢复策略与错误模式各有一个扩展作用域：<code>recovery_strategies</code> 与 <code>recovery_patterns</code>（两者都是 <code>ExtensionScope</code> 的合法取值，共 7 个作用域）。模块合约是：</p><table tabindex="0"><thead><tr><th>作用域</th><th>模块必须导出</th><th>说明</th></tr></thead><tbody><tr><td><code>recovery_strategies</code></td><td><code>name</code> + <code>execute</code></td><td>加载时调用 <code>addKnownStrategy(name)</code>，让配置解析接受该策略名</td></tr><tr><td><code>recovery_patterns</code></td><td><code>name</code> + <code>match</code></td><td>按 <code>name</code> 登记模块，类别由模块自身返回的错误对象决定</td></tr></tbody></table><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ext/my-recovery.js —— recovery_strategies 模块</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my_custom_strategy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ctx.attempt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;next_strategy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;max attempts reached&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">inject</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[CUSTOM] Executing custom recovery...</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;retry&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, delayMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;retrying&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>扩展点只负责加载与登记，真正把模块注册进引擎的是 <code>ExtensionService.init()</code>：它在初始化时遍历已加载模块，调用 <code>recoveryEngine.registerStrategy()</code> 或 <code>registerErrorPattern()</code>。声明语法与安全约束见<a href="/rolebox-docs/03-Reference/extensions.html">扩展机制</a>。</p><h3 id="命令式注册" tabindex="-1">命令式注册 <a class="header-anchor" href="#命令式注册" aria-label="Permalink to &quot;命令式注册&quot;">​</a></h3><p>已经拿到 <code>RecoveryEngine</code> 实例的代码可以直接注册，<code>registerStrategy()</code> 会同时把策略名加入已知集合：</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">engine.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerStrategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(myStrategy);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">engine.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerErrorPattern</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(myPattern);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> metrics</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> engine.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getMetrics</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> patterns</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> engine.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getPatternRegistry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> strategies</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> engine.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getStrategyRegistry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p><code>registerErrorPattern()</code> 接收的 <code>match()</code> 需返回完整的 <code>RecoveryError</code>（<code>category</code>、<code>errorType</code>、<code>message</code>、<code>raw</code>、<code>timestamp</code>），或返回 <code>null</code> 表示不匹配。</p><h2 id="_12-平台差异" tabindex="-1">12. 平台差异 <a class="header-anchor" href="#_12-平台差异" aria-label="Permalink to &quot;12. 平台差异&quot;">​</a></h2><p>恢复引擎只在 opencode 组合根中装配：<code>createPluginHooks()</code> 注册 <code>RecoveryService</code>（<code>src/core/composition.ts</code>），由它实例化 <code>RecoveryEngine</code> 与内置 Hook 注册表（<code>src/core/services/recovery-service.ts</code>）。pi 与 dsh 的装载路径没有等价装配。</p><table tabindex="0"><thead><tr><th>能力</th><th>opencode</th><th>pi</th><th>dsh</th></tr></thead><tbody><tr><td><code>RecoveryEngine</code>（策略链、状态、指标）</td><td>有</td><td>无</td><td>无</td></tr><tr><td>恢复型内置 Hook（<code>hooks.builtin</code> 的 9 个开关）</td><td>有</td><td>无（<code>HookDeps.builtInHooks</code> 有意省略）</td><td>无（<code>DshHookProvider</code> 只映射 tool-before / tool-after / chat-message）</td></tr><tr><td><code>recovery:</code> 配置块与 <code>recovery_*</code> 扩展</td><td>生效</td><td>不生效</td><td>不生效</td></tr></tbody></table><p><code>hooks.builtin</code> 的 9 个开关是主开关 <code>recovery</code> 加 5 个错误恢复 Hook（<code>session_error</code>、<code>edit_error</code>、<code>json_error</code>、<code>context_window</code>、<code>empty_response</code>）与 4 个默认关闭的护栏 Hook（<code>tool_pair_validation</code>、<code>write_existing_file_guard</code>、<code>bash_file_read_guard</code>、<code>webfetch_redirect_guard</code>）。三套 harness 的能力对照见<a href="/rolebox-docs/01-Overview/platform-harnesses.html">平台与 Harness</a>。</p><h2 id="_13-当前实现边界" tabindex="-1">13. 当前实现边界 <a class="header-anchor" href="#_13-当前实现边界" aria-label="Permalink to &quot;13. 当前实现边界&quot;">​</a></h2><ul><li><strong>只在 opencode 生效</strong>：pi 与 dsh 上 <code>hooks.recovery</code> 不产生任何效果，也没有降级提示。</li><li><strong>配置解析的第二个参数在生产路径未被使用</strong>：角色覆盖走声明即替换，不参与与默认链的深度合并。</li><li><strong><code>attempt</code> 的语义与类型注释不一致</strong>：执行器从 0 开始传值，编写自定义策略时应以 0 起计。</li><li><strong>链不是事务</strong>：每个策略的副作用（注入指令、调用模型、压缩上下文）立即生效，链被中止时不会回滚已执行的步骤。</li></ul><h2 id="相关页面" tabindex="-1">相关页面 <a class="header-anchor" href="#相关页面" aria-label="Permalink to &quot;相关页面&quot;">​</a></h2><ul><li><a href="/rolebox-docs/03-Reference/role-yaml.html">role.yaml 参考</a> — <code>hooks.recovery</code> 的字段、默认值与示例</li><li><a href="/rolebox-docs/03-Reference/error-handling.html">错误处理</a> — 用户面的降级行为与选型决策表</li><li><a href="/rolebox-docs/03-Reference/extensions.html">扩展机制</a> — <code>recovery_strategies</code> / <code>recovery_patterns</code> 的声明语法</li><li><a href="/rolebox-docs/03-Reference/hooks.html">Hook 机制</a> — 恢复型内置 Hook 与护栏 Hook</li><li><a href="/rolebox-docs/01-Overview/platform-harnesses.html">平台与 Harness</a> — 恢复引擎只在 opencode 装配</li></ul>`,65))])}const v=i(p,[["render",k]]);export{_ as __pageData,v as default};
