import{_ as e,o as a,c as i,a2 as t}from"./chunks/framework.CuUVYbLV.js";const k=JSON.parse('{"title":"函数系统","description":"用 |name| 前缀激活函数：内置 plan/execute/loop、按角色声明与合并语义、参数化调用，以及激活失败的排查方法","frontmatter":{"title":"函数系统","description":"用 |name| 前缀激活函数：内置 plan/execute/loop、按角色声明与合并语义、参数化调用，以及激活失败的排查方法"},"headers":[],"relativePath":"02-Guide/functions.md","filePath":"02-Guide/functions.md"}'),o={name:"02-Guide/functions.md"};function n(l,s,d,p,c,h){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="函数系统-function-system" tabindex="-1">函数系统（Function System） <a class="header-anchor" href="#函数系统-function-system" aria-label="Permalink to &quot;函数系统（Function System）&quot;">​</a></h1><p>函数（Function）是角色可组合的行为模块——一段按需注入系统提示的指令。你在消息的行首用 <code>|名称|</code> 前缀（激活语法）激活它，激活后的指令在整个会话中持续生效。本页讲怎么用：激活语法、内置函数、按角色声明、参数化调用与 <code>|loop|</code> 迭代；字段与条件的完整规范见<a href="/rolebox-docs/02-Guide/writing-functions.html">编写函数</a>。</p><blockquote><p>前置：<a href="/rolebox-docs/02-Guide/create-a-role.html">创建角色</a>｜相关：<a href="/rolebox-docs/02-Guide/writing-functions.html">编写函数</a>、<a href="/rolebox-docs/02-Guide/skills.html">技能系统</a></p></blockquote><h2 id="最小示例" tabindex="-1">最小示例 <a class="header-anchor" href="#最小示例" aria-label="Permalink to &quot;最小示例&quot;">​</a></h2><p>在消息的行首写 <code>|plan|</code>，后面的文本就是这条消息：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|plan| 重新设计认证模块</span></span></code></pre></div><p>应看到：解析器剥离 <code>|plan|</code> 前缀，代理进入规划模式——先调查代码库、给出计划并等你确认，而不是直接改代码。</p><p>前缀只影响激活，不改变消息内容：上面的消息对代理来说就是「重新设计认证模块」。</p><h2 id="激活语法" tabindex="-1">激活语法 <a class="header-anchor" href="#激活语法" aria-label="Permalink to &quot;激活语法&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|plan| 重新设计认证模块                    # 激活一个函数</span></span>
<span class="line"><span>|plan|execute| 为 API 添加分页功能          # 一次激活多个</span></span>
<span class="line"><span>|review:security,strict| 检查认证模块       # 带参数（见「参数化函数」）</span></span></code></pre></div><ul><li>前缀写在<strong>行首</strong>；句中出现的 <code>|…|</code> 不会被识别。</li><li>函数名只允许小写字母、数字和连字符，例如 <code>plan</code>、<code>security-scan</code>。</li><li>多个函数可以连写（<code>|a|b|</code>），也可以写成 <code>|a||b|</code>。</li><li>活跃函数是<strong>会话级集合</strong>：同一个函数重复激活不会叠加；新会话不继承上一个会话的活跃函数。</li><li>角色自定义函数要先在 <code>role.yaml</code> 声明才能激活；三个内置函数始终可激活。</li></ul><h2 id="内置函数" tabindex="-1">内置函数 <a class="header-anchor" href="#内置函数" aria-label="Permalink to &quot;内置函数&quot;">​</a></h2><p>每个角色都有三个内置函数：<code>plan</code>、<code>execute</code>、<code>loop</code>。</p><h3 id="plan-先调查-后计划" tabindex="-1">plan：先调查，后计划 <a class="header-anchor" href="#plan-先调查-后计划" aria-label="Permalink to &quot;plan：先调查，后计划&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|plan| 把用户模块从 REST 迁移到 GraphQL</span></span></code></pre></div><p>代理进入规划模式：用工具调查现状，先给一份带关键决策点的草稿，等你确认后再落一份可执行计划。它不会在计划阶段改代码；计划被批准后，<code>plan</code> 自动激活 <code>execute</code> 并把自己停用，所以通常只需要激活 <code>|plan|</code>。</p><h3 id="execute-按计划逐步实现" tabindex="-1">execute：按计划逐步实现 <a class="header-anchor" href="#execute-按计划逐步实现" aria-label="Permalink to &quot;execute：按计划逐步实现&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|execute| 按计划改造用户模块</span></span></code></pre></div><p>代理进入执行模式，按计划逐步实现，每一步之后用 LSP 诊断与测试验证，直到所有步骤完成。它消费 <code>plan</code> 产出的<strong>制品（artifact，函数产出并被其他函数消费的内容）</strong>——计划制品存在时会一并注入提示。</p><h3 id="loop-多轮迭代" tabindex="-1">loop：多轮迭代 <a class="header-anchor" href="#loop-多轮迭代" aria-label="Permalink to &quot;loop：多轮迭代&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|loop:3| 调研 rollout 失败的原因并给出结论</span></span></code></pre></div><p>代理只做<strong>编排器（orchestrator，负责派发与汇总、不亲自执行任务的会话角色）</strong>：把任务按轮派发到独立的 worker 会话，每轮结束后给出一段面向用户的摘要。参数与模式见下一节。</p><h2 id="用-loop-迭代执行" tabindex="-1">用 <code>|loop|</code> 迭代执行 <a class="header-anchor" href="#用-loop-迭代执行" aria-label="Permalink to &quot;用 \`|loop|\` 迭代执行&quot;">​</a></h2><p><code>|loop|</code> 把同一个任务重复运行 N 轮，并把上一轮的统一摘要作为下一轮的种子上下文。它适合需要反复打磨的任务：多轮调研、迭代改进、独立评估。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|loop:3| 调研 dispatch 通知机制，列出所有通知类型和触发条件</span></span></code></pre></div><p>应看到（<code>&lt;session&gt;</code> 与耗时随环境不同，此处为示例输出）：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[loop-progress loop started: 3 rounds, inherit mode]</span></span>
<span class="line"><span>[loop-progress round 1/3 completed, session=&lt;session&gt;, duration=12.5s]</span></span>
<span class="line"><span>[loop-progress round 2/3 completed, session=&lt;session&gt;, duration=10.2s]</span></span>
<span class="line"><span>[loop-progress round 3/3 completed, session=&lt;session&gt;, duration=8.1s]</span></span>
<span class="line"><span>[loop-progress loop complete]</span></span></code></pre></div><p>参数写在 <code>|loop:</code> 之后，用逗号分隔：</p><table tabindex="0"><thead><tr><th>参数</th><th>位置</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td>轮数</td><td>第 1 个</td><td><code>5</code></td><td>取值范围 1–50；超过 50 会钳位到 50 并追加提示</td></tr><tr><td>模式</td><td>第 2 个</td><td><code>inherit</code></td><td><code>inherit</code> 把上一轮摘要作为下一轮的种子上下文；<code>fresh</code> 每轮从零开始</td></tr></tbody></table><p>模式别名：<code>inherit</code> 也可写 <code>on</code>、<code>true</code>；<code>fresh</code> 也可写 <code>no-inherit</code>、<code>off</code>、<code>false</code>。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|loop| 重构 utils 模块            # 5 轮，inherit</span></span>
<span class="line"><span>|loop:10,fresh| 生成示例           # 10 轮，fresh</span></span>
<span class="line"><span>|loop:3,off| 运行测试套件          # 3 轮，fresh（别名）</span></span></code></pre></div><p>要按名字传参，用空格语法，而不是冒号语法：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|loop iterations=3 mode=fresh| 从头重复实验</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">冒号语法里 <code>mode=</code> 不会生效</p><p><code>|loop:3,mode=fresh|</code> 会把 <code>mode=fresh</code> 当成第 2 个位置参数的值；它不是合法模式，于是回退到默认的 <code>inherit</code>。要指定模式，写 <code>|loop:3,fresh|</code>，或用空格语法 <code>|loop iterations=3 mode=fresh|</code>。</p></div><p>轮数越界不会报错：<code>|loop:100|</code> 会钳位到 50 轮并追加一条 <code>Loop: 50 iterations (clamped to 50)</code> 提示；<code>|loop:0|</code> 则直接无效，循环不会启动。</p><p>取消：循环运行期间发送 <code>/stop-loop</code> 即可取消，已完成轮次的摘要保留在会话中。普通用户消息不会打断运行中的轮次。</p><p>边界：本页只讲 <code>|loop|</code> 的使用。轮次调度、状态机、持久化与恢复的实现见<a href="/rolebox-docs/04-Advanced/loop-system.html">循环系统</a>；6 个 <code>loop_*</code> 编程接口工具的参数见<a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a>。</p><h2 id="按角色声明函数" tabindex="-1">按角色声明函数 <a class="header-anchor" href="#按角色声明函数" aria-label="Permalink to &quot;按角色声明函数&quot;">​</a></h2><p>自定义函数要在 <code>role.yaml</code> 里声明，才能用 <code>|名称|</code> 激活：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># role.yaml</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">functions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">review</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # 自定义函数</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">security-scan</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">disable_functions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">execute</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # 从最终集合中移除内置的 execute</span></span></code></pre></div><p><code>functions:</code> 是<strong>合并</strong>语义：</p><ul><li>与内置默认值 <code>[plan, execute, loop]</code> 求并集并去重——只写 <code>functions: [review]</code> 时，最终集合是 <code>[plan, execute, loop, review]</code>。</li><li><code>disable_functions:</code> 从合并结果中移除指定函数；这是移除内置函数的唯一方式。</li><li>两个字段都不写时，最终集合就是内置默认值。</li><li>子代理的 <code>functions:</code> / <code>disable_functions:</code> 用同一条规则合并进它自己的配置。</li></ul><blockquote><p>自 v0.23.0 起，<code>functions:</code> 与内置默认值合并而不是替换；要移除内置函数请用 <code>disable_functions</code>。</p></blockquote><p>函数文件放在哪里、角色本地定义与全局定义谁覆盖谁，见<a href="/rolebox-docs/02-Guide/writing-functions.html">编写函数</a>。</p><h2 id="参数化函数" tabindex="-1">参数化函数 <a class="header-anchor" href="#参数化函数" aria-label="Permalink to &quot;参数化函数&quot;">​</a></h2><p>在函数文件的 frontmatter 里用 <code>params</code> 声明参数名与默认值：</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">review</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">description</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">按指定重点与严格度评审代码</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  focus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">correctness</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  severity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">normal</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">以 </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**{focus}**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 为重点、按 </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**{severity}**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 严格度评审代码。</span></span></code></pre></div><p>两种传参写法：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|review:security,strict| 检查认证模块                 # 位置参数：按 params 声明顺序</span></span>
<span class="line"><span>|review focus=security severity=strict| 检查认证模块   # 键值参数：按名字</span></span></code></pre></div><ul><li>位置参数按 <code>params</code> 的声明顺序映射；键值参数按名字映射；两者可以混用，例如 <code>|plan|review:security| 分析这个 PR</code>。</li><li>未传的参数回退到声明的默认值。</li><li>函数没有 <code>params</code> 块时，传入的参数被忽略。</li><li>冒号语法只接受逗号分隔的位置值；键值语法要求参数与函数名之间是空格。</li></ul><h2 id="调试激活失败" tabindex="-1">调试激活失败 <a class="header-anchor" href="#调试激活失败" aria-label="Permalink to &quot;调试激活失败&quot;">​</a></h2><h3 id="_1-确认函数在最终集合里" tabindex="-1">1. 确认函数在最终集合里 <a class="header-anchor" href="#_1-确认函数在最终集合里" aria-label="Permalink to &quot;1. 确认函数在最终集合里&quot;">​</a></h3><p>对已安装的角色运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rolebox</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> info</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-role</span></span></code></pre></div><p>应看到（示例输出，函数名随角色不同）：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Functions (4)</span></span>
<span class="line"><span>    plan, execute, loop, review</span></span></code></pre></div><p>这里列出的是角色<strong>声明</strong>的函数；没有声明 <code>functions:</code> 时显示内置默认值。目标函数不在列表里，说明它既不是内置函数也没有在 <code>role.yaml</code> 声明。注意：<code>rolebox info</code> 只认 <code>rolebox install</code> 安装的角色；用 <code>rolebox init</code> 在本地创建的角色不在安装锁里，直接看它的 <code>role.yaml</code> 即可。</p><h3 id="_2-排除-disable-functions" tabindex="-1">2. 排除 disable_functions <a class="header-anchor" href="#_2-排除-disable-functions" aria-label="Permalink to &quot;2. 排除 disable_functions&quot;">​</a></h3><p>被 <code>disable_functions</code> 列出的函数即使写在 <code>functions:</code> 里也会被移除。<code>rolebox info</code> 会在函数列表下方单列一行 <code>disabled:</code>。</p><h3 id="_3-检查运行时状态" tabindex="-1">3. 检查运行时状态 <a class="header-anchor" href="#_3-检查运行时状态" aria-label="Permalink to &quot;3. 检查运行时状态&quot;">​</a></h3><p>函数运行时状态按工作区持久化在 <code>.rolebox/state/fnstate-{hash}.json</code>：<code>{hash}</code> 是工作区绝对路径的 SHA-256 前 12 位。文件按会话分组，每个函数一段，重点看 <code>phase</code> 与 <code>gateSatisfied</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;analyze&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;phase&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gated&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;gateSatisfied&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>phase</code> 取 <code>active</code>、<code>gated</code>、<code>complete</code> 三者之一：<code>gated</code> 表示 <code>gate</code> 条件未满足，函数还在等条件（制品、信号或用户确认），这不是错误。</p><h3 id="_4-检查-requires-依赖" tabindex="-1">4. 检查 requires 依赖 <a class="header-anchor" href="#_4-检查-requires-依赖" aria-label="Permalink to &quot;4. 检查 requires 依赖&quot;">​</a></h3><p><code>requires</code> 与 <code>gate</code> 是两套机制：依赖未满足时函数不会被标记为 <code>gated</code>，而是指令不注入，同时收到一条「先激活依赖函数」的提醒（<code>activate required functions first</code>）。所以「函数像是没生效」也可能是依赖链没走通——确认每个 <code>requires</code> 的函数都已进入活跃集合。</p><h3 id="常见现象对照" tabindex="-1">常见现象对照 <a class="header-anchor" href="#常见现象对照" aria-label="Permalink to &quot;常见现象对照&quot;">​</a></h3><table tabindex="0"><thead><tr><th>现象</th><th>可能原因</th><th>处理</th></tr></thead><tbody><tr><td>前缀完全没有反应</td><td>函数名不在最终集合里</td><td>在 <code>role.yaml</code> 声明，或改用内置函数</td></tr><tr><td>提示中出现 <code>activate required functions first</code></td><td><code>requires</code> 依赖未激活</td><td>先激活依赖函数</td></tr><tr><td>状态文件里 <code>phase</code> 为 <code>gated</code></td><td><code>gate</code> 条件未满足</td><td>满足 gate 依赖的制品、信号或用户确认</td></tr><tr><td>声明后仍不生效</td><td>被 <code>disable_functions</code> 移除</td><td>从 <code>disable_functions</code> 中删除该项</td></tr><tr><td>参数没有生效</td><td>没有 <code>params</code> 块，或传参语法不对</td><td>补 <code>params</code>，并区分位置参数与键值参数</td></tr></tbody></table><h2 id="相关" tabindex="-1">相关 <a class="header-anchor" href="#相关" aria-label="Permalink to &quot;相关&quot;">​</a></h2><ul><li><a href="/rolebox-docs/02-Guide/writing-functions.html">编写函数</a> — frontmatter、phase、gate、transitions、observe 与条件表达式规范</li><li><a href="/rolebox-docs/02-Guide/skills.html">技能系统</a> — 按需加载的知识模块，以及它与函数的分工</li><li><a href="/rolebox-docs/02-Guide/subagents.html">子代理</a> — 函数与调度目标的协作</li><li><a href="/rolebox-docs/04-Advanced/loop-system.html">循环系统</a> — <code>|loop|</code> 的调度、状态机与恢复的内部实现</li></ul>`,69)])])}const u=e(o,[["render",n]]);export{k as __pageData,u as default};
