import{_ as n,C as l,o as e,c as p,a2 as o,b as s,w as d,a as r,E as i,a3 as c}from"./chunks/framework.CuUVYbLV.js";const b=JSON.parse('{"title":"循环系统（Loop System）","description":"LoopCoordinator 与 worker-dispatch 推链的内部实现 — 9 阶段状态机、重入保护、取消、持久化恢复与可观测性","frontmatter":{"title":"循环系统（Loop System）","description":"LoopCoordinator 与 worker-dispatch 推链的内部实现 — 9 阶段状态机、重入保护、取消、持久化恢复与可观测性"},"headers":[],"relativePath":"04-Advanced/loop-system.md","filePath":"04-Advanced/loop-system.md"}'),h={name:"04-Advanced/loop-system.md"};function k(E,t,A,g,_,m){const a=l("Mermaid");return e(),p("div",null,[t[3]||(t[3]=o('<h1 id="循环系统-loop-system" tabindex="-1">循环系统（Loop System） <a class="header-anchor" href="#循环系统-loop-system" aria-label="Permalink to &quot;循环系统（Loop System）&quot;">​</a></h1><p>循环系统让代理自主执行多轮迭代任务：每一轮把上一轮的摘要作为上下文，派发到隔离的 worker 会话。实现位于 <code>src/loop/</code>，由 <code>LoopCoordinator</code> 驱动推链（push-chain）调度，<code>DispatchAdapter</code> 桥接 dispatch 子系统，<code>LoopStore</code> 负责重启恢复。</p><blockquote><p><strong>本页的边界</strong>：本页只讲引擎内部实现。<code>|loop|</code> 的使用者用法（激活语法、配方、何时该用 fresh 模式）见<a href="/rolebox-docs/02-Guide/functions.html">函数系统</a>；<code>loop_*</code> 工具的参数与返回格式见<a href="/rolebox-docs/03-Reference/tool-catalog.html">工具目录</a>；用图引擎编排多代理任务见<a href="/rolebox-docs/02-Guide/graph-workflows.html">图工作流</a>。</p></blockquote><blockquote><p>自 v0.23.0 起，循环推进改为 <code>onTaskTerminated</code> 事件驱动的推链模型（此前依赖轮询空闲检测）；<code>|loop|</code> 函数自 v0.14.0 引入。</p></blockquote><h2 id="_1-组件构成" tabindex="-1">1. 组件构成 <a class="header-anchor" href="#_1-组件构成" aria-label="Permalink to &quot;1. 组件构成&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>模块</th><th>职责</th></tr></thead><tbody><tr><td><code>LoopCoordinator</code></td><td><code>src/loop/coordinator.ts</code></td><td>循环注册、推链推进、重入保护、取消、重启后重新订阅</td></tr><tr><td>worker 派发</td><td><code>src/loop/worker-dispatch.ts</code></td><td><code>dispatchRound()</code> / <code>handleSummary()</code> / <code>finalizeLoop()</code> / <code>failLoop()</code></td></tr><tr><td><code>LoopStore</code></td><td><code>src/loop/loop-store.ts</code></td><td>状态持久化（防抖写入）与重启对账</td></tr><tr><td><code>DispatchAdapter</code></td><td><code>src/loop/dispatch-adapter.ts</code></td><td>把 <code>DispatchManager</code> 适配成循环系统需要的 9 个方法</td></tr><tr><td>参数解析</td><td><code>src/loop/params.ts</code></td><td>解析 <code>|loop:N,mode|</code> 前缀，钳位越界轮数</td></tr><tr><td>取消判定</td><td><code>src/loop/cancellation.ts</code></td><td>判定一条消息是否构成取消信号</td></tr><tr><td>工具装配</td><td><code>src/loop/loop-tools.ts</code></td><td>创建 6 个 <code>loop_*</code> 工具</td></tr><tr><td>常量</td><td><code>src/loop/constants.ts</code></td><td>轮数、超时、延迟、锁与 schema 版本常量</td></tr></tbody></table>',6)),(e(),s(c,null,{default:d(()=>[i(a,{id:"mermaid-124",class:"mermaid",graph:"graph%20TB%0A%20%20subgraph%20Core%5B%E6%A0%B8%E5%BF%83%E5%B1%82%5D%0A%20%20%20%20COORD%5BLoopCoordinator%5D%0A%20%20%20%20STORE%5BLoopStore%5D%0A%20%20%20%20PARAMS%5Bparams%5D%0A%20%20%20%20CANCEL%5Bcancellation%5D%0A%20%20end%0A%20%20subgraph%20Bridge%5B%E6%A1%A5%E6%8E%A5%E5%B1%82%5D%0A%20%20%20%20ADAPTER%5BDispatchAdapter%5D%0A%20%20end%0A%20%20subgraph%20Dispatch%5B%E8%B0%83%E5%BA%A6%E5%B1%82%5D%0A%20%20%20%20DM%5BDispatchManager%5D%0A%20%20%20%20W%5BWorker%20Session%201..N%5D%0A%20%20end%0A%20%20LOOP%5B%22%7Cloop%3AN%7C%20%E4%BB%BB%E5%8A%A1%22%5D%20--%3E%20COORD%0A%20%20COORD%20--%3E%20ADAPTER%0A%20%20ADAPTER%20--%3E%20DM%0A%20%20DM%20--%3E%20W%0A%20%20COORD%20--%3E%20STORE%0A%20%20COORD%20--%3E%20PARAMS%0A%20%20COORD%20--%3E%20CANCEL%0A%20%20STORE%20-.-%3E%7Cpersist%20%2F%20reconcile%7C%20FILE%5B.rolebox%2Fstate%2Floops-%7BdirHash%7D.json%5D%0A"})]),fallback:d(()=>[...t[0]||(t[0]=[r(" Loading... ",-1)])]),_:1})),t[4]||(t[4]=o(`<h2 id="_2-loop-的解析与上下文拼接" tabindex="-1">2. <code>|loop|</code> 的解析与上下文拼接 <a class="header-anchor" href="#_2-loop-的解析与上下文拼接" aria-label="Permalink to &quot;2. \`|loop|\` 的解析与上下文拼接&quot;">​</a></h2><p><code>LOOP_FUNCTION_NAME</code> 定义入口函数名 <code>loop</code>。参数由 <code>parseLoopParams()</code> 解析，同时接受位置参数与命名参数：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>|loop:3| 分析这三个提案</span></span>
<span class="line"><span>|loop:5,fresh| 从头开始重复实验</span></span>
<span class="line"><span>|loop:3,mode=fresh| 显式指定模式</span></span>
<span class="line"><span>|loop:iterations=3,mode=inherit| 完全命名形式</span></span></code></pre></div><table tabindex="0"><thead><tr><th>参数</th><th>位置 / 命名</th><th>类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td>轮数</td><td><code>_0</code> / <code>iterations</code></td><td><code>number</code></td><td><code>5</code></td><td>最小值 1；超过硬上限 50 时<strong>静默钳位</strong>并置 <code>clamped</code> 标志</td></tr><tr><td>模式</td><td><code>_1</code> / <code>mode</code></td><td><code>inherit</code> | <code>fresh</code></td><td><code>inherit</code></td><td>别名：<code>on</code>/<code>true</code> → <code>inherit</code>，<code>no-inherit</code>/<code>off</code>/<code>false</code> → <code>fresh</code></td></tr></tbody></table><p>钳位不抛错：<code>|loop:100|</code> 会静默按 50 轮运行，调用方只能通过解析结果的 <code>clamped</code> 字段或轮次记录发现。</p><p><code>inherit</code> 模式的种子拼接在 <code>dispatchRound()</code> 中完成：取上一轮统一摘要的<strong>末尾</strong> <code>SEED_CHAR_CAP</code>（8000）字符，以 <code>---</code> 分隔符前置到原始任务之前。</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 结构示意</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> prompt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loop.basePrompt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (loop.mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;inherit&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loop.lastSummary) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> seed</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loop.lastSummary.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> SEED_CHAR_CAP</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loop.lastSummary.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">SEED_CHAR_CAP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    :</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loop.lastSummary;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  prompt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> seed </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">---</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loop.basePrompt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="_3-生命周期状态机" tabindex="-1">3. 生命周期状态机 <a class="header-anchor" href="#_3-生命周期状态机" aria-label="Permalink to &quot;3. 生命周期状态机&quot;">​</a></h2><p><code>LoopPhase</code> 是 9 阶段状态机，其中 4 个是终态：</p>`,9)),(e(),s(c,null,{default:d(()=>[i(a,{id:"mermaid-202",class:"mermaid",graph:"stateDiagram-v2%0A%20%20%20%20%5B*%5D%20--%3E%20activating%3A%20register()%0A%20%20%20%20activating%20--%3E%20dispatching%3A%20_kickoffFromActivating%0A%20%20%20%20dispatching%20--%3E%20awaiting_worker%3A%20dispatchRound()%0A%20%20%20%20awaiting_worker%20--%3E%20summarizing%3A%20onWorkerCompleted%0A%20%20%20%20summarizing%20--%3E%20dispatching%3A%20%E8%BF%98%E6%9C%89%E5%89%A9%E4%BD%99%E8%BD%AE%E6%AC%A1%0A%20%20%20%20summarizing%20--%3E%20finalizing%3A%20%E6%9C%80%E5%90%8E%E4%B8%80%E8%BD%AE%E5%AE%8C%E6%88%90%20%2F%20cancelRequested%0A%20%20%20%20finalizing%20--%3E%20complete%3A%20finalizeLoop()%0A%20%20%20%20finalizing%20--%3E%20cancelled%3A%20finalizeLoop(cancelled)%0A%20%20%20%20summarizing%20--%3E%20error%3A%20failLoop()%0A%20%20%20%20awaiting_worker%20--%3E%20error%3A%20worker%20%E5%A4%B1%E8%B4%A5%0A%20%20%20%20dispatching%20--%3E%20error%3A%20%E6%B4%BE%E5%8F%91%E5%A4%B1%E8%B4%A5%0A%20%20%20%20awaiting_worker%20--%3E%20interrupted%3A%20%E9%87%8D%E5%90%AF%E6%81%A2%E5%A4%8D%0A%20%20%20%20complete%20--%3E%20%5B*%5D%0A%20%20%20%20cancelled%20--%3E%20%5B*%5D%0A%20%20%20%20interrupted%20--%3E%20%5B*%5D%0A%20%20%20%20error%20--%3E%20%5B*%5D%0A"})]),fallback:d(()=>[...t[1]||(t[1]=[r(" Loading... ",-1)])]),_:1})),t[5]||(t[5]=o('<table tabindex="0"><thead><tr><th>阶段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>activating</code></td><td>活跃</td><td>初始化，准备第一轮</td></tr><tr><td><code>dispatching</code></td><td>活跃</td><td>正在把一轮派发给 <code>DispatchManager</code></td></tr><tr><td><code>awaiting_worker</code></td><td>活跃</td><td>等待 worker 任务结束</td></tr><tr><td><code>summarizing</code></td><td>活跃</td><td>读取 origin 摘要，推进轮次计数</td></tr><tr><td><code>finalizing</code></td><td>过渡</td><td>收尾：取消在途 worker、注入完成笔记</td></tr><tr><td><code>complete</code></td><td>终态</td><td>全部迭代成功结束</td></tr><tr><td><code>cancelled</code></td><td>终态</td><td>被显式取消</td></tr><tr><td><code>interrupted</code></td><td>终态</td><td>会话超时或重启丢失上下文</td></tr><tr><td><code>error</code></td><td>终态</td><td>不可恢复的错误</td></tr></tbody></table><h2 id="_4-推链调度" tabindex="-1">4. 推链调度 <a class="header-anchor" href="#_4-推链调度" aria-label="Permalink to &quot;4. 推链调度&quot;">​</a></h2><p>循环不轮询：每一轮结束时 dispatch 层发出任务终止事件，<code>DispatchAdapter</code> 把它转成 <code>onWorkerCompleted()</code>，协调器在同一临界区内完成「取结果 → 汇总 → 派发下一轮」，形成自驱链条。</p>',3)),(e(),s(c,null,{default:d(()=>[i(a,{id:"mermaid-325",class:"mermaid",graph:"sequenceDiagram%0A%20%20%20%20participant%20LC%20as%20LoopCoordinator%0A%20%20%20%20participant%20DA%20as%20DispatchAdapter%0A%20%20%20%20participant%20DM%20as%20DispatchManager%0A%20%20%20%20participant%20W%20as%20Worker%20Session%0A%20%20%20%20LC-%3E%3EDA%3A%20dispatchRound()%0A%20%20%20%20DA-%3E%3EDM%3A%20launch(task)%0A%20%20%20%20DM--%3E%3EDA%3A%20taskId%20%2F%20sessionId%0A%20%20%20%20LC-%3E%3EDA%3A%20registerTerminatedListener(taskId)%0A%20%20%20%20DM-%3E%3EDA%3A%20onTaskTerminated(taskId%2C%20status)%0A%20%20%20%20DA-%3E%3ELC%3A%20onWorkerCompleted(taskId)%0A%20%20%20%20LC-%3E%3EDA%3A%20getRoundResult(taskId)%0A%20%20%20%20LC-%3E%3ELC%3A%20summarizing%20%E2%86%92%20_advanceFromSummarizing%0A%20%20%20%20LC-%3E%3EDA%3A%20dispatchRound()%EF%BC%88%E4%B8%8B%E4%B8%80%E8%BD%AE%EF%BC%89%0A"})]),fallback:d(()=>[...t[2]||(t[2]=[r(" Loading... ",-1)])]),_:1})),t[6]||(t[6]=o(`<p><code>onOriginIdle()</code> 保留为兼容钩子，其内部 switch 已经为空——origin 会话的空闲事件不再推进任何阶段。</p><h3 id="重入保护" tabindex="-1">重入保护 <a class="header-anchor" href="#重入保护" aria-label="Permalink to &quot;重入保护&quot;">​</a></h3><p><code>_advancing</code> 映射保证同一 origin 会话同时只有一个推进操作：</p><ol><li><strong>显式加锁</strong>：推进前写入 <code>sessionId → 时间戳</code>。</li><li><strong>暂停完成通知</strong>：锁被持有时，<code>onWorkerCompleted()</code> 把任务 ID 放进 <code>_pendingCompletions</code> 队列。</li><li><strong>停滞锁扫描</strong>：每 15 秒扫描一次，超过 30 秒的锁被强制释放并排空队列。</li><li><strong>finally 排空</strong>：每次释放锁后处理队列中积压的完成事件。</li></ol><h3 id="dispatchadapter-的-9-个方法" tabindex="-1">DispatchAdapter 的 9 个方法 <a class="header-anchor" href="#dispatchadapter-的-9-个方法" aria-label="Permalink to &quot;DispatchAdapter 的 9 个方法&quot;">​</a></h3><table tabindex="0"><thead><tr><th>方法</th><th>用途</th></tr></thead><tbody><tr><td><code>dispatchRound()</code></td><td>通过 <code>DispatchManager</code> 提交一轮 worker 任务</td></tr><tr><td><code>getRoundResult()</code></td><td>取得已完成轮次的结果</td></tr><tr><td><code>cancelRound()</code></td><td>取消在运行的 worker 轮</td></tr><tr><td><code>readOriginSummary()</code></td><td>读取 origin 会话最新的助手输出</td></tr><tr><td><code>getLastMessageId()</code></td><td>取会话最后一条消息 ID</td></tr><tr><td><code>injectNote()</code></td><td>向 origin 会话写入静默进度笔记</td></tr><tr><td><code>registerTerminatedListener()</code></td><td>注册 worker 终止的一次性回调</td></tr><tr><td><code>removeTerminatedListener()</code></td><td>移除已注册回调</td></tr><tr><td><code>getTaskStatus()</code></td><td>查询 dispatch 任务存活状态</td></tr></tbody></table><p>每轮任务都以 <code>run_in_background: true</code> 和 <code>noParentInherit: true</code> 提交，因此 worker 不会继承父会话上下文——上下文只通过 §2 的 seed 拼接传递。</p><h2 id="_5-取消" tabindex="-1">5. 取消 <a class="header-anchor" href="#_5-取消" aria-label="Permalink to &quot;5. 取消&quot;">​</a></h2><p>取消只由显式命令触发：<code>/stop-loop</code> 处理器把 <code>STOP_LOOP_SIGNAL</code>（<code>[rolebox:stop-loop]</code>）注入消息文本，<code>shouldCancelLoop()</code> 据此判定。</p><table tabindex="0"><thead><tr><th>条件</th><th>行为</th></tr></thead><tbody><tr><td>消息不含 <code>STOP_LOOP_SIGNAL</code></td><td>不取消（普通消息不再中断循环）</td></tr><tr><td>阶段为终态</td><td>不取消</td></tr><tr><td>阶段为 <code>activating</code> / <code>summarizing</code> / <code>finalizing</code></td><td>不取消（origin 自持相位）</td></tr><tr><td>阶段为 <code>awaiting_worker</code> 或 <code>dispatching</code></td><td><strong>取消</strong></td></tr></tbody></table><p>协调器提供两个取消入口：<code>requestCancel()</code> 设置 <code>cancelRequested</code>，在下次汇总时生效；<code>cancelNow()</code> 立即清理 worker 监听器并强制进入 <code>finalizing</code>。如果 origin 会话正持有 <code>_advancing</code> 锁，<code>cancelNow()</code> 会先标记再等锁释放，避免与推链竞争。</p><p>收尾时 <code>finalizeLoop()</code> 取消仍活跃的 worker、把未执行的轮次标记为 cancelled，并向 origin 会话注入包含已完成轮次摘要的笔记。</p><h2 id="_6-持久化与恢复" tabindex="-1">6. 持久化与恢复 <a class="header-anchor" href="#_6-持久化与恢复" aria-label="Permalink to &quot;6. 持久化与恢复&quot;">​</a></h2><p><code>LoopStore</code> 把状态写入 <code>&lt;工作目录&gt;/.rolebox/state/loops-{dirHash}.json</code>，<code>dirHash</code> 由工作目录短哈希得到，因此同一台机器上的多个工作区互不覆盖。</p><table tabindex="0"><thead><tr><th>机制</th><th>说明</th></tr></thead><tbody><tr><td>防抖保存</td><td><code>save()</code> 在 200 ms 窗口内合并多次调用为一次 I/O，并把所有等待者一起 resolve</td></tr><tr><td>同步保存</td><td><code>saveSync()</code> 供关键路径立即落盘</td></tr><tr><td>版本校验</td><td>只接受 1–3 的 schema 版本，1/2 自动迁移到 3</td></tr><tr><td>终态修剪</td><td><code>complete</code> / <code>cancelled</code> / <code>interrupted</code> / <code>error</code> 的循环在加载与对账时被移除</td></tr></tbody></table><h3 id="重启恢复的三个阶段" tabindex="-1">重启恢复的三个阶段 <a class="header-anchor" href="#重启恢复的三个阶段" aria-label="Permalink to &quot;重启恢复的三个阶段&quot;">​</a></h3><ol><li><strong>加载</strong>：读取 JSON，校验格式与 <code>LOOP_STATE_SCHEMA_VERSION</code>。</li><li><strong>对账</strong>：<code>reconcile()</code> 为每个非终态循环查询它的 worker 任务状态。</li><li><strong>重新订阅</strong>：<code>reSubscribeListeners()</code> 重新挂上终止监听器，把推链接回运行中的循环。</li></ol><table tabindex="0"><thead><tr><th>worker 状态</th><th>恢复动作</th></tr></thead><tbody><tr><td><code>completed</code></td><td>阶段设为 <code>summarizing</code>，立即推进汇总与下一轮</td></tr><tr><td><code>running</code> / <code>pending</code></td><td>阶段设为 <code>awaiting_worker</code>，重新注册监听器</td></tr><tr><td><code>unknown</code> / <code>error</code> / <code>cancelled</code></td><td>阶段设为 <code>interrupted</code></td></tr><tr><td>任务不存在（无 <code>activeWorkerTaskId</code>）</td><td>阶段设为 <code>interrupted</code></td></tr></tbody></table><p>重新订阅时按阶段分派：<code>summarizing</code> 走 <code>_advanceFromSummarizing()</code>，<code>activating</code> 走 <code>_kickoffFromActivating()</code>，<code>awaiting_worker</code> 且已完成走 <code>onWorkerCompleted()</code>，否则只补注册监听器。</p><h2 id="_7-配置常量" tabindex="-1">7. 配置常量 <a class="header-anchor" href="#_7-配置常量" aria-label="Permalink to &quot;7. 配置常量&quot;">​</a></h2><p>常量集中在 <code>src/loop/constants.ts</code>，没有对应的 role.yaml 字段。</p><table tabindex="0"><thead><tr><th>常量</th><th>值</th><th>说明</th></tr></thead><tbody><tr><td><code>DEFAULT_ITERATIONS</code></td><td><code>5</code></td><td>未指定时的轮数</td></tr><tr><td><code>MAX_ITERATIONS_HARD_CAP</code></td><td><code>50</code></td><td>轮数硬上限，超出静默钳位</td></tr><tr><td><code>DISPATCH_ROUND_TIMEOUT_MS</code></td><td><code>900000</code>（15 分钟）</td><td>单轮超时</td></tr><tr><td><code>INTER_ROUND_DELAY_MS</code></td><td><code>2000</code></td><td>轮间最小延迟</td></tr><tr><td><code>SUMMARY_INPUT_CHAR_CAP</code></td><td><code>8000</code></td><td>送入汇总器的单轮输出上限</td></tr><tr><td><code>SEED_CHAR_CAP</code></td><td><code>8000</code></td><td>继承模式拼进下一轮的摘要上限</td></tr><tr><td><code>LOOP_STATE_SCHEMA_VERSION</code></td><td><code>3</code></td><td>持久化 schema 版本</td></tr><tr><td><code>ADVANCING_LOCK_TIMEOUT_MS</code></td><td><code>30000</code></td><td>推进锁停滞判定阈值</td></tr><tr><td><code>SWEEPER_INTERVAL_MS</code></td><td><code>15000</code></td><td>停滞锁扫描间隔</td></tr><tr><td><code>MAX_TREE_WORKER_SESSIONS</code></td><td><code>30</code></td><td>树状 worker 循环的并发上限</td></tr><tr><td><code>CONSECUTIVE_STALE_THRESHOLD</code></td><td><code>2</code></td><td>连续无进展轮数达到该值时提前终止</td></tr></tbody></table><h2 id="_8-可观测性" tabindex="-1">8. 可观测性 <a class="header-anchor" href="#_8-可观测性" aria-label="Permalink to &quot;8. 可观测性&quot;">​</a></h2><p>循环在关键节点向 origin 会话注入带 <code>LOOP_PROGRESS_MARKER</code>（<code>[loop-progress</code>）前缀的静默笔记：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[loop-progress loop started: 3 rounds, inherit mode]</span></span>
<span class="line"><span>[loop-progress round 1/3 completed, session=abc123, duration=12.5s]</span></span>
<span class="line"><span>[loop-progress loop complete]</span></span>
<span class="line"><span>Rounds: r1:abc123(12.5s,completed), r2:def456(10.2s,completed), r3:ghi789(8.1s,completed)</span></span></code></pre></div><ul><li>各组件用 <code>createSubLogger</code> 写结构化日志，子日志名分别为 <code>loop/coordinator</code>、<code>loop/worker-dispatch</code>、<code>loop-store</code>；推链跟踪走 <code>loop-trace</code>，停滞锁事件走 <code>advancing-lock</code>。</li><li><code>getAdvancingLockState()</code> 返回 <code>{ activeLocks, staleLocks }</code>：当前持有的推进锁数量与累计清扫次数，可用于判断推链是否被卡住。</li></ul><h2 id="_9-loop-工具集" tabindex="-1">9. <code>loop_*</code> 工具集 <a class="header-anchor" href="#_9-loop-工具集" aria-label="Permalink to &quot;9. \`loop_*\` 工具集&quot;">​</a></h2><p><code>createLoopTools()</code> 一次性创建 6 个工具，它们都直接读写协调器状态，不经过图引擎：</p><table tabindex="0"><thead><tr><th>工具</th><th>用途</th></tr></thead><tbody><tr><td><code>loop_start</code></td><td>注册一个顺序多会话循环并启动首轮</td></tr><tr><td><code>loop_status</code></td><td>查询单个循环的快照；省略 <code>session_id</code> 返回聚合指标</td></tr><tr><td><code>loop_cancel</code></td><td>取消运行中的循环（接受 origin 或 worker 会话 ID）</td></tr><tr><td><code>loop_output</code></td><td>读取指定轮次的 worker 输出，支持分页</td></tr><tr><td><code>loop_history</code></td><td>逐轮执行历史（worker 会话 ID、耗时、状态）</td></tr><tr><td><code>loop_list</code></td><td>列出全部被跟踪的循环，可按阶段与 agent 过滤</td></tr></tbody></table><p>参数与返回格式见<a href="/rolebox-docs/03-Reference/tool-catalog.html">工具目录</a>。装配存在平台差异：dsh 插件直接注册这 6 个工具；opencode 的 <code>ToolService</code> 有意关闭了裸 <code>loop_*</code> 注册（注释写明是为了防止模型绕过图引擎），多代理编排统一走命令式 <code>graph_*</code> 引擎；Pi 的工具装配只在装配方显式传入 <code>loopTools</code> 时才注入，默认不注入。<code>loop_output</code> 在没有 <code>session_client</code> 时只返回循环元数据。</p><h2 id="_10-与-dispatch-子系统的关系" tabindex="-1">10. 与 dispatch 子系统的关系 <a class="header-anchor" href="#_10-与-dispatch-子系统的关系" aria-label="Permalink to &quot;10. 与 dispatch 子系统的关系&quot;">​</a></h2><p>循环建于 dispatch 之上，但两者职责不重叠：循环持有轮次语义与恢复能力，dispatch 只负责单轮任务调度。循环通过 <code>IDispatchAdapter</code> 接口访问 dispatch，不直接调用 dispatch 工具函数。</p><table tabindex="0"><thead><tr><th>维度</th><th>循环系统</th><th>dispatch 系统</th></tr></thead><tbody><tr><td>职责</td><td>多轮迭代编排</td><td>单轮任务调度</td></tr><tr><td>状态</td><td><code>LoopState</code>（9 阶段）</td><td><code>DispatchTask</code> 生命周期</td></tr><tr><td>并发控制</td><td><code>_advancing</code> 重入保护</td><td>并发槽位管理</td></tr><tr><td>持久化</td><td><code>LoopStore</code>（JSON）</td><td>无</td></tr><tr><td>恢复</td><td><code>reconcile()</code> + <code>reSubscribeListeners()</code></td><td>无</td></tr><tr><td>超时</td><td><code>DISPATCH_ROUND_TIMEOUT_MS</code></td><td><code>backgroundStaleTimeoutMs</code></td></tr></tbody></table><h2 id="_11-当前实现边界" tabindex="-1">11. 当前实现边界 <a class="header-anchor" href="#_11-当前实现边界" aria-label="Permalink to &quot;11. 当前实现边界&quot;">​</a></h2><ul><li><strong>没有独立的循环配置文件</strong>：轮数来自 <code>|loop:N|</code> 前缀，其余行为由 <code>src/loop/constants.ts</code> 的常量固定；<code>role.yaml</code> 不提供循环字段。</li><li><strong>钳位静默</strong>：轮数越界不报错，只在解析结果里留下 <code>clamped</code>。</li><li><strong>普通消息不中断循环</strong>：除了 <code>/stop-loop</code>，用户消息与系统重新提示都不会取消运行中的循环。</li><li><strong>树状 worker 上限是全局的</strong>：<code>MAX_TREE_WORKER_SESSIONS</code> 限制的是并发注册的树状循环会话数，不是任意会话数。</li></ul><h2 id="相关页面" tabindex="-1">相关页面 <a class="header-anchor" href="#相关页面" aria-label="Permalink to &quot;相关页面&quot;">​</a></h2><ul><li><a href="/rolebox-docs/02-Guide/functions.html">函数系统</a> — <code>|loop|</code> 的激活语法与使用配方</li><li><a href="/rolebox-docs/03-Reference/tool-catalog.html">工具目录</a> — <code>loop_*</code> 工具的参数与返回格式</li><li><a href="/rolebox-docs/04-Advanced/graph-engine.html">图执行引擎</a> — 命令式编排的引擎模型</li><li><a href="/rolebox-docs/03-Reference/dispatch-config.html">调度配置</a> — dispatch 子系统的配置面</li><li><a href="/rolebox-docs/01-Overview/platform-harnesses.html">平台与 Harness</a> — 各 harness 的工具装配差异</li></ul>`,37))])}const D=n(h,[["render",k]]);export{b as __pageData,D as default};
