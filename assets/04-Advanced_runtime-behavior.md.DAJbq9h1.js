import{_ as c,C as s,o,c as i,a2 as d,b as n,w as t,a as r,E as l,a3 as p}from"./chunks/framework.CuUVYbLV.js";const _=JSON.parse('{"title":"运行时行为","description":"图引擎跑起来之后发生什么——节点状态机、信号推进时序、join 评估、审批与预算检查、崩溃恢复、graph_state 与 turn-end 管线。","frontmatter":{"title":"运行时行为","description":"图引擎跑起来之后发生什么——节点状态机、信号推进时序、join 评估、审批与预算检查、崩溃恢复、graph_state 与 turn-end 管线。"},"headers":[],"relativePath":"04-Advanced/runtime-behavior.md","filePath":"04-Advanced/runtime-behavior.md"}'),h={name:"04-Advanced/runtime-behavior.md"};function g(E,e,k,u,m,b){const a=s("Mermaid");return o(),i("div",null,[e[1]||(e[1]=d('<h1 id="运行时行为-runtime-behavior" tabindex="-1">运行时行为（Runtime Behavior） <a class="header-anchor" href="#运行时行为-runtime-behavior" aria-label="Permalink to &quot;运行时行为（Runtime Behavior）&quot;">​</a></h1><p>图声明通过校验后进入<strong>运行时</strong>阶段：一个运行时对象持有该图的全部状态，按节点发出的信号推进，把关键状态写盘，并在进程崩溃后接着跑。本页讲<strong>时序</strong>——一次执行按什么顺序发生什么、每个节点状态只能从哪里来、到哪里去。</p><p>引擎的静态模型（图的结构、运行时对象与状态字段、各子系统职责）在<a href="/rolebox-docs/04-Advanced/graph-engine.html">图执行引擎</a>；把图搭起来并跑起来的操作步骤在<a href="/rolebox-docs/02-Guide/graph-workflows.html">图工作流</a>；工具参数表在<a href="/rolebox-docs/03-Reference/tools/orchestration-tools.html">编排工具</a>。本页不重复这三者的内容。</p><blockquote><p>前置：<a href="/rolebox-docs/04-Advanced/graph-engine.html">图执行引擎</a>｜相关：<a href="/rolebox-docs/04-Advanced/graph-declaration.html">图声明</a>、<a href="/rolebox-docs/04-Advanced/signal-system.html">信号系统</a></p></blockquote><h2 id="_1-一次执行的时序总览" tabindex="-1">1. 一次执行的时序总览 <a class="header-anchor" href="#_1-一次执行的时序总览" aria-label="Permalink to &quot;1. 一次执行的时序总览&quot;">​</a></h2><p><code>createEngine()</code> 构造运行时但不派发任何东西；<code>provision()</code> 注册节点并引导拓扑；<code>run()</code> 把阶段从 <code>idle</code> 推到 <code>executing</code> 并派发根节点。此后引擎<strong>不再轮询</strong>：每次有节点发出终止信号，就进入一次推进临界区——转换生命周期、评估出边与 join、派发新的就绪节点，并在每次派发后复查终止条件。</p>',6)),(o(),n(p,null,{default:t(()=>[l(a,{id:"mermaid-20",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20IDLE%5B%22EnginePhase.idle%22%5D%20--%3E%7C%22provision()%EF%BC%9A%E6%B3%A8%E5%86%8C%E8%8A%82%E7%82%B9%20%2B%20%E5%BC%95%E5%AF%BC%E6%8B%93%E6%89%91%22%7C%20BOOT%5B%22%E6%A0%B9%E8%8A%82%E7%82%B9%20ready%EF%BC%8C%E5%8A%A0%E5%85%A5%20frontier%22%5D%0A%20%20%20%20BOOT%20--%3E%7C%22run()%22%7C%20EXEC%5B%22EnginePhase.executing%22%5D%0A%20%20%20%20EXEC%20--%3E%7C%22%E6%B4%BE%E5%8F%91%E6%A0%B9%E8%8A%82%E7%82%B9%22%7C%20RUN%5B%22%E8%8A%82%E7%82%B9%20running%22%5D%0A%20%20%20%20RUN%20--%3E%7C%22%E7%BB%88%E6%AD%A2%E4%BF%A1%E5%8F%B7%22%7C%20ADV%5B%22%E6%8E%A8%E8%BF%9B%E4%B8%B4%E7%95%8C%E5%8C%BA%20_runCriticalSection%22%5D%0A%20%20%20%20ADV%20--%3E%7C%22%E8%BD%AC%E6%8D%A2%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%20%2B%20%E8%AF%84%E4%BC%B0%E5%87%BA%E8%BE%B9%20%2B%20join%22%7C%20EDGE%7B%22%E4%B8%8B%E6%B8%B8%20join%20%E6%BB%A1%E8%B6%B3%EF%BC%9F%22%7D%0A%20%20%20%20EDGE%20--%3E%7C%22%E6%BB%A1%E8%B6%B3%22%7C%20READY%5B%22%E4%B8%8B%E6%B8%B8%20ready%EF%BC%8C%E5%8A%A0%E5%85%A5%20frontier%22%5D%0A%20%20%20%20READY%20--%3E%20RUN%0A%20%20%20%20ADV%20--%3E%7C%22%E6%AF%8F%E6%AC%A1%E6%B4%BE%E5%8F%91%E5%90%8E%E5%A4%8D%E6%9F%A5%22%7C%20TERM%7B%22%E8%BF%98%E6%9C%89%E6%B4%BB%E8%B7%83%E8%8A%82%E7%82%B9%EF%BC%9F%22%7D%0A%20%20%20%20TERM%20--%3E%7C%22%E5%90%A6%22%7C%20DONE%5B%22EnginePhase.complete%3Cbr%2F%3E%5BGRAPH%20COMPLETE%5D%22%5D%0A%20%20%20%20TERM%20--%3E%7C%22%E4%BB%85%E5%89%A9%20blocked%22%7C%20BLOCK%5B%22%E4%BF%9D%E6%8C%81%20executing%3Cbr%2F%3E%5BGRAPH%20BLOCKED%5D%22%5D%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[r(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=d(`<p>两条旁路进的是同一个运行时：<code>recover()</code> 从磁盘上的持久化状态接续中断的执行；<code>adoptPrior()</code> 把先前一次运行的逐节点进度采纳进<strong>新建</strong>的运行时（工具层每次构建步骤之后都会重建引擎）。两者都不重复派发已经推进过的节点。</p><h2 id="_2-nodestatus-状态机" tabindex="-1">2. NodeStatus 状态机 <a class="header-anchor" href="#_2-nodestatus-状态机" aria-label="Permalink to &quot;2. NodeStatus 状态机&quot;">​</a></h2><p>9 个状态共享一张转换表（词汇表见<a href="/rolebox-docs/04-Advanced/graph-engine.html">图执行引擎</a>）。合法性是 <code>(from, to)</code> 的纯函数，不读 <code>agent</code> / <code>prompt</code> / <code>needs_approval</code>；实现在 <code>src/graph/engine/node-lifecycle.ts</code>：</p><table tabindex="0"><thead><tr><th><code>from</code></th><th>允许的 <code>to</code></th></tr></thead><tbody><tr><td><code>pending</code></td><td><code>ready</code>、<code>cancelled</code>、<code>escalate</code></td></tr><tr><td><code>ready</code></td><td><code>running</code>、<code>cancelled</code>、<code>escalate</code></td></tr><tr><td><code>running</code></td><td><code>completed</code>、<code>escalate</code>、<code>timeout</code>、<code>cancelled</code>、<code>blocked</code></td></tr><tr><td><code>completed</code></td><td><code>done</code>、<code>ready</code>、<code>escalate</code></td></tr><tr><td><code>blocked</code></td><td><code>completed</code>、<code>ready</code>、<code>escalate</code></td></tr><tr><td><code>timeout</code></td><td><code>done</code></td></tr><tr><td><code>escalate</code></td><td><code>done</code>、<code>ready</code></td></tr><tr><td><code>cancelled</code></td><td><code>done</code></td></tr><tr><td><code>done</code></td><td>——（终态，无）</td></tr></tbody></table><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>正常路径：  pending → ready → running → completed → done</span></span>
<span class="line"><span>暂停路径：  running → blocked → completed（批准）/ ready（拒绝后携带反馈重入）</span></span>
<span class="line"><span>错误路径：  running → escalate → done，或 escalate → ready（自动重试）</span></span>
<span class="line"><span>            running → timeout → done</span></span>
<span class="line"><span>取消路径：  pending / ready / running → cancelled → done</span></span>
<span class="line"><span>级联路径：  pending → escalate（被上游升级波及、从未启动的节点）</span></span></code></pre></div><p>两条<strong>非顺序</strong>边值得单独记住：</p><ul><li><code>completed → ready</code> 是<strong>循环组重入边</strong>：收敛节点发出 <code>revise_needed</code> 且带修订内容时，失败的上游节点重新进入 <code>ready</code>，并受循环组 <code>max_traversals</code> 计数约束。</li><li><code>completed → escalate</code> 是<strong>修订上限边</strong>：评审节点完成了一轮 <code>revise_needed</code> 但循环组遍历额度已耗尽时，改为升级而不是继续循环。</li></ul><p>状态转换与记账是同一次操作：<code>transitionNode()</code> 先断言合法性，再写入与状态耦合的字段——<code>ready → running</code> 递增 <code>sessionsSpawned</code>、写入派发任务 / 会话 ID 与 <code>startedAt</code>；任何 <code>→ completed</code> 写入 <code>completedAt</code> 与结果引用；每次状态变更都标记脏位并自动记录一份生命周期检查点。</p><h2 id="_3-推进临界区-无轮询的推进" tabindex="-1">3. 推进临界区：无轮询的推进 <a class="header-anchor" href="#_3-推进临界区-无轮询的推进" aria-label="Permalink to &quot;3. 推进临界区：无轮询的推进&quot;">​</a></h2><p>一次推进按固定顺序走完五步（<code>src/graph/engine/engine-advance.ts</code>）：</p><ol><li>转换本次终止节点的生命周期。</li><li>评估出边（<code>always</code> / <code>on_signal</code>，<code>on_condition</code> 交给条件解析器）。</li><li>检查下游扇入的 join。</li><li>把满足条件的下游入队（加入前沿）。</li><li>派发就绪节点。</li></ol><p>整个过程运行在 <code>_advancing</code> 临界区内（<code>EngineState.advancingLock</code>）：同一时刻每个图实例只允许一个推进过程。<strong>临界区持锁期间到达的信号不会丢失</strong>——它们被排入 <code>pendingCompletions</code>，待当前临界区退出后在新的临界区中排空重放。锁与队列原语（<code>acquireAdvancingLock()</code> / <code>releaseAdvancingLock()</code> / <code>queuePendingCompletion()</code> / <code>drainPendingCompletions()</code>）都在 <code>src/graph/engine/engine-state.ts</code>。</p><p>一个已知的观测窗口：<code>status()</code> 是同步取的快照，<strong>不获取推进锁</strong>，因此临界区进行中读取快照时，看到的可能是该次推进的中间态而不是静止态。</p><h2 id="_4-信号-→-生命周期-映射与时序" tabindex="-1">4. 信号 → 生命周期：映射与时序 <a class="header-anchor" href="#_4-信号-→-生命周期-映射与时序" aria-label="Permalink to &quot;4. 信号 → 生命周期：映射与时序&quot;">​</a></h2><p>调度任务结束时，引擎把它映射成信号；映射是<strong>共享的单一实现</strong>，实时派发缝与恢复路径用的是同一份（<code>mapDispatchStatusToSignal()</code>，<code>src/graph/engine/engine-recovery.ts</code>）：</p><table tabindex="0"><thead><tr><th>调度任务结局</th><th>引擎信号</th></tr></thead><tbody><tr><td><code>completed</code></td><td><code>answer</code></td></tr><tr><td><code>error</code></td><td><code>escalate</code>（携带任务错误）</td></tr><tr><td><code>timeout</code></td><td><code>escalate</code></td></tr><tr><td><code>cancelled</code></td><td>无终止信号（节点直接取消）</td></tr></tbody></table><p>终止信号把节点推向终局（<code>answer</code> 使节点进入 <code>completed</code>，<code>escalate</code> 使节点进入 <code>escalate</code>；<code>revise_needed</code> 先完成评审节点自身，再由回边重开上游）；暂停信号（<code>need_approval</code> / <code>blocked</code> / <code>need_clarification</code>）把节点推入 <code>blocked</code>；同一次运行记录了多个终止信号时，按最坏严重度择一：<code>escalate</code> &gt; <code>revise_needed</code> &gt; <code>answer</code>。</p><p>循环组成员的终止信号不直接走上面的路径，而是先经过 <code>executeLoopStep()</code>（<code>src/graph/engine/loop-group-executor.ts</code>）的<strong>合并收敛决策</strong>：它协调遍历计数、revise 重派发、升级级联与上游取消，并返回六种结局之一——<code>converged</code> / <code>revising</code> / <code>stuck</code> / <code>max_traversals_exhausted</code> / <code>escalating</code> / <code>ignored</code>。上限耗尽或停滞退出时携带结构化升级载荷（<code>reason</code>、尽力抽取的未解决项、退出时的遍历计数）。</p><h2 id="_5-join-评估" tabindex="-1">5. join 评估 <a class="header-anchor" href="#_5-join-评估" aria-label="Permalink to &quot;5. join 评估&quot;">​</a></h2><p><strong>扇入</strong>的判定入口是 <code>evaluateJoin(state, node)</code>（<code>src/graph/engine/join-evaluator.ts</code>）。它把每个上游按已记录的信号分入三桶：<code>answer</code> 计成功，任何非 <code>answer</code> 的终止信号计失败，尚无载荷的计待定；然后按策略给出三态结论 <code>JoinVerdict</code>：<code>satisfied</code> / <code>failed</code> / <code>waiting</code>。</p><table tabindex="0"><thead><tr><th>策略</th><th>满足条件</th><th>失败条件</th></tr></thead><tbody><tr><td>无上游边</td><td>图根节点，立即满足</td><td>——</td></tr><tr><td><code>all</code></td><td>每个上游都记录 <code>answer</code></td><td>任一上游记录非 <code>answer</code> 终止信号</td></tr><tr><td><code>any</code></td><td>第一个上游 <code>answer</code> 到达</td><td>所有上游都在任何 <code>answer</code> 之前发出非 <code>answer</code> 终止信号</td></tr><tr><td><code>quorum:N</code></td><td><code>answer</code> 计数 ≥ N</td><td><code>answer + pending &lt; N</code>（配额已不可能达成）</td></tr></tbody></table><p>两条时序细节：循环组<strong>首轮</strong>（<code>traversalCount === 0</code>）会从上游集合中排除 <code>revise_needed</code> 回边，让入口节点的 join 只靠外部上游即可满足，后续轮次与非循环节点计入全部上游；<code>joinSatisfied()</code> 是同一评估的布尔投影（仅 <code>satisfied</code> 为真），结果缓存到节点运行状态供调用方免于重复推导拓扑。</p><h2 id="_6-revise-与-escalate-的传播时序" tabindex="-1">6. revise 与 escalate 的传播时序 <a class="header-anchor" href="#_6-revise-与-escalate-的传播时序" aria-label="Permalink to &quot;6. revise 与 escalate 的传播时序&quot;">​</a></h2><p><strong>revise 传播</strong>（<code>propagateRevise()</code>，<code>src/graph/engine/signal-propagation.ts</code>）按固定顺序处理收敛节点发出的 <code>revise_needed</code>：</p><ol><li>节点不属于任何循环组 → 直接升级，原因为 <code>no loop group</code>。</li><li><strong>停滞早退</strong>：连续两次收敛输出指纹相同，说明评审无法推进 → 以 <code>stuck</code> 置为 <code>done</code>。</li><li><strong>遍历计数</strong>：递增循环组遍历计数失败（已达 <code>max_traversals</code>）→ 以 <code>max_traversals exhausted</code> 置为 <code>done</code>。</li><li>否则沿 <code>on_signal(revise_needed)</code> 回边，把<strong>同一循环组内</strong>、且可由 <code>ready</code> 到达的上游节点重新标记 <code>ready</code> 并加入前沿，同时把修订反馈并入其重执行提示。</li></ol><p><strong>escalate 传播</strong>（<code>propagateEscalate()</code>）先过<strong>重试闸门</strong>：有效重试额度取三个来源的最大值——节点自身 <code>budget.max_retries</code>、任一出边 <code>retry.max</code>、任一入边 <code>retry.max</code>。额度尚有余额时递增 <code>retryCount</code>、把上一次失败原因并入提示、重新标记 <code>ready</code> 并加入前沿；若限定重试边声明了 <code>backoff_ms</code>，则把重新派发推迟到 <code>now + backoff_ms</code>（写入 <code>retryBackoffUntil</code>）。</p><p>额度用尽则沿出边做 BFS：单输入节点是透明直通；遇到第一个多输入扇入节点就把 <code>escalate</code> 记为上游结果并重新评估其 join——<code>failed</code> 则该汇聚节点也升级并继续前推；<code>satisfied</code> / <code>waiting</code> 表示部分失败被吸收（<code>any</code> / <code>quorum</code> 仍可推进），该分支停止。升级到达无出边的汇点，表示已到尽头。</p><h2 id="_7-级联取消时序" tabindex="-1">7. 级联取消时序 <a class="header-anchor" href="#_7-级联取消时序" aria-label="Permalink to &quot;7. 级联取消时序&quot;">​</a></h2><p><code>cancelPendingUpstreams(state, node, joinVerdict, dispatchPort?)</code>（<code>src/graph/engine/cascade-canceller.ts</code>）是扇入的<strong>自动回收半边</strong>：汇聚节点的 join 一旦判定完成，不再可能影响结果的上游就被退掉。</p><ul><li><code>waiting</code> → 严格空操作，不取消任何节点。</li><li><code>satisfied</code> / <code>failed</code> → 逐个回收尚未产出载荷的上游：生命周期走 <code>running | ready | pending → cancelled → done</code>，并从前沿移除；有派发缝且节点带派发任务 ID 时，以 fire-and-forget 方式取消派发任务（永不等待确认）。</li><li>已经记录过载荷的上游（<code>answer</code> / <code>escalate</code> / <code>revise_needed</code>）报为 <code>alreadyResolved</code> 并原样保留，其部分失败信号继续供下游诊断使用。</li><li><strong>共享上游守卫</strong>：若某上游仍被另一个 join 未决（<code>waiting</code>）的兄弟汇聚节点需要，则跳过它，避免饿死兄弟节点。</li></ul><p>上面这条路径只回收<strong>上游</strong>；取消一整张图或指定节点走的是另外两个入口——<code>cancel()</code>（整图，阶段推进到 <code>complete</code>，<code>blocked</code> 节点留给人工）与 <code>cancelNodes()</code>（局部，<code>cascade</code> 时级联到传递下游，不触碰阶段）。</p><h2 id="_8-审批门与预算-运行时检查" tabindex="-1">8. 审批门与预算：运行时检查 <a class="header-anchor" href="#_8-审批门与预算-运行时检查" aria-label="Permalink to &quot;8. 审批门与预算：运行时检查&quot;">​</a></h2><p>声明了 <code>needs_approval</code> 的节点派发后进入 <code>blocked</code>。这是一个<strong>暂停标志</strong>：引擎停下该节点下游子图的推进，并按会话链向上发出 <code>[GRAPH BLOCKED]</code> 提示，等待人工决策。决策通过 <code>graph_approve</code> 工具进入：</p><table tabindex="0"><thead><tr><th>决策</th><th>转换</th><th>效果</th></tr></thead><tbody><tr><td>批准 <code>approve</code></td><td><code>blocked → completed</code></td><td>记录 <code>answer</code> 信号并激活前向 <code>answer</code> 边</td></tr><tr><td>拒绝 <code>reject</code></td><td><code>blocked → ready</code> 或 <code>blocked → escalate</code></td><td>有循环组可重开则携带反馈重入，否则升级</td></tr><tr><td>部分批准</td><td>分支级</td><td>接受 <code>approved</code> 上游、取消 <code>rejected</code> 分支的传递依赖，被拒上游重入 <code>ready</code></td></tr></tbody></table><p>拒绝反馈的重执行提示合并由 <code>mergeRejectionFeedback()</code> 完成（<code>src/graph/engine/approval-handler.ts</code>）。对已经解决的节点再次调用 <code>graph_approve</code> 是<strong>真正的空操作</strong>：不派发就绪前沿、不复查终止，并如实返回 <code>applied=false</code>。</p><p><strong>预算检查有两个粒度。</strong> <code>checkGraphBudget(graphId, state)</code> 是两层检查：先委托 <code>BudgetTracker.isRequestBudgetExceeded(graphId)</code>（图 ID 即请求作用域），再对照图声明自身的 <code>max_total_input_tokens</code> / <code>max_total_output_tokens</code> / <code>max_total_cost_usd</code> 比较累计计数器（<code>&gt;=</code> 即越界），返回第一个越界项。<code>checkNodeBudget()</code> 是派发前的逐节点预检，对照节点声明的上限与节点累计用量。图级越界会让就绪节点升级并取消被搁置的 <code>pending</code> 节点，使图能到达 <code>complete</code> 而不是挂死；用量在任务终止时累计进预算总额。</p><p>一个容易误解的字段：未声明预算时，<code>state.budget.sessionsSpawned</code> 只是净值展示计数器，<strong>永不</strong>作为闸门。</p><h2 id="_9-终止复查与通知" tabindex="-1">9. 终止复查与通知 <a class="header-anchor" href="#_9-终止复查与通知" aria-label="Permalink to &quot;9. 终止复查与通知&quot;">​</a></h2><p>每次派发过程结束后，引擎复查一次终止（<code>_checkTermination()</code> → <code>checkGraphTermination()</code>，<code>src/graph/engine/engine-termination.ts</code>），只有三种结局：</p><ol><li>不再有活跃节点（无 <code>running</code> / <code>ready</code> / <code>pending</code> / <code>blocked</code>）→ 阶段 <code>executing → complete</code>，发出 <code>isBlocked=false</code> 的终止事件，提示为 <code>[GRAPH COMPLETE]</code>。</li><li>不再有<strong>调度活跃</strong>节点（无 <code>running</code> / <code>ready</code> / <code>pending</code>）但仍有 ≥1 个 <code>blocked</code> → 发出 <code>isBlocked=true</code> 的终止事件，<strong>但不做阶段转换</strong>：图保持 <code>executing</code> 等待人工，提示为 <code>[GRAPH BLOCKED]</code>。</li><li><code>pending</code> 节点<strong>不是</strong>调度活跃节点：它只等上游结果，自身永远无法推进图。</li></ol><p>终止事件按状态分别计数（<code>completed</code> / <code>done</code> / <code>cancelled</code> / <code>escalate</code> / <code>timeout</code> / <code>blocked</code> / <code>running</code>），其中 <code>done</code> 与 <code>cancelled</code> 单独成桶，避免被取消或循环上限退场的节点被当成成功。两类终止事件各有两层去重——每次运行实例的标记加持久化的 <code>terminalNotified</code>——保证每轮至多通知一次。</p><h2 id="_10-持久化、恢复与平台差异" tabindex="-1">10. 持久化、恢复与平台差异 <a class="header-anchor" href="#_10-持久化、恢复与平台差异" aria-label="Permalink to &quot;10. 持久化、恢复与平台差异&quot;">​</a></h2><table tabindex="0"><thead><tr><th>文件</th><th>路径</th><th>说明</th></tr></thead><tbody><tr><td>引擎状态</td><td><code>.rolebox/state/engine-{slug}.json</code></td><td>单个图的持久化状态（阶段、节点生命周期、预算、前沿、循环组、检查点）；slug 由 <code>engineStateSlug()</code> 从 <code>graphId</code> 提取</td></tr><tr><td>事件日志</td><td><code>.rolebox/state/graph-events-{hash}.ndjson</code></td><td>引擎写侧事件的追加式 NDJSON 日志（节点派发、终止转换、阶段变化、预算更新）</td></tr></tbody></table><p>持久化格式带版本号（<code>ENGINE_PERSISTENCE_VERSION = 2</code>）；序列化、反序列化与容错加载分别是 <code>serializeEngineState()</code> / <code>deserializeEngineState()</code> / <code>loadEngineStateFromJson()</code>；版本不匹配的文件不会被加载。写盘分两级（<code>src/graph/engine/engine-persistence.ts</code>）：</p><ul><li><strong>关键写（同步）</strong>：节点生命周期、阶段、前沿、检查点、审批状态等关键变更立即同步落盘；<code>save()</code> 是原子写（临时文件 + rename），失败时返回 <code>false</code> 且<strong>从不抛错</strong>，调用方据此保留脏标记以便下次重试。</li><li><strong>非关键写（防抖）</strong>：信号台账历史、预算 / 逐节点用量计数等非关键变更走 <code>scheduleSave()</code>，<code>500 ms</code> 防抖合并为一次写；到达终止阶段时由 <code>flush()</code> 强制排空。</li><li>被替换或丢弃的运行时调用 <code>dispose()</code>：<strong>取消</strong>防抖并丢弃待写内容，绝不把陈旧状态刷到后继运行时的状态文件上。</li></ul><p>进程崩溃后持久化状态仍在，但内存中的派发监听与推进临界区都已消失。重启后 <code>recover()</code> 按四步走：</p><ol><li>加载持久化状态。</li><li>把每个 <code>running</code> 节点与调度系统对账：任务已消失 → <code>timeout</code>；任务在重启窗口内完成 → 重发其终止信号；任务仍存活 → 重新订阅其终止监听。</li><li>由所有 <code>ready</code> 节点重建前沿。</li><li>排空窗口期内推迟的完成事件。</li></ol><p>相关原语包括 <code>reconcileEngine()</code>、<code>hydrateEngineState()</code>、<code>adoptPriorNodeStates()</code> 与陈旧临界区清理 <code>clearStaleCriticalSection()</code>（都在 <code>src/graph/engine/engine-recovery.ts</code>）。插件启动时还有一次<strong>恢复扫掠</strong>（<code>recoverInterruptedGraphs()</code>，<code>src/graph/engine/engine-startup.ts</code>）：扫描 <code>.rolebox/state/engine-*.json</code>，跳过阶段已是 <code>complete</code> 的图，对其余每个图构建引擎并等待恢复；逐图 try/catch，单个损坏文件绝不中断整轮扫掠。</p><p><strong>平台差异。</strong> 在 opencode 平台上引擎完全在内存中运行：状态从不写入 <code>engine-*.json</code>，也没有持久化的事件日志，图生命周期与创建它的进程同生共死（契约见 <code>src/graph/tools/live-state.ts</code>）；dsh / Pi 走上面的磁盘持久化路径。</p><h2 id="_11-每轮-graph-state-提示块" tabindex="-1">11. 每轮 graph_state 提示块 <a class="header-anchor" href="#_11-每轮-graph-state-提示块" aria-label="Permalink to &quot;11. 每轮 graph_state 提示块&quot;">​</a></h2><blockquote><p>自 v1.8.0 起，引擎每轮向系统提示注入 <code>&lt;graph_state&gt;</code> 定向块，取代 v1 的旧状态注入块。</p></blockquote><p><code>buildEngineGraphStateBlock(states)</code> 是一个<strong>纯渲染器</strong>（<code>src/graph/engine/graph-state-block.ts</code>）：它接收引擎状态快照列表，只渲染实际存在的字段，回答「我在工作流的哪一步、下一步跑什么」。没有活动图时返回空字符串，调用方视作干净的空操作，提示里不会留下孤立标签。</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">graph_state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">graph</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;review-loop-1737000000000-1&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> phase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;executing&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;review-loop&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">active_nodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;reviewer&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">active_nodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">pending_nodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;writer, critic&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">pending_nodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">loop_groups</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">loop</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;writer-critic&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> traversals</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1/3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">loop_groups</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">blocked_nodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">node</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gate&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> needs_approval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;awaiting human approval&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">node</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">blocked_nodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">graph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">graph_state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><ul><li>数据来源是 <code>GraphToolSet.liveEngineStates()</code>：按注册顺序快照工具集内存注册表中的每个运行时（经 <code>EngineRuntime.status()</code>，因此是克隆副本）。</li><li><code>&lt;active_nodes&gt;</code> 收集 <code>running</code> 节点；<code>&lt;pending_nodes&gt;</code> 收集 <code>pending</code> 与 <code>ready</code> 节点。</li><li><code>&lt;loop_groups&gt;</code> 仅在存在循环组时出现，逐组给出 <code>traversals=&quot;已用/上限&quot;</code>。</li><li><code>&lt;blocked_nodes&gt;</code> 仅在存在 <code>blocked</code> 节点时出现，正文是该节点的错误原因，缺省为 <code>awaiting human approval</code>，并带 <code>needs_approval</code> 属性。</li><li>图名与 ID 由用户 / 声明控制，渲染时转义 <code>&amp;</code>、<code>&lt;</code>、<code>&gt;</code>、<code>&quot;</code> 四类字符。</li><li>注入点是 <code>system.transform</code> 钩子（<code>src/hooks/system-transform.ts</code>）：每轮解析一次，非空时推入系统提示。</li></ul><h2 id="_12-turn-end-决策管线-copilot" tabindex="-1">12. turn-end 决策管线（Copilot） <a class="header-anchor" href="#_12-turn-end-决策管线-copilot" aria-label="Permalink to &quot;12. turn-end 决策管线（Copilot）&quot;">​</a></h2><p>除图运行时之外，rolebox 在 <code>session.idle</code> 上还有一条<strong>收尾决策</strong>管线（<code>src/hooks/event-handler.ts</code> 触发）：一轮对话结束时决定「是否再给模型推一把」。<code>runTurnEndPipeline()</code>（<code>src/copilot/pipeline.ts</code>）按<strong>严格优先级</strong>依次评估三个来源，且每次 idle 至多注入一次：</p><table tabindex="0"><thead><tr><th>顺序</th><th>来源</th><th>语义</th></tr></thead><tbody><tr><td>1</td><td>builtin 函数续跑</td><td>始终先评估；一旦注入即停止</td></tr><tr><td>2</td><td>用户启发式规则</td><td>对最后一条助手文本求值：命中 <code>skip</code> 即消费本轮（不注入、也不下沉到 LLM）；命中 continue / blocked / done 则注入回复文本并停止</td></tr><tr><td>3</td><td>LLM 角色裁决</td><td>转录 → 提示 → 裁决；<code>advance</code> 注入回复文本，<code>hand_to_user</code> 或任何失败都不注入</td></tr></tbody></table><p>规则语义（<code>src/copilot/rules.ts</code>）：按配置顺序求值，<strong>首个命中者胜</strong>；<code>match.pattern</code> 是对最后助手文本的正则，<code>match.contains</code> 是大小写不敏感子串，两者同时给出时<strong>取与</strong>；非法正则只告警并跳过该条规则，求值<strong>永不抛错</strong>；未命中则下沉到下一个来源。</p><p>角色未配置 copilot 或 <code>copilot.enabled</code> 为 <code>false</code> 时，<strong>只有</strong> builtin 来源运行。LLM 裁决跑在一个全新的子会话上并带硬超时（<code>max_verdict_timeout_ms</code> 默认 <code>30000</code>），任何失败都跳过。copilot 注入的提示带 <code>COPILOT_MARKER</code>（<code>src/copilot/constants.ts</code>），因此在 <code>chat.message</code> 重入时被归类为<strong>合成注入</strong>：既不重置续跑计数器，也不取消活跃循环。设计上不设注入预算或代码级护栏——破坏性与 HITL（人工在环）的谨慎属于<strong>建议性</strong>提示指引，由角色作者判断掌控。</p><h2 id="相关页面" tabindex="-1">相关页面 <a class="header-anchor" href="#相关页面" aria-label="Permalink to &quot;相关页面&quot;">​</a></h2><ul><li><a href="/rolebox-docs/04-Advanced/graph-engine.html">图执行引擎</a> —— 图、节点、边与子系统职责的静态模型</li><li><a href="/rolebox-docs/04-Advanced/graph-declaration.html">图声明</a> —— 图文档的字段、校验与序列化</li><li><a href="/rolebox-docs/02-Guide/graph-workflows.html">图工作流</a> —— 用 <code>graph_*</code> 编排团队的任务视角</li><li><a href="/rolebox-docs/04-Advanced/signal-system.html">信号系统</a> —— 信号类型、台账与 <code>signal</code> 工具</li><li><a href="/rolebox-docs/04-Advanced/loop-system.html">循环系统</a> —— LoopCoordinator 与 <code>|loop|</code> 函数</li><li><a href="/rolebox-docs/04-Advanced/workflow-patterns.html">工作流模式</a> —— 内建拓扑与选型</li><li><a href="/rolebox-docs/06-Appendix/source-index.html">源码索引</a> —— 概念到模块的总索引</li></ul>`,61))])}const B=c(h,[["render",g]]);export{_ as __pageData,B as default};
